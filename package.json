{
  "name": "hablla",
  "version": "0.1.0",
  "description": "Typed TypeScript SDK for the Hablla API, generated from the studio bundle and the official swagger.",
  "license": "UNLICENSED",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "assets"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "@babel/parser": "^7.25.0",
    "@babel/types": "^7.25.0",
    "@types/node": "^20.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^2.0.0"
  }
}
