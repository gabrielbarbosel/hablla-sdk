// GENERATED by tooling/build-gas.js from the hablla SDK v0.1.10. Do not edit.
// Instala globalThis.hablla (client) e globalThis.Hablla ({ runSync, unwrap }).
// O Code.gs consome: runSync(() => hablla.<resource>.<metodo>(...)).

"use strict";
(() => {
  var __defProp = Object.defineProperty;
  var __defProps = Object.defineProperties;
  var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
  var __getOwnPropSymbols = Object.getOwnPropertySymbols;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __propIsEnum = Object.prototype.propertyIsEnumerable;
  var __pow = Math.pow;
  var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
  var __spreadValues = (a, b) => {
    for (var prop in b || (b = {}))
      if (__hasOwnProp.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    if (__getOwnPropSymbols)
      for (var prop of __getOwnPropSymbols(b)) {
        if (__propIsEnum.call(b, prop))
          __defNormalProp(a, prop, b[prop]);
      }
    return a;
  };
  var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
  var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
  var __async = (__this, __arguments, generator) => {
    return new Promise((resolve, reject) => {
      var fulfilled = (value) => {
        try {
          step(generator.next(value));
        } catch (e) {
          reject(e);
        }
      };
      var rejected = (value) => {
        try {
          step(generator.throw(value));
        } catch (e) {
          reject(e);
        }
      };
      var step = (x) => x.done ? resolve(x.value) : Promise.resolve(x.value).then(fulfilled, rejected);
      step((generator = generator.apply(__this, __arguments)).next());
    });
  };

  // src/sdk/core/strategy.ts
  var MemoryStrategyCache = class {
    constructor() {
      __publicField(this, "map", {});
    }
    load() {
      return __async(this, null, function* () {
        return __spreadValues({}, this.map);
      });
    }
    save(map) {
      return __async(this, null, function* () {
        this.map = __spreadValues({}, map);
      });
    }
  };

  // src/sdk/core/auth.ts
  var HabllaAuth = class {
    constructor(transport, config, cache = new MemoryStrategyCache()) {
      __publicField(this, "transport", transport);
      __publicField(this, "config", config);
      __publicField(this, "cache", cache);
      __publicField(this, "accessToken", null);
      __publicField(this, "expiresAt", 0);
      __publicField(this, "pending", null);
      __publicField(this, "strategies", {});
      __publicField(this, "loaded", false);
      __publicField(this, "maxAttempts");
      __publicField(this, "maxBackoffMs");
      var _a, _b, _c, _d;
      this.maxAttempts = (_b = (_a = config.retry) == null ? void 0 : _a.maxAttempts) != null ? _b : 6;
      this.maxBackoffMs = (_d = (_c = config.retry) == null ? void 0 : _c.maxBackoffMs) != null ? _d : 8e3;
    }
    /** Replaces the strategy cache (e.g. with a durable runtime implementation). */
    useCache(cache) {
      this.cache = cache;
      this.loaded = false;
    }
    get isExpired() {
      this.syncFromGlobal();
      return !this.accessToken || Date.now() > this.expiresAt - 3e4;
    }
    syncFromGlobal() {
      var _a, _b;
      const g = globalThis;
      if (((_a = g.HABLLA_ENV) == null ? void 0 : _a.accessToken) && ((_b = g.HABLLA_ENV) == null ? void 0 : _b.accessTokenExp)) {
        const exp = Number(g.HABLLA_ENV.accessTokenExp) || 0;
        if (Date.now() < exp - 3e4) {
          this.accessToken = g.HABLLA_ENV.accessToken;
          this.expiresAt = exp;
        }
      }
    }
    syncToGlobal(accessToken, expiresAt) {
      this.accessToken = accessToken;
      this.expiresAt = expiresAt;
      const g = globalThis;
      if (g.HABLLA_ENV) {
        g.HABLLA_ENV.accessToken = accessToken;
        g.HABLLA_ENV.accessTokenExp = expiresAt;
      }
    }
    /** Returns a valid Bearer access token, refreshing when needed. */
    token() {
      return __async(this, null, function* () {
        var _a;
        this.syncFromGlobal();
        if (this.accessToken && !this.isExpired) return this.accessToken;
        (_a = this.pending) != null ? _a : this.pending = this.refresh().finally(() => {
          this.pending = null;
        });
        return this.pending;
      });
    }
    /** Authorization header value for a strategy. */
    authorization(strategy) {
      return __async(this, null, function* () {
        return strategy === "bearer" ? `Bearer ${yield this.token()}` : this.config.workspaceToken;
      });
    }
    /** Strategy to try for an endpoint: the cached choice, otherwise workspace first. */
    resolveStrategy(cacheKey) {
      return __async(this, null, function* () {
        var _a;
        if (!this.loaded) {
          this.strategies = yield this.cache.load();
          this.loaded = true;
        }
        return (_a = this.strategies[cacheKey]) != null ? _a : "workspace";
      });
    }
    /** Records the working strategy for an endpoint and persists the change. */
    recordStrategy(cacheKey, strategy) {
      return __async(this, null, function* () {
        if (this.strategies[cacheKey] === strategy) return;
        this.strategies[cacheKey] = strategy;
        yield this.cache.save(this.strategies);
      });
    }
    /**
     * Resolves a Bearer token while tolerating the cross-isolate refresh race:
     * each attempt first re-reads the shared token from `HABLLA_ENV` (a sibling
     * isolate may have just refreshed it) and only calls Firebase when the shared
     * token is still stale. Transient Firebase failures (e.g. 400 from concurrent
     * refreshes) are retried with exponential backoff and jitter.
     */
    refresh() {
      return __async(this, null, function* () {
        for (let attempt = 0; ; attempt++) {
          this.syncFromGlobal();
          if (this.accessToken && Date.now() < this.expiresAt - 3e4) return this.accessToken;
          try {
            return yield this.refreshOnce();
          } catch (err) {
            if (attempt >= this.maxAttempts - 1) throw err;
            const backoff = Math.min(500 * __pow(2, attempt), this.maxBackoffMs) + Date.now() % 250;
            yield new Promise((resolve) => setTimeout(resolve, backoff));
          }
        }
      });
    }
    refreshOnce() {
      return __async(this, null, function* () {
        var _a, _b;
        const key = encodeURIComponent(this.config.firebaseApiKey);
        const body = `grant_type=refresh_token&refresh_token=${encodeURIComponent(this.config.refreshToken)}`;
        const res = yield this.transport.send({
          method: "POST",
          url: `https://securetoken.googleapis.com/v1/token?key=${key}`,
          headers: { "Content-Type": "application/x-www-form-urlencoded" },
          body
        });
        if (res.status >= 300 || !((_a = res.data) == null ? void 0 : _a.access_token)) {
          throw new Error(`Firebase refresh failed (${res.status})`);
        }
        const expiresAt = Date.now() + Number((_b = res.data.expires_in) != null ? _b : 3600) * 1e3;
        this.syncToGlobal(res.data.access_token, expiresAt);
        return res.data.access_token;
      });
    }
  };

  // src/sdk/core/types.ts
  function isMultipart(body) {
    return !!body && typeof body === "object" && body.kind === "multipart";
  }

  // src/sdk/core/query.ts
  function serializeQuery(obj) {
    if (!obj || typeof obj !== "object") return "";
    const parts = [];
    const add = (key, val) => {
      if (val == null) return;
      if (Array.isArray(val)) {
        val.forEach((v, i) => add(`${key}[${i}]`, v));
      } else if (typeof val === "object") {
        for (const [k, v] of Object.entries(val)) add(`${key}[${k}]`, v);
      } else {
        parts.push(`${encodeURIComponent(key)}=${encodeURIComponent(String(val))}`);
      }
    };
    for (const [k, v] of Object.entries(obj)) add(k, v);
    return parts.length ? `?${parts.join("&")}` : "";
  }
  function serializeQueryJson(obj) {
    if (!obj || typeof obj !== "object") return "";
    const parts = [];
    for (const [key, val] of Object.entries(obj)) {
      if (val == null) continue;
      const encoded = typeof val === "object" ? JSON.stringify(val) : String(val);
      parts.push(`${encodeURIComponent(key)}=${encodeURIComponent(encoded)}`);
    }
    return parts.length ? `?${parts.join("&")}` : "";
  }

  // src/sdk/core/errors.ts
  function safeStringify(value, max) {
    try {
      return JSON.stringify(value).slice(0, max);
    } catch (e) {
      return String(value).slice(0, max);
    }
  }
  var HabllaApiError = class extends Error {
    constructor(res, debug) {
      var _a;
      let message = `Hablla API ${res.status}: ${safeStringify(res.data, 200)}`;
      if (debug) {
        message += ` | req: ${debug.method} ${debug.path}`;
        if (debug.requestBody !== void 0) {
          message += ` body=${safeStringify(debug.requestBody, 300)}`;
        }
        if ((_a = debug.trace) == null ? void 0 : _a.length) {
          const trace = debug.trace.map((t) => `${t.method} ${t.path} -> ${t.status}`).join(" ; ");
          message += ` | trace: ${trace}`;
        }
      }
      super(message);
      __publicField(this, "status");
      __publicField(this, "data");
      __publicField(this, "debug");
      this.name = "HabllaApiError";
      this.status = res.status;
      this.data = res.data;
      this.debug = debug;
    }
  };

  // src/sdk/core/pagination.ts
  var sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));
  function statusOf(err) {
    var _a, _b;
    const e = err;
    return (_b = e == null ? void 0 : e.status) != null ? _b : (_a = e == null ? void 0 : e.response) == null ? void 0 : _a.status;
  }
  function paginate(_0) {
    return __async(this, arguments, function* (fetchPage, opts = {}) {
      var _a, _b, _c, _d, _e, _f, _g;
      const limit = (_a = opts.limit) != null ? _a : 100;
      const concurrency = (_b = opts.concurrency) != null ? _b : 4;
      const retries = (_c = opts.retries) != null ? _c : 2;
      const slaRounds = (_d = opts.slaRounds) != null ? _d : 3;
      const startPage = (_e = opts.startPage) != null ? _e : 1;
      const maxPages = (_f = opts.maxPages) != null ? _f : Infinity;
      const eachPage = opts.eachPage;
      const byPage = {};
      let processed = 0;
      let fetchedPages = 0;
      const take = (items2, page) => {
        processed += items2.length;
        fetchedPages += 1;
        if (eachPage) eachPage(items2, page);
        else byPage[page] = items2;
      };
      const fetchWithRetry = (page) => __async(null, null, function* () {
        let lastErr;
        for (let attempt = 0; attempt <= retries; attempt += 1) {
          try {
            return yield fetchPage(page, limit);
          } catch (err) {
            lastErr = err;
            const status = statusOf(err);
            if (status != null && status < 500 && status !== 429) throw err;
            yield sleep(300 * __pow(2, attempt));
          }
        }
        throw lastErr;
      });
      const head = yield fetchWithRetry(startPage);
      const totalPages = head.totalPages || 1;
      const totalItems = (_g = head.totalItems) != null ? _g : head.results.length;
      const lastPage = Math.min(totalPages, startPage + maxPages - 1);
      take(head.results, startPage);
      let queue = [];
      for (let p = startPage + 1; p <= lastPage; p += 1) queue.push(p);
      const failedPages = [];
      for (let round = 0; round <= slaRounds; round += 1) {
        if (queue.length === 0) break;
        const pending = queue;
        queue = [];
        const retryable = [];
        for (let i = 0; i < pending.length; i += concurrency) {
          const slice = pending.slice(i, i + concurrency);
          yield Promise.all(
            slice.map((page) => __async(null, null, function* () {
              try {
                const res = yield fetchWithRetry(page);
                take(res.results, page);
              } catch (e) {
                retryable.push(page);
              }
            }))
          );
        }
        queue = retryable;
        if (round === slaRounds) failedPages.push(...retryable);
      }
      const items = eachPage ? [] : Object.keys(byPage).sort((a, b) => Number(a) - Number(b)).flatMap((p) => byPage[Number(p)]);
      const nextPage = lastPage < totalPages ? lastPage + 1 : null;
      return {
        items,
        processed,
        totalItems,
        totalPages,
        fetchedPages,
        failedPages,
        nextPage,
        done: failedPages.length === 0 && nextPage === null
      };
    });
  }

  // src/sdk/core/http-client.ts
  var TRACE_LIMIT = 25;
  function isNetworkError(err) {
    var _a;
    const msg = String((_a = err == null ? void 0 : err.message) != null ? _a : "");
    return (err == null ? void 0 : err.code) === "ECONNABORTED" || /timeout|ETIMEDOUT|ECONNRESET|socket hang up|network/i.test(msg) || /dns error|address unavailable|unable to connect|connection refused|ENOTFOUND|EAI_AGAIN|ECONNREFUSED/i.test(msg);
  }
  var HabllaHttpClient = class {
    constructor(transport, auth, config) {
      __publicField(this, "transport", transport);
      __publicField(this, "auth", auth);
      __publicField(this, "config", config);
      __publicField(this, "debug");
      __publicField(this, "maxAttempts");
      __publicField(this, "maxBackoffMs");
      var _a, _b, _c, _d, _e;
      this.debug = (_a = config.debug) != null ? _a : false;
      this.maxAttempts = (_c = (_b = config.retry) == null ? void 0 : _b.maxAttempts) != null ? _c : 6;
      this.maxBackoffMs = (_e = (_d = config.retry) == null ? void 0 : _d.maxBackoffMs) != null ? _e : 2e4;
    }
    /** Appends to the runtime-global debug trace (ring buffer). No-op unless debug. */
    record(entry) {
      var _a;
      const g = globalThis;
      const dbg = (_a = g.$habllaDebug) != null ? _a : g.$habllaDebug = { trace: [] };
      dbg.trace.push(entry);
      if (dbg.trace.length > TRACE_LIMIT) dbg.trace.shift();
      return dbg.trace;
    }
    resolvePath(rawPath, params = {}) {
      return rawPath.replace(/{([^}]+)}/g, (match, token) => {
        var _a;
        const key = token.includes(".") ? token.slice(token.lastIndexOf(".") + 1) : token;
        const val = (_a = params[key]) != null ? _a : key.includes("workspace") ? this.config.workspaceId : null;
        if (val == null) throw new Error("Missing path parameter: " + token);
        return encodeURIComponent(String(val));
      });
    }
    /**
     * Content-Type is a per-body decision. A multipart body owns its own
     * Content-Type (the transport generates the boundary), so none is set here. An
     * explicit `contentType` (e.g. a form-urlencoded string) is honored as given. A
     * plain JSON object defaults to `application/json`; a raw string with no
     * explicit content type is sent as-is, never re-labeled as JSON.
     */
    send(method, url, opts, strategy) {
      return __async(this, null, function* () {
        const headers = {
          Accept: "application/json",
          Authorization: yield this.auth.authorization(strategy)
        };
        const body = opts.body;
        if (isMultipart(body)) {
        } else if (opts.contentType) {
          headers["Content-Type"] = opts.contentType;
        } else if (body != null && typeof body === "object") {
          headers["Content-Type"] = "application/json";
        }
        Object.assign(headers, opts.headers);
        return this.transport.send({ method, url, headers, body });
      });
    }
    /**
     * Retries transient failures with exponential backoff and jitter. A 429 (rate
     * limit) is always safe to retry — the request was rejected, not processed. A
     * 5xx or a network/timeout error is only retried for idempotent methods (GET):
     * on a POST/PUT/PATCH/DELETE the request may have been applied before the
     * response was lost, so retrying could duplicate a send or a create.
     */
    request(_0, _1) {
      return __async(this, arguments, function* (method, rawPath, opts = {}) {
        var _a, _b;
        const sleep2 = (ms) => new Promise((r) => setTimeout(r, ms));
        const idempotent = method === "GET" || method === "HEAD";
        for (let attemptNum = 0; ; attemptNum++) {
          try {
            return yield this._requestOnce(method, rawPath, opts);
          } catch (err) {
            const status = (_b = err == null ? void 0 : err.status) != null ? _b : (_a = err == null ? void 0 : err.response) == null ? void 0 : _a.status;
            const transient = status === 429 || idempotent && (status != null && status >= 500 || isNetworkError(err));
            if (transient && attemptNum < this.maxAttempts - 1) {
              yield sleep2(Math.min(1e3 * Math.pow(2, attemptNum), this.maxBackoffMs) + Date.now() % 400);
              continue;
            }
            throw err;
          }
        }
      });
    }
    /**
     * Sends once with the endpoint's resolved strategy. The strategy is seeded from
     * the shared cache and defaults to workspace-first. On a genuine auth rejection
     * (401/403) the request was not processed, so the other strategy is tried and
     * the working one is recorded — symmetric, so a wrong seed self-corrects (a
     * Bearer-seeded endpoint that a token cannot use falls back to workspace, and
     * vice-versa). Any other workspace failure is retried once on Bearer to satisfy
     * the request but is not recorded, so a transient error never rewrites the cache.
     * A non-auth failure on a Bearer endpoint is not retried (avoids a duplicate
     * POST when the request may already have been applied).
     */
    _requestOnce(_0, _1) {
      return __async(this, arguments, function* (method, rawPath, opts = {}) {
        const serialize = opts.queryFormat === "json" ? serializeQueryJson : serializeQuery;
        const url = this.config.baseUrl + this.resolvePath(rawPath, opts.path) + serialize(opts.query);
        const cacheKey = `${method}:${rawPath}`;
        const primary = yield this.auth.resolveStrategy(cacheKey);
        const idempotent = method === "GET" || method === "HEAD";
        let res = yield this.send(method, url, opts, primary);
        if (res.status < 300) {
          yield this.auth.recordStrategy(cacheKey, primary);
        } else if (res.status === 401 || res.status === 403) {
          const alternate = primary === "workspace" ? "bearer" : "workspace";
          res = yield this.send(method, url, opts, alternate);
          if (res.status < 300) yield this.auth.recordStrategy(cacheKey, alternate);
        } else if (primary === "workspace" && idempotent) {
          res = yield this.send(method, url, opts, "bearer");
        }
        let trace;
        if (this.debug) trace = this.record({ method, path: rawPath, status: res.status });
        if (res.status >= 300) {
          throw new HabllaApiError(
            res,
            this.debug ? { method, path: rawPath, requestBody: method === "GET" ? void 0 : opts.body, trace } : void 0
          );
        }
        return res.data;
      });
    }
    get(path, opts) {
      return this.request("GET", path, opts);
    }
    post(path, opts) {
      return this.request("POST", path, opts);
    }
    put(path, opts) {
      return this.request("PUT", path, opts);
    }
    patch(path, opts) {
      return this.request("PATCH", path, opts);
    }
    delete(path, opts) {
      return this.request("DELETE", path, opts);
    }
    /** Fetches every page of a paginated endpoint. See {@link paginate}. */
    paginate(fetchPage, opts) {
      return paginate(fetchPage, opts);
    }
  };

  // src/sdk/resources/base.ts
  var Resource = class {
    constructor(http) {
      __publicField(this, "http", http);
    }
  };

  // src/sdk/resources/gen_acceptInvite.ts
  var AcceptInvite = class extends Resource {
    /**
     * createAcceptInvite.
     * @method POST /v1/workspaces/{workspace_id}/accept-invite
     * @remarks Any query params may be sent (none documented).
     */
    createAcceptInvite(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/accept-invite", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_annotations.ts
  var Annotations = class extends Resource {
    /**
     * Delete annotation by id.
     * @method DELETE /v1/workspaces/{workspace_id}/annotations/{annotation_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteAnnotation(annotationId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/annotations/{annotation_id}", { path: { annotation_id: annotationId }, query: opts.query });
    }
    /**
     * Get annotation by id.
     * @method GET /v1/workspaces/{workspace_id}/annotations/{annotation_id}
     * @remarks Documented query: filters (extra keys allowed).
     */
    getAnnotation(annotationId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/annotations/{annotation_id}", { path: { annotation_id: annotationId }, query: opts.query });
    }
    /**
     * Update annotation by id.
     * @method PUT /v1/workspaces/{workspace_id}/annotations/{annotation_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateAnnotation(annotationId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/annotations/{annotation_id}", { path: { annotation_id: annotationId }, body, query: opts.query });
    }
    /**
     * Get all annotations.
     * @method GET /v1/workspaces/{workspace_id}/annotations
     * @remarks Documented query: filters, page, limit, order, direction_order, person, card, service, organization, description, type, populate, is_fixed (extra keys allowed).
     */
    listAnnotations(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/annotations", { query: opts.query, queryFormat: "json" });
    }
    /**
     * createAnnotation. Create a new annotation.
     * @method POST /v1/workspaces/{workspace_id}/annotations
     * @remarks Any query params may be sent (none documented).
     * @param file The spreadsheet file part (sent under the `file` field).
     * @param fields Extra form-data text fields to send alongside the file.
     */
    createAnnotation(file, fields, opts = {}) {
      const body = { kind: "multipart", fields, files: { file } };
      return this.http.post("/v1/workspaces/{workspace_id}/annotations", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_asaas.ts
  var Asaas = class extends Resource {
    /**
     * getInvoice.
     * @method GET /v1/workspaces/{workspace_id}/asaas/subscription/invoice/{invoice_id}
     * @remarks Any query params may be sent (none documented).
     */
    getInvoice(invoiceId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/asaas/subscription/invoice/{invoice_id}", { path: { invoice_id: invoiceId }, query: opts.query });
    }
  };

  // src/sdk/resources/gen_authentication.ts
  var Authentication = class extends Resource {
    /**
     * forgotPassword.
     * @method POST /v1/authentication/forgot-password
     * @remarks Any query params may be sent (none documented).
     */
    forgotPassword(body, opts = {}) {
      return this.http.post("/v1/authentication/forgot-password", { body, query: opts.query });
    }
    /**
     * login.
     * @method POST /v1/authentication/login
     * @remarks Any query params may be sent (none documented).
     */
    login(body, opts = {}) {
      return this.http.post("/v1/authentication/login", { body, query: opts.query });
    }
    /**
     * registerWithExternal.
     * @method POST /v1/authentication/register-with-external
     * @remarks Any query params may be sent (none documented).
     */
    registerWithExternal(body, opts = {}) {
      return this.http.post("/v1/authentication/register-with-external", { body, query: opts.query });
    }
    /**
     * registerWithPassword.
     * @method POST /v1/authentication/register-with-password
     * @remarks Any query params may be sent (none documented).
     */
    registerWithPassword(body, opts = {}) {
      return this.http.post("/v1/authentication/register-with-password", { body, query: opts.query });
    }
    /**
     * resendEmailVerification.
     * @method POST /v1/authentication/resend-email-verification
     * @remarks Any query params may be sent (none documented).
     */
    resendEmailVerification(body, opts = {}) {
      return this.http.post("/v1/authentication/resend-email-verification", { body, query: opts.query });
    }
    /**
     * resetPassword.
     * @method POST /v1/authentication/reset-password
     * @remarks Any query params may be sent (none documented).
     */
    resetPassword(body, opts = {}) {
      return this.http.post("/v1/authentication/reset-password", { body, query: opts.query });
    }
    /**
     * getVerifyEmail.
     * @method GET /v1/authentication/verify-email
     * @remarks Any query params may be sent (none documented).
     */
    getVerifyEmail(opts = {}) {
      return this.http.get("/v1/authentication/verify-email", { query: opts.query, queryFormat: "json" });
    }
  };

  // src/sdk/resources/gen_automationRules.ts
  var AutomationRules = class extends Resource {
    /**
     * addFlow.
     * @method PATCH /v1/workspaces/{workspace_id}/automation-rules/{automation_rule_id}/add-flow
     * @remarks Any query params may be sent (none documented).
     */
    addFlow(automationRuleId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/automation-rules/{automation_rule_id}/add-flow", { path: { automation_rule_id: automationRuleId }, body, query: opts.query });
    }
    /**
     * removeFlow.
     * @method PATCH /v1/workspaces/{workspace_id}/automation-rules/{automation_rule_id}/remove-flow
     * @remarks Any query params may be sent (none documented).
     */
    removeFlow(automationRuleId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/automation-rules/{automation_rule_id}/remove-flow", { path: { automation_rule_id: automationRuleId }, body, query: opts.query });
    }
    /**
     * getBoard.
     * @method GET /v1/workspaces/{workspace_id}/automation-rules/boards/{board_id}
     * @remarks Documented query: filters (extra keys allowed).
     */
    getBoard(boardId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/automation-rules/boards/{board_id}", { path: { board_id: boardId }, query: opts.query });
    }
    /**
     * updateAutomationRule.
     * @method PUT /v1/workspaces/{workspace_id}/automation-rules/{automation_rule_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateAutomationRule(automationRuleId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/automation-rules/{automation_rule_id}", { path: { automation_rule_id: automationRuleId }, body, query: opts.query });
    }
    /**
     * createAutomationRule.
     * @method POST /v1/workspaces/{workspace_id}/automation-rules
     * @remarks Any query params may be sent (none documented).
     */
    createAutomationRule(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/automation-rules", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_backup.ts
  var Backup = class extends Resource {
    /**
     * createBackup.
     * @method POST /v1/workspaces/{workspace_id}/backup
     * @remarks Any query params may be sent (none documented).
     */
    createBackup(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/backup", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_block.ts
  var Block = class extends Resource {
    /**
     * patchBlock.
     * @method PATCH /v1/workspaces/{workspace_id}/block
     * @remarks Documented query: is_blocked (extra keys allowed).
     */
    patchBlock(body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/block", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_blockedWords.ts
  var BlockedWords = class extends Resource {
    /**
     * deleteBlockedWord.
     * @method DELETE /v1/workspaces/{workspace_id}/blocked-words/{blocked_word_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteBlockedWord(blockedWordId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/blocked-words/{blocked_word_id}", { path: { blocked_word_id: blockedWordId }, query: opts.query });
    }
    /**
     * listBlockedWords.
     * @method GET /v1/workspaces/{workspace_id}/blocked-words
     * @remarks Documented query: filters (extra keys allowed).
     */
    listBlockedWords(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/blocked-words", { query: opts.query });
    }
    /**
     * createBlockedWord.
     * @method POST /v1/workspaces/{workspace_id}/blocked-words
     * @remarks Any query params may be sent (none documented).
     */
    createBlockedWord(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/blocked-words", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_boards.ts
  var Boards = class extends Resource {
    /**
     * Delete list.
     * @method DELETE /v1/workspaces/{workspace_id}/boards/{board_id}/lists/{list_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteLists(boardId, listId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/boards/{board_id}/lists/{list_id}", { path: { board_id: boardId, list_id: listId }, query: opts.query });
    }
    /**
     * Get list by id.
     * @method GET /v1/workspaces/{workspace_id}/boards/{board_id}/lists/{list_id}
     * @remarks Any query params may be sent (none documented).
     */
    getList(boardId, listId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/boards/{board_id}/lists/{list_id}", { path: { board_id: boardId, list_id: listId }, query: opts.query });
    }
    /**
     * Update list by id.
     * @method PUT /v1/workspaces/{workspace_id}/boards/{board_id}/lists/{list_id}
     * @remarks Any query params may be sent (none documented).
     */
    putLists(boardId, listId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/boards/{board_id}/lists/{list_id}", { path: { board_id: boardId, list_id: listId }, body, query: opts.query });
    }
    /**
     * Get cards from a board by id.
     * @method GET /v1/workspaces/{workspace_id}/boards/{id}/cards
     * @remarks Documented query: limit, order, direction_order, name, search, campaign, source, person, organization, user, sector, status, product, reason, rating, tags, populate, board_populate, start_date, end_date, field_date, created_at, updated_at, has_next_task, next_task_start_date, prediction_date, entry_date, finished_at, next_task_type, custom_fields, list, highlight_old_cards, custom_id (extra keys allowed).
     */
    getCardsV1(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/boards/{id}/cards", { path: { id }, query: opts.query });
    }
    /**
     * Get all lists.
     * @method GET /v1/workspaces/{workspace_id}/boards/{board_id}/lists
     * @remarks Documented query: filters, page, limit, order, direction_order, name, populate (extra keys allowed).
     */
    getLists(boardId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/boards/{board_id}/lists", { path: { board_id: boardId }, query: opts.query });
    }
    /**
     * Create a list.
     * @method POST /v1/workspaces/{workspace_id}/boards/{board_id}/lists
     * @remarks Any query params may be sent (none documented).
     */
    lists(boardId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/boards/{board_id}/lists", { path: { board_id: boardId }, body, query: opts.query });
    }
    /**
     * Reorder lists inside a board by id.
     * @method PUT /v1/workspaces/{workspace_id}/boards/{board_id}/reorder-lists
     * @remarks Any query params may be sent (none documented).
     */
    reorderLists(boardId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/boards/{board_id}/reorder-lists", { path: { board_id: boardId }, body, query: opts.query });
    }
    /**
     * Get reports from a board by id.
     * @method GET /v1/workspaces/{workspace_id}/boards/{id}/reports
     * @remarks Any query params may be sent (none documented).
     */
    getReports(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/boards/{id}/reports", { path: { id }, query: opts.query });
    }
    /**
     * Delete board by id.
     * @method DELETE /v1/workspaces/{workspace_id}/boards/{board_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteBoard(boardId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/boards/{board_id}", { path: { board_id: boardId }, query: opts.query });
    }
    /**
     * Get board by id.
     * @method GET /v1/workspaces/{workspace_id}/boards/{board_id}
     * @remarks Documented query: populate (extra keys allowed).
     */
    getBoard(boardId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/boards/{board_id}", { path: { board_id: boardId }, query: opts.query });
    }
    /**
     * Update a board by id.
     * @method PUT /v1/workspaces/{workspace_id}/boards/{board_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateBoard(boardId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/boards/{board_id}", { path: { board_id: boardId }, body, query: opts.query });
    }
    /**
     * Get all boards.
     * @method GET /v1/workspaces/{workspace_id}/boards
     * @remarks Documented query: filters, page, limit, order, direction_order, name, populate (extra keys allowed).
     */
    listBoards(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/boards", { query: opts.query });
    }
    /**
     * Create a new board.
     * @method POST /v1/workspaces/{workspace_id}/boards
     * @remarks Any query params may be sent (none documented).
     */
    createBoard(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/boards", { body, query: opts.query });
    }
    /**
     * Get cards from a board by id (V2).
     * @method GET /v2/workspaces/{workspace_id}/boards/{board_id}/cards
     * @remarks Documented query: filters, limit, order, direction_order, name, search, campaign, source, person, organization, user, sector, status, product, reason, rating, tags, populate, board_populate, start_date, end_date, field_date, created_at, updated_at, has_next_task, next_task_start_date, prediction_date, entry_date, finished_at, next_task_type, custom_fields, list, highlight_old_cards, custom_id (extra keys allowed).
     */
    getCards(boardId, opts = {}) {
      return this.http.get("/v2/workspaces/{workspace_id}/boards/{board_id}/cards", { path: { board_id: boardId }, query: opts.query, queryFormat: "json" });
    }
  };

  // src/sdk/resources/gen_calendars.ts
  var Calendars = class extends Resource {
    /**
     * refreshCalendarGroupToken.
     * @method PUT /v1/workspaces/{workspace_id}/calendars/groups/{group_id}/jwt/refresh
     * @remarks Any query params may be sent (none documented).
     */
    refreshCalendarGroupToken(groupId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/calendars/groups/{group_id}/jwt/refresh", { path: { group_id: groupId }, body, query: opts.query });
    }
    /**
     * getDuplicated.
     * @method GET /v1/workspaces/{workspace_id}/calendars/groups/slug/duplicated/{duplicated_id}
     * @remarks Any query params may be sent (none documented).
     */
    getDuplicated(duplicatedId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/calendars/groups/slug/duplicated/{duplicated_id}", { path: { duplicated_id: duplicatedId }, query: opts.query });
    }
    /**
     * deleteBlockedDays.
     * @method DELETE /v1/workspaces/{workspace_id}/calendars/{calendar_id}/blocked-days/{blocked_day_id}
     * @remarks Documented query: id (extra keys allowed).
     */
    deleteBlockedDays(calendarId, blockedDayId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/calendars/{calendar_id}/blocked-days/{blocked_day_id}", { path: { calendar_id: calendarId, blocked_day_id: blockedDayId }, query: opts.query });
    }
    /**
     * refreshCalendarToken.
     * @method PUT /v1/workspaces/{workspace_id}/calendars/{calendar_id}/jwt/refresh
     * @remarks Any query params may be sent (none documented).
     */
    refreshCalendarToken(calendarId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/calendars/{calendar_id}/jwt/refresh", { path: { calendar_id: calendarId }, body, query: opts.query });
    }
    /**
     * addCalendar.
     * @method PATCH /v1/workspaces/{workspace_id}/calendars/groups/{group_id}/add-calendar
     * @remarks Any query params may be sent (none documented).
     */
    addCalendar(groupId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/calendars/groups/{group_id}/add-calendar", { path: { group_id: groupId }, body, query: opts.query });
    }
    /**
     * getBlockedDays.
     * @method GET /v1/workspaces/{workspace_id}/calendars/groups/{group_id}/blocked-days
     * @remarks Documented query: filters (extra keys allowed).
     */
    getBlockedDays(groupId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/calendars/groups/{group_id}/blocked-days", { path: { group_id: groupId }, query: opts.query });
    }
    /**
     * removeCalendar.
     * @method PATCH /v1/workspaces/{workspace_id}/calendars/groups/{group_id}/remove-calendar
     * @remarks Any query params may be sent (none documented).
     */
    removeCalendar(groupId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/calendars/groups/{group_id}/remove-calendar", { path: { group_id: groupId }, body, query: opts.query });
    }
    /**
     * getUsersTasks.
     * @method GET /v1/workspaces/{workspace_id}/calendars/groups/{group_id}/users-tasks
     * @remarks Documented query: filters (extra keys allowed).
     */
    getUsersTasks(groupId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/calendars/groups/{group_id}/users-tasks", { path: { group_id: groupId }, query: opts.query });
    }
    /**
     * getAvailabilityCalendarById.
     * @method GET /v1/workspaces/{workspace_id}/calendars/{calendar_id}/availability
     * @remarks Any query params may be sent (none documented).
     */
    getAvailabilityCalendarById(calendarId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/calendars/{calendar_id}/availability", { path: { calendar_id: calendarId }, query: opts.query });
    }
    /**
     * blockedDays.
     * @method POST /v1/workspaces/{workspace_id}/calendars/{calendar_id}/blocked-days
     * @remarks Any query params may be sent (none documented).
     */
    blockedDays(calendarId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/calendars/{calendar_id}/blocked-days", { path: { calendar_id: calendarId }, body, query: opts.query });
    }
    /**
     * deleteGroups.
     * @method DELETE /v1/workspaces/{workspace_id}/calendars/groups/{group_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteGroups(groupId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/calendars/groups/{group_id}", { path: { group_id: groupId }, query: opts.query });
    }
    /**
     * getGroup.
     * @method GET /v1/workspaces/{workspace_id}/calendars/groups/{group_id}
     * @remarks Any query params may be sent (none documented).
     */
    getGroup(groupId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/calendars/groups/{group_id}", { path: { group_id: groupId }, query: opts.query });
    }
    /**
     * putGroups.
     * @method PUT /v1/workspaces/{workspace_id}/calendars/groups/{group_id}
     * @remarks Any query params may be sent (none documented).
     */
    putGroups(groupId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/calendars/groups/{group_id}", { path: { group_id: groupId }, body, query: opts.query });
    }
    /**
     * getCalendarMeAvailability.
     * @method GET /v1/workspaces/{workspace_id}/calendars/me/availability
     * @remarks Any query params may be sent (none documented).
     */
    getCalendarMeAvailability(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/calendars/me/availability", { query: opts.query });
    }
    /**
     * deleteCalendar.
     * @method DELETE /v1/workspaces/{workspace_id}/calendars/{calendar_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteCalendar(calendarId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/calendars/{calendar_id}", { path: { calendar_id: calendarId }, query: opts.query });
    }
    /**
     * getCalendar.
     * @method GET /v1/workspaces/{workspace_id}/calendars/{calendar_id}
     * @remarks Any query params may be sent (none documented).
     */
    getCalendar(calendarId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/calendars/{calendar_id}", { path: { calendar_id: calendarId }, query: opts.query });
    }
    /**
     * updateCalendar.
     * @method PUT /v1/workspaces/{workspace_id}/calendars/{calendar_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateCalendar(calendarId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/calendars/{calendar_id}", { path: { calendar_id: calendarId }, body, query: opts.query });
    }
    /**
     * getGroups.
     * @method GET /v1/workspaces/{workspace_id}/calendars/groups
     * @remarks Documented query: filters (extra keys allowed).
     */
    getGroups(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/calendars/groups", { query: opts.query });
    }
    /**
     * groups.
     * @method POST /v1/workspaces/{workspace_id}/calendars/groups
     * @remarks Any query params may be sent (none documented).
     */
    groups(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/calendars/groups", { body, query: opts.query });
    }
    /**
     * getMe.
     * @method GET /v1/workspaces/{workspace_id}/calendars/me
     * @remarks Any query params may be sent (none documented).
     */
    getMe(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/calendars/me", { query: opts.query });
    }
    /**
     * listCalendars.
     * @method GET /v1/workspaces/{workspace_id}/calendars
     * @remarks Documented query: filters (extra keys allowed).
     */
    listCalendars(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/calendars", { query: opts.query });
    }
    /**
     * createCalendar.
     * @method POST /v1/workspaces/{workspace_id}/calendars
     * @remarks Any query params may be sent (none documented).
     */
    createCalendar(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/calendars", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_calls.ts
  var Calls = class extends Resource {
    /**
     * createCall.
     * @method POST /v1/workspaces/{workspace_id}/calls
     * @remarks Any query params may be sent (none documented).
     */
    createCall(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/calls", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_campaigns.ts
  var Campaigns = class extends Resource {
    /**
     * patchCancel.
     * @method PATCH /v1/workspaces/{workspace_id}/campaigns/{campaign_id}/cancel
     * @remarks Any query params may be sent (none documented).
     */
    patchCancel(campaignId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/campaigns/{campaign_id}/cancel", { path: { campaign_id: campaignId }, body, query: opts.query });
    }
    /**
     * deleteCampaign.
     * @method DELETE /v1/workspaces/{workspace_id}/campaigns/{campaign_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteCampaign(campaignId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/campaigns/{campaign_id}", { path: { campaign_id: campaignId }, query: opts.query });
    }
    /**
     * getCampaign.
     * @method GET /v1/workspaces/{workspace_id}/campaigns/{campaign_id}
     * @remarks Any query params may be sent (none documented).
     */
    getCampaign(campaignId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/campaigns/{campaign_id}", { path: { campaign_id: campaignId }, query: opts.query });
    }
    /**
     * updateCampaign.
     * @method PUT /v1/workspaces/{workspace_id}/campaigns/{campaign_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateCampaign(campaignId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/campaigns/{campaign_id}", { path: { campaign_id: campaignId }, body, query: opts.query });
    }
    /**
     * listCampaigns.
     * @method GET /v1/workspaces/{workspace_id}/campaigns
     * @remarks Documented query: filters (extra keys allowed).
     */
    listCampaigns(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/campaigns", { query: opts.query });
    }
    /**
     * sheet. Uploads an xlsx spreadsheet as multipart/form-data to create a campaign.
     * @method POST /v2/workspaces/{workspace_id}/campaigns/sheet
     * @remarks Any query params may be sent (none documented).
     * @param file The spreadsheet file part (sent under the `file` field).
     * @param fields Extra form-data text fields to send alongside the file.
     */
    sheet(file, fields, opts = {}) {
      const body = { kind: "multipart", fields, files: { file } };
      return this.http.post("/v2/workspaces/{workspace_id}/campaigns/sheet", { body, query: opts.query });
    }
    /**
     * createCampaign.
     * @method POST /v2/workspaces/{workspace_id}/campaigns
     * @remarks Any query params may be sent (none documented).
     */
    createCampaign(body, opts = {}) {
      return this.http.post("/v2/workspaces/{workspace_id}/campaigns", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_cards.ts
  var Cards = class extends Resource {
    /**
     * Remove checklist item from a card by id.
     * @method DELETE /v1/workspaces/{workspace_id}/cards/{card_id}/checklist/{checklist_id}
     * @remarks Documented query: flow_execution, user (extra keys allowed).
     */
    deleteChecklist(cardId, checklistId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/cards/{card_id}/checklist/{checklist_id}", { path: { card_id: cardId, checklist_id: checklistId }, query: opts.query });
    }
    /**
     * Update checklist from a card by id.
     * @method PUT /v1/workspaces/{workspace_id}/cards/{card_id}/checklist/{checklist_id}
     * @remarks Documented query: flow_execution, user (extra keys allowed).
     */
    putChecklist(cardId, checklistId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/cards/{card_id}/checklist/{checklist_id}", { path: { card_id: cardId, checklist_id: checklistId }, body, query: opts.query });
    }
    /**
     * Remove a product from a card card by id.
     * @method DELETE /v1/workspaces/{workspace_id}/cards/{id}/products/{product_id}
     * @remarks Documented query: code (extra keys allowed).
     */
    deleteProducts(productId, id, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/cards/{id}/products/{product_id}", { path: { product_id: productId, id }, query: opts.query });
    }
    /**
     * Update product on a card by id.
     * @method PUT /v1/workspaces/{workspace_id}/cards/{id}/products/{product_id}
     * @remarks Documented query: code (extra keys allowed).
     */
    putProducts(productId, id, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/cards/{id}/products/{product_id}", { path: { product_id: productId, id }, body, query: opts.query });
    }
    /**
     * Batch update products on card by id.
     * @method POST /v1/workspaces/{workspace_id}/cards/{card_id}/products/batch
     * @remarks Any query params may be sent (none documented).
     */
    batchAddProductsCard(cardId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/cards/{card_id}/products/batch", { path: { card_id: cardId }, body, query: opts.query });
    }
    /**
     * Add followers to a card by id.
     * @method PATCH /v1/workspaces/{workspace_id}/cards/{card_id}/add-followers
     * @remarks Documented query: followers, flow_execution, user (extra keys allowed).
     */
    addFollowers(cardId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/cards/{card_id}/add-followers", { path: { card_id: cardId }, body, query: opts.query });
    }
    /**
     * Add persons on a card by id.
     * @method PATCH /v1/workspaces/{workspace_id}/cards/{card_id}/add-persons
     * @remarks Documented query: persons, flow_execution, user (extra keys allowed).
     */
    addPersons(cardId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/cards/{card_id}/add-persons", { path: { card_id: cardId }, body, query: opts.query });
    }
    /**
     * Add tags to a card by id.
     * @method PUT /v1/workspaces/{workspace_id}/cards/{card_id}/add-tags
     * @remarks Documented query: tags, flow_execution, user (extra keys allowed).
     */
    addTags(cardId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/cards/{card_id}/add-tags", { path: { card_id: cardId }, body, query: opts.query });
    }
    /**
     * Add checklist to a card by id.
     * @method POST /v1/workspaces/{workspace_id}/cards/{card_id}/checklist
     * @remarks Documented query: flow_execution, user (extra keys allowed).
     */
    checklist(cardId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/cards/{card_id}/checklist", { path: { card_id: cardId }, body, query: opts.query });
    }
    /**
     * Get all costs allocations by card.
     * @method GET /v1/workspaces/{workspace_id}/cards/{card_id}/costs
     * @remarks Documented query: filters, page, limit, order, direction_order, entity_type, start_date, end_date, populate (extra keys allowed).
     */
    getCosts(cardId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/cards/{card_id}/costs", { path: { card_id: cardId }, query: opts.query });
    }
    /**
     * Move card to a new list by id.
     * @method PATCH /v1/workspaces/{workspace_id}/cards/{card_id}/move
     * @remarks Documented query: list, flow_execution, user (extra keys allowed).
     */
    move(cardId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/cards/{card_id}/move", { path: { card_id: cardId }, body, query: opts.query });
    }
    /**
     * Add payment intervals to a card by id.
     * @method PUT /v1/workspaces/{workspace_id}/cards/{card_id}/payment-interval
     * @remarks Documented query: flow_execution, user (extra keys allowed).
     */
    putPaymentInterval(cardId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/cards/{card_id}/payment-interval", { path: { card_id: cardId }, body, query: opts.query });
    }
    /**
     * Add product to a card card by id.
     * @method POST /v1/workspaces/{workspace_id}/cards/{id}/products
     * @remarks Any query params may be sent (none documented).
     */
    products(id, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/cards/{id}/products", { path: { id }, body, query: opts.query });
    }
    /**
     * Add products prices to a card by id.
     * @method PUT /v1/workspaces/{workspace_id}/cards/{card_id}/products-prices
     * @remarks Documented query: flow_execution, user (extra keys allowed).
     */
    putProductsPrices(cardId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/cards/{card_id}/products-prices", { path: { card_id: cardId }, body, query: opts.query });
    }
    /**
     * Purge card by id.
     * @method GET /v1/workspaces/{workspace_id}/cards/{card_id}/purge
     * @remarks Any query params may be sent (none documented).
     */
    getPurge(cardId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/cards/{card_id}/purge", { path: { card_id: cardId }, query: opts.query });
    }
    /**
     * Remove followers from a card by id.
     * @method PATCH /v1/workspaces/{workspace_id}/cards/{card_id}/remove-followers
     * @remarks Documented query: followers (extra keys allowed).
     */
    removeFollowers(cardId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/cards/{card_id}/remove-followers", { path: { card_id: cardId }, body, query: opts.query });
    }
    /**
     * Remove payment intervals from a card by id.
     * @method PUT /v1/workspaces/{workspace_id}/cards/{card_id}/remove-payment-interval
     * @remarks Documented query: flow_execution, user (extra keys allowed).
     */
    removePaymentInterval(cardId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/cards/{card_id}/remove-payment-interval", { path: { card_id: cardId }, body, query: opts.query });
    }
    /**
     * Remove persons from a card by id.
     * @method PATCH /v1/workspaces/{workspace_id}/cards/{card_id}/remove-persons
     * @remarks Documented query: persons, flow_execution, user (extra keys allowed).
     */
    removePersons(cardId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/cards/{card_id}/remove-persons", { path: { card_id: cardId }, body, query: opts.query });
    }
    /**
     * Remove products prices from a card by id.
     * @method PUT /v1/workspaces/{workspace_id}/cards/{card_id}/remove-products-prices
     * @remarks Documented query: flow_execution, user (extra keys allowed).
     */
    removeProductsPrices(cardId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/cards/{card_id}/remove-products-prices", { path: { card_id: cardId }, body, query: opts.query });
    }
    /**
     * Remove tags from a card by id.
     * @method PUT /v1/workspaces/{workspace_id}/cards/{card_id}/remove-tags
     * @remarks Documented query: tags (extra keys allowed).
     */
    removeTags(cardId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/cards/{card_id}/remove-tags", { path: { card_id: cardId }, body, query: opts.query });
    }
    /**
     * Delete card by id.
     * @method DELETE /v1/workspaces/{workspace_id}/cards/{card_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteCard(cardId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/cards/{card_id}", { path: { card_id: cardId }, query: opts.query });
    }
    /**
     * Get card by id.
     * @method GET /v1/workspaces/{workspace_id}/cards/{card_id}
     * @remarks Documented query: params, flow_execution, user (extra keys allowed).
     */
    getCard(cardId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/cards/{card_id}", { path: { card_id: cardId }, query: opts.query });
    }
    /**
     * Update card by id.
     * @method PUT /v1/workspaces/{workspace_id}/cards/{card_id}
     * @remarks Documented query: flow_execution, user (extra keys allowed).
     */
    updateCard(cardId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/cards/{card_id}", { path: { card_id: cardId }, body, query: opts.query });
    }
    /**
     * Create a new batch action.
     * @method POST /v1/workspaces/{workspace_id}/cards/batch
     * @remarks Any query params may be sent (none documented).
     */
    batchCards(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/cards/batch", { body, query: opts.query });
    }
    /**
     * Create or update a card.
     * @method POST /v1/workspaces/{workspace_id}/cards/create-or-update
     * @remarks Documented query: page, limit, order, direction_order, name, search, campaign, source, id, list, custom_id, board, person, organization, user, product, service, status, rating, tags, start_date, end_date, field_date, created_at, updated_at, prediction_date, next_task_start_date, next_task_type, has_next_task, custom_fields, update_rule (extra keys allowed).
     */
    createOrUpdate(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/cards/create-or-update", { body, query: opts.query });
    }
    /**
     * Get all cards.
     * @method GET /v1/workspaces/{workspace_id}/cards
     * @remarks Documented query: page, limit, order, direction_order, name, search, campaign, source, list, custom_id, board, person, organization, user, product, service, sector, status, rating, tags, followers, users, populate, start_date, end_date, field_date, created_at, updated_at, finished_at, prediction_date, entry_date, next_task_start_date, next_task_type, has_next_task, custom_fields, highlight_old_cards (extra keys allowed).
     */
    listCardsV1(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/cards", { query: opts.query });
    }
    /**
     * Create a new card.
     * @method POST /v1/workspaces/{workspace_id}/cards
     * @remarks Any query params may be sent (none documented).
     */
    createCard(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/cards", { body, query: opts.query });
    }
    /**
     * Create all cards (V2).
     * @method GET /v2/workspaces/{workspace_id}/cards
     * @remarks Documented query: page, limit, order, direction_order, name, search, campaign, source, list, custom_id, board, person, organization, user, product, service, sector, status, rating, tags, followers, users, populate, start_date, end_date, field_date, created_at, updated_at, finished_at, prediction_date, entry_date, next_task_start_date, next_task_type, has_next_task, custom_fields, highlight_old_cards (extra keys allowed).
     */
    listCardsV2(opts = {}) {
      return this.http.get("/v2/workspaces/{workspace_id}/cards", { query: opts.query });
    }
    /**
     * Get all cards (V3).
     * @method GET /v3/workspaces/{workspace_id}/cards
     * @remarks Documented query: filters, page, limit, order, direction_order, name, search, campaign, source, list, custom_id, board, person, organization, user, product, service, sector, status, rating, tags, followers, users, populate, start_date, end_date, field_date, created_at, updated_at, finished_at, prediction_date, entry_date, next_task_start_date, next_task_type, has_next_task, custom_fields, highlight_old_cards (extra keys allowed).
     */
    listCards(opts = {}) {
      return this.http.get("/v3/workspaces/{workspace_id}/cards", { query: opts.query, queryFormat: "json" });
    }
  };

  // src/sdk/resources/gen_cardsGroups.ts
  var CardsGroups = class extends Resource {
    /**
     * getItems.
     * @method GET /v1/workspaces/{workspace_id}/cards-groups/{cards_group_id}/items
     * @remarks Documented query: filters (extra keys allowed).
     */
    getItems(cardsGroupId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/cards-groups/{cards_group_id}/items", { path: { cards_group_id: cardsGroupId }, query: opts.query });
    }
    /**
     * listCardsGroups.
     * @method GET /v1/workspaces/{workspace_id}/cards-groups
     * @remarks Documented query: filters (extra keys allowed).
     */
    listCardsGroups(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/cards-groups", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_cdn.ts
  var Cdn = class extends Resource {
    /**
     * deleteCdn.
     * @method DELETE /v1/workspaces/{workspace_id}/cdn
     * @remarks Any query params may be sent (none documented).
     */
    deleteCdn(opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/cdn", { query: opts.query });
    }
    /**
     * createCdn.
     * @method POST /v1/workspaces/{workspace_id}/cdn
     * @remarks Any query params may be sent (none documented).
     * @param file The spreadsheet file part (sent under the `file` field).
     * @param fields Extra form-data text fields to send alongside the file.
     */
    createCdn(file, fields, opts = {}) {
      const body = { kind: "multipart", fields, files: { file } };
      return this.http.post("/v1/workspaces/{workspace_id}/cdn", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_changeOwner.ts
  var ChangeOwner = class extends Resource {
    /**
     * patchChangeOwner.
     * @method PATCH /v1/workspaces/{workspace_id}/change-owner
     * @remarks Any query params may be sent (none documented).
     */
    patchChangeOwner(body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/change-owner", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_classes.ts
  var Classes = class extends Resource {
    /**
     * makePublic.
     * @method POST /v1/workspaces/{workspace_id}/classes/{class_id}/make-public
     * @remarks Any query params may be sent (none documented).
     */
    makePublic(classId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/classes/{class_id}/make-public", { path: { class_id: classId }, body, query: opts.query });
    }
    /**
     * updateClass.
     * @method PUT /v1/workspaces/{workspace_id}/classes/{class_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateClass(classId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/classes/{class_id}", { path: { class_id: classId }, body, query: opts.query });
    }
    /**
     * publish.
     * @method POST /v1/workspaces/{workspace_id}/classes/publish
     * @remarks Any query params may be sent (none documented).
     */
    publish(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/classes/publish", { body, query: opts.query });
    }
    /**
     * listClasses.
     * @method GET /v1/workspaces/{workspace_id}/classes
     * @remarks Documented query: filters (extra keys allowed).
     */
    listClasses(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/classes", { query: opts.query });
    }
    /**
     * createClass.
     * @method POST /v1/workspaces/{workspace_id}/classes
     * @remarks Any query params may be sent (none documented).
     */
    createClass(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/classes", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_comments.ts
  var Comments = class extends Resource {
    /**
     * deleteComment.
     * @method DELETE /v1/workspaces/{workspace_id}/comments/{comment_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteComment(commentId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/comments/{comment_id}", { path: { comment_id: commentId }, query: opts.query });
    }
    /**
     * createComment.
     * @method POST /v1/workspaces/{workspace_id}/comments/{comment_id}
     * @remarks Any query params may be sent (none documented).
     */
    createComment(commentId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/comments/{comment_id}", { path: { comment_id: commentId }, body, query: opts.query });
    }
    /**
     * listComments.
     * @method GET /v1/workspaces/{workspace_id}/comments
     * @remarks Documented query: filters (extra keys allowed).
     */
    listComments(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/comments", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_connections.ts
  var Connections = class extends Resource {
    /**
     * createDuplicate.
     * @method POST /v1/workspaces/{workspace_id}/connections/{connection_id}/templates/duplicate/{duplicate_id}
     * @remarks Any query params may be sent (none documented).
     */
    createDuplicate(connectionId, duplicateId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/connections/{connection_id}/templates/duplicate/{duplicate_id}", { path: { connection_id: connectionId, duplicate_id: duplicateId }, body, query: opts.query });
    }
    /**
     * deleteTemplates.
     * @method DELETE /v1/workspaces/{workspace_id}/connections/{connection_id}/templates/{template_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteTemplates(connectionId, templateId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/connections/{connection_id}/templates/{template_id}", { path: { connection_id: connectionId, template_id: templateId }, query: opts.query });
    }
    /**
     * getTemplate.
     * @method GET /v1/workspaces/{workspace_id}/connections/{connection_id}/templates/{template_id}
     * @remarks Any query params may be sent (none documented).
     */
    getTemplate(connectionId, templateId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/connections/{connection_id}/templates/{template_id}", { path: { connection_id: connectionId, template_id: templateId }, query: opts.query });
    }
    /**
     * putTemplates.
     * @method PUT /v1/workspaces/{workspace_id}/connections/{connection_id}/templates/{template_id}
     * @remarks Any query params may be sent (none documented).
     */
    putTemplates(connectionId, templateId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/connections/{connection_id}/templates/{template_id}", { path: { connection_id: connectionId, template_id: templateId }, body, query: opts.query });
    }
    /**
     * getChat.
     * @method GET /v1/workspaces/{workspace_id}/connections/{connection_id}/templates/chat
     * @remarks Documented query: filters (extra keys allowed).
     */
    getChat(connectionId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/connections/{connection_id}/templates/chat", { path: { connection_id: connectionId }, query: opts.query });
    }
    /**
     * Get all messages by connection id and to.
     * @method GET /v1/workspaces/{workspace_id}/connections/{id}/messages
     * @remarks Documented query: page, limit, order, direction_order, user, body, populate, start_date, end_date, to (extra keys allowed).
     */
    getMessages(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/connections/{id}/messages", { path: { id }, query: opts.query });
    }
    /**
     * Create a message connection.
     * @method POST /v1/workspaces/{workspace_id}/connections/{connection_id}/messages
     * @remarks Any query params may be sent (none documented).
     */
    messages(connectionId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/connections/{connection_id}/messages", { path: { connection_id: connectionId }, body, query: opts.query });
    }
    /**
     * Create a message templates connections.
     * @method POST /v1/workspaces/{workspace_id}/connections/{connection_id}/messages-templates
     * @remarks Any query params may be sent (none documented).
     */
    messagesTemplates(connectionId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/connections/{connection_id}/messages-templates", { path: { connection_id: connectionId }, body, query: opts.query });
    }
    /**
     * Get all meta waba logs for a connection.
     * @method GET /v1/workspaces/{workspace_id}/connections/{connection_id}/meta-waba-logs
     * @remarks Documented query: filters, page, limit, order, direction_order, type, start_date, end_date, populate (extra keys allowed).
     */
    getMetaWabaLogs(connectionId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/connections/{connection_id}/meta-waba-logs", { path: { connection_id: connectionId }, query: opts.query });
    }
    /**
     * Register a whatsapp connection on Meta.
     * @method POST /v1/workspaces/{workspace_id}/connections/{connection_id}/register
     * @remarks Any query params may be sent (none documented).
     */
    register(connectionId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/connections/{connection_id}/register", { path: { connection_id: connectionId }, body, query: opts.query });
    }
    /**
     * Subscribe apps on Meta for a connection.
     * @method POST /v1/workspaces/{workspace_id}/connections/{connection_id}/subscribe-app
     * @remarks Any query params may be sent (none documented).
     */
    subscribeApp(connectionId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/connections/{connection_id}/subscribe-app", { path: { connection_id: connectionId }, body, query: opts.query });
    }
    /**
     * Get Meta subscribed apps for a connection.
     * @method GET /v1/workspaces/{workspace_id}/connections/{connection_id}/subscribed-apps
     * @remarks Any query params may be sent (none documented).
     */
    getSubscribedApps(connectionId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/connections/{connection_id}/subscribed-apps", { path: { connection_id: connectionId }, query: opts.query });
    }
    /**
     * Sync connection by id.
     * @method GET /v1/workspaces/{workspace_id}/connections/{connection_id}/sync
     * @remarks Any query params may be sent (none documented).
     */
    sync(connectionId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/connections/{connection_id}/sync", { path: { connection_id: connectionId }, query: opts.query });
    }
    /**
     * getTemplates.
     * @method GET /v1/workspaces/{workspace_id}/connections/{connection_id}/templates
     * @remarks Documented query: filters (extra keys allowed).
     */
    getTemplates(connectionId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/connections/{connection_id}/templates", { path: { connection_id: connectionId }, query: opts.query });
    }
    /**
     * templatesV1.
     * @method POST /v1/workspaces/{workspace_id}/connections/{connection_id}/templates
     * @remarks Any query params may be sent (none documented).
     */
    templatesV1(connectionId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/connections/{connection_id}/templates", { path: { connection_id: connectionId }, body, query: opts.query });
    }
    /**
     * Delete connection by id.
     * @method DELETE /v1/workspaces/{workspace_id}/connections/{connection_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteConnection(connectionId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/connections/{connection_id}", { path: { connection_id: connectionId }, query: opts.query });
    }
    /**
     * Get connection by id.
     * @method GET /v1/workspaces/{workspace_id}/connections/{connection_id}
     * @remarks Any query params may be sent (none documented).
     */
    getConnection(connectionId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/connections/{connection_id}", { path: { connection_id: connectionId }, query: opts.query });
    }
    /**
     * Update connection by id.
     * @method PUT /v1/workspaces/{workspace_id}/connections/{connection_id}
     * @remarks Documented query: populate (extra keys allowed).
     */
    updateConnection(connectionId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/connections/{connection_id}", { path: { connection_id: connectionId }, body, query: opts.query });
    }
    /**
     * Get available connections.
     * @method GET /v1/workspaces/{workspace_id}/connections/availables
     * @remarks Any query params may be sent (none documented).
     */
    getAvailables(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/connections/availables", { query: opts.query });
    }
    /**
     * Get connections counter.
     * @method GET /v1/workspaces/{workspace_id}/connections/counters
     * @remarks Any query params may be sent (none documented).
     */
    getCounters(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/connections/counters", { query: opts.query });
    }
    /**
     * Get connections by array of ids.
     * @method GET /v1/workspaces/{workspace_id}/connections/multiple
     * @remarks Documented query: ids (extra keys allowed).
     */
    getMultiple(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/connections/multiple", { query: opts.query });
    }
    /**
     * Get all connections.
     * @method GET /v1/workspaces/{workspace_id}/connections
     * @remarks Documented query: page, limit, order, direction_order, name, key, type, generic_type, types, status, populate, ids, is_deleted (extra keys allowed).
     */
    listConnectionsV1(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/connections", { query: opts.query });
    }
    /**
     * Create a connection.
     * @method POST /v1/workspaces/{workspace_id}/connections
     * @remarks Any query params may be sent (none documented).
     */
    createConnection(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/connections", { body, query: opts.query });
    }
    /**
     * templates.
     * @method POST /v2/workspaces/{workspace_id}/connections/{connection_id}/templates
     * @remarks Any query params may be sent (none documented).
     */
    templates(connectionId, body, opts = {}) {
      return this.http.post("/v2/workspaces/{workspace_id}/connections/{connection_id}/templates", { path: { connection_id: connectionId }, body, query: opts.query });
    }
    /**
     * Get all connections (V2).
     * @method GET /v2/workspaces/{workspace_id}/connections
     * @remarks Documented query: filters, page, limit, order, direction_order, name, key, type, generic_type, types, status, populate, ids, is_deleted (extra keys allowed).
     */
    listConnections(opts = {}) {
      return this.http.get("/v2/workspaces/{workspace_id}/connections", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_copilot.ts
  var Copilot = class extends Resource {
    /**
     * getReset.
     * @method GET /v1/workspaces/{workspace_id}/copilot/reset/{reset_id}
     * @remarks Any query params may be sent (none documented).
     */
    getReset(resetId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/copilot/reset/{reset_id}", { path: { reset_id: resetId }, query: opts.query });
    }
    /**
     * createCopilot.
     * @method POST /v1/workspaces/{workspace_id}/copilot
     * @remarks Any query params may be sent (none documented).
     */
    createCopilot(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/copilot", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_costCenters.ts
  var CostCenters = class extends Resource {
    /**
     * deleteCostAllocations.
     * @method DELETE /v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries/{cost_entry_id}/cost-allocations/{cost_allocation_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteCostAllocations(costCenterId, costEntryId, costAllocationId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries/{cost_entry_id}/cost-allocations/{cost_allocation_id}", { path: { cost_center_id: costCenterId, cost_entry_id: costEntryId, cost_allocation_id: costAllocationId }, query: opts.query });
    }
    /**
     * getCostAllocation.
     * @method GET /v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries/{cost_entry_id}/cost-allocations/{cost_allocation_id}
     * @remarks Any query params may be sent (none documented).
     */
    getCostAllocation(costCenterId, costEntryId, costAllocationId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries/{cost_entry_id}/cost-allocations/{cost_allocation_id}", { path: { cost_center_id: costCenterId, cost_entry_id: costEntryId, cost_allocation_id: costAllocationId }, query: opts.query });
    }
    /**
     * putCostAllocations.
     * @method PUT /v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries/{cost_entry_id}/cost-allocations/{cost_allocation_id}
     * @remarks Any query params may be sent (none documented).
     */
    putCostAllocations(costCenterId, costEntryId, costAllocationId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries/{cost_entry_id}/cost-allocations/{cost_allocation_id}", { path: { cost_center_id: costCenterId, cost_entry_id: costEntryId, cost_allocation_id: costAllocationId }, body, query: opts.query });
    }
    /**
     * getCostAllocations.
     * @method GET /v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries/{cost_entry_id}/cost-allocations
     * @remarks Documented query: filters (extra keys allowed).
     */
    getCostAllocations(costCenterId, costEntryId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries/{cost_entry_id}/cost-allocations", { path: { cost_center_id: costCenterId, cost_entry_id: costEntryId }, query: opts.query });
    }
    /**
     * costAllocations.
     * @method POST /v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries/{cost_entry_id}/cost-allocations
     * @remarks Any query params may be sent (none documented).
     */
    costAllocations(costCenterId, costEntryId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries/{cost_entry_id}/cost-allocations", { path: { cost_center_id: costCenterId, cost_entry_id: costEntryId }, body, query: opts.query });
    }
    /**
     * deleteCostEntries.
     * @method DELETE /v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries/{cost_entry_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteCostEntries(costCenterId, costEntryId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries/{cost_entry_id}", { path: { cost_center_id: costCenterId, cost_entry_id: costEntryId }, query: opts.query });
    }
    /**
     * getCostEntry.
     * @method GET /v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries/{cost_entry_id}
     * @remarks Any query params may be sent (none documented).
     */
    getCostEntry(costCenterId, costEntryId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries/{cost_entry_id}", { path: { cost_center_id: costCenterId, cost_entry_id: costEntryId }, query: opts.query });
    }
    /**
     * putCostEntries.
     * @method PUT /v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries/{cost_entry_id}
     * @remarks Any query params may be sent (none documented).
     */
    putCostEntries(costCenterId, costEntryId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries/{cost_entry_id}", { path: { cost_center_id: costCenterId, cost_entry_id: costEntryId }, body, query: opts.query });
    }
    /**
     * getCostEntries.
     * @method GET /v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries
     * @remarks Documented query: filters (extra keys allowed).
     */
    getCostEntries(costCenterId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries", { path: { cost_center_id: costCenterId }, query: opts.query });
    }
    /**
     * costEntries.
     * @method POST /v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries
     * @remarks Any query params may be sent (none documented).
     */
    costEntries(costCenterId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}/cost-entries", { path: { cost_center_id: costCenterId }, body, query: opts.query });
    }
    /**
     * deleteCostCenter.
     * @method DELETE /v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteCostCenter(costCenterId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}", { path: { cost_center_id: costCenterId }, query: opts.query });
    }
    /**
     * getCostCenter.
     * @method GET /v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}
     * @remarks Any query params may be sent (none documented).
     */
    getCostCenter(costCenterId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}", { path: { cost_center_id: costCenterId }, query: opts.query });
    }
    /**
     * updateCostCenter.
     * @method PUT /v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateCostCenter(costCenterId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/cost-centers/{cost_center_id}", { path: { cost_center_id: costCenterId }, body, query: opts.query });
    }
    /**
     * listCostCenters.
     * @method GET /v1/workspaces/{workspace_id}/cost-centers
     * @remarks Documented query: filters (extra keys allowed).
     */
    listCostCenters(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/cost-centers", { query: opts.query });
    }
    /**
     * createCostCenter.
     * @method POST /v1/workspaces/{workspace_id}/cost-centers
     * @remarks Any query params may be sent (none documented).
     */
    createCostCenter(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/cost-centers", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_count.ts
  var Count = class extends Resource {
    /**
     * listCount.
     * @method GET /v1/workspaces/{workspace_id}/count
     * @remarks Any query params may be sent (none documented).
     */
    listCount(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/count", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_createUsers.ts
  var CreateUsers = class extends Resource {
    /**
     * createCreateUserV1.
     * @method POST /v1/workspaces/{workspace_id}/create-users
     * @remarks Any query params may be sent (none documented).
     */
    createCreateUserV1(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/create-users", { body, query: opts.query });
    }
    /**
     * createCreateUser.
     * @method POST /v2/workspaces/{workspace_id}/create-users
     * @remarks Any query params may be sent (none documented).
     */
    createCreateUser(body, opts = {}) {
      return this.http.post("/v2/workspaces/{workspace_id}/create-users", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_credentials.ts
  var Credentials = class extends Resource {
    /**
     * deleteCredential.
     * @method DELETE /v1/workspaces/{workspace_id}/credentials/{credential_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteCredential(credentialId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/credentials/{credential_id}", { path: { credential_id: credentialId }, query: opts.query });
    }
    /**
     * getCredential.
     * @method GET /v1/workspaces/{workspace_id}/credentials/{credential_id}
     * @remarks Any query params may be sent (none documented).
     */
    getCredential(credentialId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/credentials/{credential_id}", { path: { credential_id: credentialId }, query: opts.query });
    }
    /**
     * updateCredential.
     * @method PUT /v1/workspaces/{workspace_id}/credentials/{credential_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateCredential(credentialId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/credentials/{credential_id}", { path: { credential_id: credentialId }, body, query: opts.query });
    }
    /**
     * listCredentials.
     * @method GET /v1/workspaces/{workspace_id}/credentials
     * @remarks Documented query: filters (extra keys allowed).
     */
    listCredentials(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/credentials", { query: opts.query });
    }
    /**
     * createCredential.
     * @method POST /v1/workspaces/{workspace_id}/credentials
     * @remarks Any query params may be sent (none documented).
     */
    createCredential(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/credentials", { body, query: opts.query });
    }
    /**
     * loginOauth.
     * @method POST /v2/workspaces/{workspace_id}/credentials/login-oauth
     * @remarks Any query params may be sent (none documented).
     */
    loginOauth(body, opts = {}) {
      return this.http.post("/v2/workspaces/{workspace_id}/credentials/login-oauth", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_customFields.ts
  var CustomFields = class extends Resource {
    /**
     * Delete custom field by id.
     * @method DELETE /v1/workspaces/{workspace_id}/custom-fields/{custom_field_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteCustomField(customFieldId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/custom-fields/{custom_field_id}", { path: { custom_field_id: customFieldId }, query: opts.query });
    }
    /**
     * Get custom field by id.
     * @method GET /v1/workspaces/{workspace_id}/custom-fields/{custom_field_id}
     * @remarks Any query params may be sent (none documented).
     */
    getCustomField(customFieldId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/custom-fields/{custom_field_id}", { path: { custom_field_id: customFieldId }, query: opts.query });
    }
    /**
     * Update custom field by id.
     * @method PUT /v1/workspaces/{workspace_id}/custom-fields/{custom_field_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateCustomField(customFieldId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/custom-fields/{custom_field_id}", { path: { custom_field_id: customFieldId }, body, query: opts.query });
    }
    /**
     * Get all custom fields.
     * @method GET /v1/workspaces/{workspace_id}/custom-fields
     * @remarks Documented query: filters, page, limit, order, direction_order, name, type, target, board, is_required, is_sensitive, is_visible, start_date, end_date, field_date (extra keys allowed).
     */
    listCustomFields(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/custom-fields", { query: opts.query });
    }
    /**
     * Create custom field.
     * @method POST /v1/workspaces/{workspace_id}/custom-fields
     * @remarks Any query params may be sent (none documented).
     */
    createCustomField(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/custom-fields", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_customScreens.ts
  var CustomScreens = class extends Resource {
    /**
     * deleteCustomScreen.
     * @method DELETE /v1/workspaces/{workspace_id}/custom-screens/{custom_screen_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteCustomScreen(customScreenId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/custom-screens/{custom_screen_id}", { path: { custom_screen_id: customScreenId }, query: opts.query });
    }
    /**
     * getCustomScreen.
     * @method GET /v1/workspaces/{workspace_id}/custom-screens/{custom_screen_id}
     * @remarks Any query params may be sent (none documented).
     */
    getCustomScreen(customScreenId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/custom-screens/{custom_screen_id}", { path: { custom_screen_id: customScreenId }, query: opts.query });
    }
    /**
     * updateCustomScreen.
     * @method PUT /v1/workspaces/{workspace_id}/custom-screens/{custom_screen_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateCustomScreen(customScreenId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/custom-screens/{custom_screen_id}", { path: { custom_screen_id: customScreenId }, body, query: opts.query });
    }
    /**
     * listCustomScreens.
     * @method GET /v1/workspaces/{workspace_id}/custom-screens
     * @remarks Documented query: filters (extra keys allowed).
     */
    listCustomScreens(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/custom-screens", { query: opts.query });
    }
    /**
     * createCustomScreen.
     * @method POST /v1/workspaces/{workspace_id}/custom-screens
     * @remarks Any query params may be sent (none documented).
     */
    createCustomScreen(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/custom-screens", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_data.ts
  var Data = class extends Resource {
    /**
     * deleteData.
     * @method DELETE /v2/workspaces/{workspace_id}/data/{data_id}/{data2_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteData(dataId, data2Id, opts = {}) {
      return this.http.delete("/v2/workspaces/{workspace_id}/data/{data_id}/{data2_id}", { path: { data_id: dataId, data2_id: data2Id }, query: opts.query });
    }
    /**
     * updateData.
     * @method PUT /v2/workspaces/{workspace_id}/data/{data_id}/{data2_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateData(dataId, data2Id, body, opts = {}) {
      return this.http.put("/v2/workspaces/{workspace_id}/data/{data_id}/{data2_id}", { path: { data_id: dataId, data2_id: data2Id }, body, query: opts.query });
    }
    /**
     * getData.
     * @method GET /v2/workspaces/{workspace_id}/data/{data_id}
     * @remarks Documented query: filters (extra keys allowed).
     */
    getData(dataId, opts = {}) {
      return this.http.get("/v2/workspaces/{workspace_id}/data/{data_id}", { path: { data_id: dataId }, query: opts.query });
    }
    /**
     * createData.
     * @method POST /v2/workspaces/{workspace_id}/data/{data_id}
     * @remarks Any query params may be sent (none documented).
     */
    createData(dataId, body, opts = {}) {
      return this.http.post("/v2/workspaces/{workspace_id}/data/{data_id}", { path: { data_id: dataId }, body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_dictionaries.ts
  var Dictionaries = class extends Resource {
    /**
     * deleteDictionary.
     * @method DELETE /v1/workspaces/{workspace_id}/dictionaries/{dictionary_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteDictionary(dictionaryId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/dictionaries/{dictionary_id}", { path: { dictionary_id: dictionaryId }, query: opts.query });
    }
    /**
     * getDictionary.
     * @method GET /v1/workspaces/{workspace_id}/dictionaries/{dictionary_id}
     * @remarks Any query params may be sent (none documented).
     */
    getDictionary(dictionaryId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/dictionaries/{dictionary_id}", { path: { dictionary_id: dictionaryId }, query: opts.query });
    }
    /**
     * updateDictionary.
     * @method PUT /v1/workspaces/{workspace_id}/dictionaries/{dictionary_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateDictionary(dictionaryId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/dictionaries/{dictionary_id}", { path: { dictionary_id: dictionaryId }, body, query: opts.query });
    }
    /**
     * listDictionaries.
     * @method GET /v1/workspaces/{workspace_id}/dictionaries
     * @remarks Documented query: filters (extra keys allowed).
     */
    listDictionaries(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/dictionaries", { query: opts.query });
    }
    /**
     * createDictionary.
     * @method POST /v1/workspaces/{workspace_id}/dictionaries
     * @remarks Any query params may be sent (none documented).
     */
    createDictionary(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/dictionaries", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_dictionariesFields.ts
  var DictionariesFields = class extends Resource {
    /**
     * listDictionariesFields.
     * @method GET /v1/workspaces/{workspace_id}/dictionaries-fields
     * @remarks Documented query: filters (extra keys allowed).
     */
    listDictionariesFields(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/dictionaries-fields", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_distributionLogs.ts
  var DistributionLogs = class extends Resource {
    /**
     * Get next distribution.
     * @method GET /v1/workspaces/{workspace_id}/distribution-logs/next-distribution
     * @remarks Any query params may be sent (none documented).
     */
    getNextDistribution(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/distribution-logs/next-distribution", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_domains.ts
  var Domains = class extends Resource {
    /**
     * deleteDomain.
     * @method DELETE /v1/workspaces/{workspace_id}/domains/{domain_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteDomain(domainId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/domains/{domain_id}", { path: { domain_id: domainId }, query: opts.query });
    }
    /**
     * getDomain.
     * @method GET /v1/workspaces/{workspace_id}/domains/{domain_id}
     * @remarks Any query params may be sent (none documented).
     */
    getDomain(domainId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/domains/{domain_id}", { path: { domain_id: domainId }, query: opts.query });
    }
    /**
     * updateDomain.
     * @method PUT /v1/workspaces/{workspace_id}/domains/{domain_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateDomain(domainId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/domains/{domain_id}", { path: { domain_id: domainId }, body, query: opts.query });
    }
    /**
     * getAvailableName.
     * @method GET /v1/workspaces/{workspace_id}/domains/available-name
     * @remarks Any query params may be sent (none documented).
     */
    getAvailableName(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/domains/available-name", { query: opts.query });
    }
    /**
     * listDomains.
     * @method GET /v1/workspaces/{workspace_id}/domains
     * @remarks Documented query: filters (extra keys allowed).
     */
    listDomains(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/domains", { query: opts.query });
    }
    /**
     * createDomain.
     * @method POST /v1/workspaces/{workspace_id}/domains
     * @remarks Any query params may be sent (none documented).
     */
    createDomain(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/domains", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_emailEvents.ts
  var EmailEvents = class extends Resource {
    /**
     * createEmailEvent.
     * @method POST /v1/workspaces/{workspace_id}/email-events/{email_event_id}
     * @remarks Any query params may be sent (none documented).
     */
    createEmailEvent(emailEventId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/email-events/{email_event_id}", { path: { email_event_id: emailEventId }, body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_enrichment.ts
  var Enrichment = class extends Resource {
    /**
     * checkDocument.
     * @method POST /v1/enrichment/check-document
     * @remarks Any query params may be sent (none documented).
     */
    checkDocument(body, opts = {}) {
      return this.http.post("/v1/enrichment/check-document", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_events.ts
  var Events = class extends Resource {
    /**
     * Get event by id.
     * @method GET /v1/workspaces/{workspace_id}/events/{id}
     * @remarks Any query params may be sent (none documented).
     */
    getEvent(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/events/{id}", { path: { id }, query: opts.query });
    }
    /**
     * Get all events.
     * @method GET /v1/workspaces/{workspace_id}/events
     * @remarks Documented query: filters, page, limit, order, direction_order, person, user, service, card, organization, start_date, end_date, populate, event_type (extra keys allowed).
     */
    listEvents(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/events", { query: opts.query });
    }
    /**
     * Create a event.
     * @method POST /v1/workspaces/{workspace_id}/events
     * @remarks Any query params may be sent (none documented).
     */
    createEvent(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/events", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_export.ts
  var Export = class extends Resource {
    /**
     * createExport.
     * @method POST /v1/workspaces/{workspace_id}/export
     * @remarks Any query params may be sent (none documented).
     */
    createExport(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/export", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_extensions.ts
  var Extensions = class extends Resource {
    /**
     * deleteExtension.
     * @method DELETE /v1/workspaces/{workspace_id}/extensions/{extension_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteExtension(extensionId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/extensions/{extension_id}", { path: { extension_id: extensionId }, query: opts.query });
    }
    /**
     * getExtension.
     * @method GET /v1/workspaces/{workspace_id}/extensions/{extension_id}
     * @remarks Any query params may be sent (none documented).
     */
    getExtension(extensionId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/extensions/{extension_id}", { path: { extension_id: extensionId }, query: opts.query });
    }
    /**
     * updateExtension.
     * @method PUT /v1/workspaces/{workspace_id}/extensions/{extension_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateExtension(extensionId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/extensions/{extension_id}", { path: { extension_id: extensionId }, body, query: opts.query });
    }
    /**
     * getMe.
     * @method GET /v1/workspaces/{workspace_id}/extensions/me
     * @remarks Any query params may be sent (none documented).
     */
    getMe(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/extensions/me", { query: opts.query });
    }
    /**
     * listExtensions.
     * @method GET /v1/workspaces/{workspace_id}/extensions
     * @remarks Documented query: filters (extra keys allowed).
     */
    listExtensions(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/extensions", { query: opts.query });
    }
    /**
     * createExtension.
     * @method POST /v1/workspaces/{workspace_id}/extensions
     * @remarks Any query params may be sent (none documented).
     */
    createExtension(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/extensions", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_fastMessages.ts
  var FastMessages = class extends Resource {
    /**
     * deleteFastMessage.
     * @method DELETE /v1/workspaces/{workspace_id}/fast-messages/{fast_message_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteFastMessage(fastMessageId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/fast-messages/{fast_message_id}", { path: { fast_message_id: fastMessageId }, query: opts.query });
    }
    /**
     * getFastMessage.
     * @method GET /v1/workspaces/{workspace_id}/fast-messages/{fast_message_id}
     * @remarks Any query params may be sent (none documented).
     */
    getFastMessage(fastMessageId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/fast-messages/{fast_message_id}", { path: { fast_message_id: fastMessageId }, query: opts.query });
    }
    /**
     * updateFastMessage.
     * @method PUT /v1/workspaces/{workspace_id}/fast-messages/{fast_message_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateFastMessage(fastMessageId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/fast-messages/{fast_message_id}", { path: { fast_message_id: fastMessageId }, body, query: opts.query });
    }
    /**
     * listFastMessages.
     * @method GET /v1/workspaces/{workspace_id}/fast-messages
     * @remarks Documented query: filters (extra keys allowed).
     */
    listFastMessages(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/fast-messages", { query: opts.query });
    }
    /**
     * createFastMessage.
     * @method POST /v1/workspaces/{workspace_id}/fast-messages
     * @remarks Any query params may be sent (none documented).
     */
    createFastMessage(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/fast-messages", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_feedPosts.ts
  var FeedPosts = class extends Resource {
    /**
     * deleteComments.
     * @method DELETE /v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/comments/{comment_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteComments(feedPostId, commentId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/comments/{comment_id}", { path: { feed_post_id: feedPostId, comment_id: commentId }, query: opts.query });
    }
    /**
     * patchComments.
     * @method PATCH /v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/comments/{comment_id}
     * @remarks Any query params may be sent (none documented).
     */
    patchComments(feedPostId, commentId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/comments/{comment_id}", { path: { feed_post_id: feedPostId, comment_id: commentId }, body, query: opts.query });
    }
    /**
     * deletePollVotes.
     * @method DELETE /v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/poll-votes/{poll_vote_id}
     * @remarks Any query params may be sent (none documented).
     */
    deletePollVotes(feedPostId, pollVoteId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/poll-votes/{poll_vote_id}", { path: { feed_post_id: feedPostId, poll_vote_id: pollVoteId }, query: opts.query });
    }
    /**
     * getComments.
     * @method GET /v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/comments
     * @remarks Any query params may be sent (none documented).
     */
    getComments(feedPostId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/comments", { path: { feed_post_id: feedPostId }, query: opts.query });
    }
    /**
     * comments.
     * @method POST /v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/comments
     * @remarks Any query params may be sent (none documented).
     */
    comments(feedPostId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/comments", { path: { feed_post_id: feedPostId }, body, query: opts.query });
    }
    /**
     * getPollVotes.
     * @method GET /v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/poll-votes
     * @remarks Any query params may be sent (none documented).
     */
    getPollVotes(feedPostId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/poll-votes", { path: { feed_post_id: feedPostId }, query: opts.query });
    }
    /**
     * pollVotes.
     * @method POST /v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/poll-votes
     * @remarks Any query params may be sent (none documented).
     */
    pollVotes(feedPostId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/poll-votes", { path: { feed_post_id: feedPostId }, body, query: opts.query });
    }
    /**
     * deleteReaction.
     * @method DELETE /v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/reaction
     * @remarks Any query params may be sent (none documented).
     */
    deleteReaction(feedPostId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/reaction", { path: { feed_post_id: feedPostId }, query: opts.query });
    }
    /**
     * reaction.
     * @method POST /v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/reaction
     * @remarks Any query params may be sent (none documented).
     */
    reaction(feedPostId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}/reaction", { path: { feed_post_id: feedPostId }, body, query: opts.query });
    }
    /**
     * deleteSchedule.
     * @method DELETE /v1/workspaces/{workspace_id}/feed-posts/schedule/{schedule_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteSchedule(scheduleId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/feed-posts/schedule/{schedule_id}", { path: { schedule_id: scheduleId }, query: opts.query });
    }
    /**
     * getScheduledPostById.
     * @method GET /v1/workspaces/{workspace_id}/feed-posts/schedule/{schedule_id}
     * @remarks Any query params may be sent (none documented).
     */
    getScheduledPostById(scheduleId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/feed-posts/schedule/{schedule_id}", { path: { schedule_id: scheduleId }, query: opts.query });
    }
    /**
     * patchSchedule.
     * @method PATCH /v1/workspaces/{workspace_id}/feed-posts/schedule/{schedule_id}
     * @remarks Any query params may be sent (none documented).
     */
    patchSchedule(scheduleId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/feed-posts/schedule/{schedule_id}", { path: { schedule_id: scheduleId }, body, query: opts.query });
    }
    /**
     * deleteFeedPost.
     * @method DELETE /v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteFeedPost(feedPostId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}", { path: { feed_post_id: feedPostId }, query: opts.query });
    }
    /**
     * getFeedPost.
     * @method GET /v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}
     * @remarks Any query params may be sent (none documented).
     */
    getFeedPost(feedPostId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}", { path: { feed_post_id: feedPostId }, query: opts.query });
    }
    /**
     * updateFeedPost.
     * @method PUT /v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}
     * @remarks Any query params may be sent (none documented).
     * @param file The spreadsheet file part (sent under the `file` field).
     * @param fields Extra form-data text fields to send alongside the file.
     */
    updateFeedPost(feedPostId, file, fields, opts = {}) {
      const body = { kind: "multipart", fields, files: { file } };
      return this.http.put("/v1/workspaces/{workspace_id}/feed-posts/{feed_post_id}", { path: { feed_post_id: feedPostId }, body, query: opts.query });
    }
    /**
     * getHabllaPosts.
     * @method GET /v1/workspaces/{workspace_id}/feed-posts/hablla-posts
     * @remarks Any query params may be sent (none documented).
     */
    getHabllaPosts(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/feed-posts/hablla-posts", { query: opts.query });
    }
    /**
     * getReactions.
     * @method GET /v1/workspaces/{workspace_id}/feed-posts/reactions
     * @remarks Documented query: filters (extra keys allowed).
     */
    getReactions(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/feed-posts/reactions", { query: opts.query });
    }
    /**
     * getAllScheduledPosts.
     * @method GET /v1/workspaces/{workspace_id}/feed-posts/schedule
     * @remarks Documented query: filters (extra keys allowed).
     */
    getAllScheduledPosts(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/feed-posts/schedule", { query: opts.query });
    }
    /**
     * schedule.
     * @method POST /v1/workspaces/{workspace_id}/feed-posts/schedule
     * @remarks Any query params may be sent (none documented).
     */
    schedule(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/feed-posts/schedule", { body, query: opts.query });
    }
    /**
     * listFeedPosts.
     * @method GET /v1/workspaces/{workspace_id}/feed-posts
     * @remarks Documented query: filters (extra keys allowed).
     */
    listFeedPosts(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/feed-posts", { query: opts.query });
    }
    /**
     * createFeedPost.
     * @method POST /v1/workspaces/{workspace_id}/feed-posts
     * @remarks Any query params may be sent (none documented).
     * @param file The spreadsheet file part (sent under the `file` field).
     * @param fields Extra form-data text fields to send alongside the file.
     */
    createFeedPost(file, fields, opts = {}) {
      const body = { kind: "multipart", fields, files: { file } };
      return this.http.post("/v1/workspaces/{workspace_id}/feed-posts", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_flows.ts
  var Flows = class extends Resource {
    /**
     * executeNode.
     * @method POST /v1/workspaces/{workspace_id}/flows/{flow_id}/execute-node
     * @remarks Any query params may be sent (none documented).
     */
    executeNode(flowId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/flows/{flow_id}/execute-node", { path: { flow_id: flowId }, body, query: opts.query });
    }
    /**
     * install.
     * @method POST /v1/workspaces/{workspace_id}/flows/{flow_id}/install
     * @remarks Any query params may be sent (none documented).
     */
    install(flowId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/flows/{flow_id}/install", { path: { flow_id: flowId }, body, query: opts.query });
    }
    /**
     * makePublic.
     * @method POST /v1/workspaces/{workspace_id}/flows/{flow_id}/make-public
     * @remarks Any query params may be sent (none documented).
     */
    makePublic(flowId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/flows/{flow_id}/make-public", { path: { flow_id: flowId }, body, query: opts.query });
    }
    /**
     * publish.
     * @method POST /v1/workspaces/{workspace_id}/flows/{flow_id}/publish
     * @remarks Any query params may be sent (none documented).
     */
    publish(flowId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/flows/{flow_id}/publish", { path: { flow_id: flowId }, body, query: opts.query });
    }
    /**
     * getVersion.
     * @method GET /v1/workspaces/{workspace_id}/flows/{flow_id}/version
     * @remarks Documented query: filters (extra keys allowed).
     */
    getVersion(flowId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/flows/{flow_id}/version", { path: { flow_id: flowId }, query: opts.query });
    }
    /**
     * deleteFlow.
     * @method DELETE /v1/workspaces/{workspace_id}/flows/{flow_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteFlow(flowId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/flows/{flow_id}", { path: { flow_id: flowId }, query: opts.query });
    }
    /**
     * getFlow.
     * @method GET /v1/workspaces/{workspace_id}/flows/{flow_id}
     * @remarks Any query params may be sent (none documented).
     */
    getFlow(flowId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/flows/{flow_id}", { path: { flow_id: flowId }, query: opts.query });
    }
    /**
     * updateFlow.
     * @method PUT /v1/workspaces/{workspace_id}/flows/{flow_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateFlow(flowId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/flows/{flow_id}", { path: { flow_id: flowId }, body, query: opts.query });
    }
    /**
     * getPublic.
     * @method GET /v1/workspaces/{workspace_id}/flows/public
     * @remarks Documented query: filters (extra keys allowed).
     */
    getPublic(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/flows/public", { query: opts.query });
    }
    /**
     * listFlows.
     * @method GET /v1/workspaces/{workspace_id}/flows
     * @remarks Documented query: filters (extra keys allowed).
     */
    listFlows(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/flows", { query: opts.query });
    }
    /**
     * createFlow.
     * @method POST /v1/workspaces/{workspace_id}/flows
     * @remarks Any query params may be sent (none documented).
     */
    createFlow(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/flows", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_flowsExecutions.ts
  var FlowsExecutions = class extends Resource {
    /**
     * retryExecution.
     * @method POST /v1/workspaces/{workspace_id}/flows-executions/{flows_execution_id}/retry
     * @remarks Any query params may be sent (none documented).
     */
    retryExecution(flowsExecutionId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/flows-executions/{flows_execution_id}/retry", { path: { flows_execution_id: flowsExecutionId }, body, query: opts.query });
    }
    /**
     * stop.
     * @method POST /v1/workspaces/{workspace_id}/flows-executions/{flows_execution_id}/stop
     * @remarks Any query params may be sent (none documented).
     */
    stop(flowsExecutionId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/flows-executions/{flows_execution_id}/stop", { path: { flows_execution_id: flowsExecutionId }, body, query: opts.query });
    }
    /**
     * getFlowsExecution.
     * @method GET /v1/workspaces/{workspace_id}/flows-executions/{flows_execution_id}
     * @remarks Any query params may be sent (none documented).
     */
    getFlowsExecution(flowsExecutionId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/flows-executions/{flows_execution_id}", { path: { flows_execution_id: flowsExecutionId }, query: opts.query });
    }
    /**
     * retryMultipleExecutions.
     * @method POST /v1/workspaces/{workspace_id}/flows-executions/retry
     * @remarks Any query params may be sent (none documented).
     */
    retryMultipleExecutions(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/flows-executions/retry", { body, query: opts.query });
    }
    /**
     * listFlowsExecutions.
     * @method GET /v1/workspaces/{workspace_id}/flows-executions
     * @remarks Documented query: filters (extra keys allowed).
     */
    listFlowsExecutions(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/flows-executions", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_goals.ts
  var Goals = class extends Resource {
    /**
     * deleteGoal.
     * @method DELETE /v1/workspaces/{workspace_id}/goals/{goal_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteGoal(goalId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/goals/{goal_id}", { path: { goal_id: goalId }, query: opts.query });
    }
    /**
     * getGoal.
     * @method GET /v1/workspaces/{workspace_id}/goals/{goal_id}
     * @remarks Any query params may be sent (none documented).
     */
    getGoal(goalId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/goals/{goal_id}", { path: { goal_id: goalId }, query: opts.query });
    }
    /**
     * updateGoal.
     * @method PUT /v1/workspaces/{workspace_id}/goals/{goal_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateGoal(goalId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/goals/{goal_id}", { path: { goal_id: goalId }, body, query: opts.query });
    }
    /**
     * listGoals.
     * @method GET /v1/workspaces/{workspace_id}/goals
     * @remarks Documented query: filters (extra keys allowed).
     */
    listGoals(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/goals", { query: opts.query });
    }
    /**
     * createGoal.
     * @method POST /v1/workspaces/{workspace_id}/goals
     * @remarks Any query params may be sent (none documented).
     */
    createGoal(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/goals", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_goalsScopes.ts
  var GoalsScopes = class extends Resource {
    /**
     * deleteGoalsScope.
     * @method DELETE /v1/workspaces/{workspace_id}/goals-scopes/{goals_scope_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteGoalsScope(goalsScopeId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/goals-scopes/{goals_scope_id}", { path: { goals_scope_id: goalsScopeId }, query: opts.query });
    }
    /**
     * updateGoalsScope.
     * @method PUT /v1/workspaces/{workspace_id}/goals-scopes/{goals_scope_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateGoalsScope(goalsScopeId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/goals-scopes/{goals_scope_id}", { path: { goals_scope_id: goalsScopeId }, body, query: opts.query });
    }
    /**
     * listGoalsScopes.
     * @method GET /v1/workspaces/{workspace_id}/goals-scopes
     * @remarks Documented query: filters (extra keys allowed).
     */
    listGoalsScopes(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/goals-scopes", { query: opts.query });
    }
    /**
     * createGoalsScope.
     * @method POST /v1/workspaces/{workspace_id}/goals-scopes
     * @remarks Any query params may be sent (none documented).
     */
    createGoalsScope(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/goals-scopes", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_habllaAgent.ts
  var HabllaAgent = class extends Resource {
    /**
     * putHistory.
     * @method PUT /v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/skill/{skill_id}/history/{history_id}
     * @remarks Any query params may be sent (none documented).
     */
    putHistory(habllaAgentId, skillId, historyId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/skill/{skill_id}/history/{history_id}", { path: { hablla_agent_id: habllaAgentId, skill_id: skillId, history_id: historyId }, body, query: opts.query });
    }
    /**
     * getAgentHistoryBySkill.
     * @method GET /v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/skill/{skill_id}/history
     * @remarks Documented query: filters (extra keys allowed).
     */
    getAgentHistoryBySkill(habllaAgentId, skillId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/skill/{skill_id}/history", { path: { hablla_agent_id: habllaAgentId, skill_id: skillId }, query: opts.query, queryFormat: "json" });
    }
    /**
     * putRestore.
     * @method PUT /v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/restore/{restore_id}
     * @remarks Any query params may be sent (none documented).
     */
    putRestore(habllaAgentId, restoreId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/restore/{restore_id}", { path: { hablla_agent_id: habllaAgentId, restore_id: restoreId }, body, query: opts.query });
    }
    /**
     * deleteSkill.
     * @method DELETE /v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/skill/{skill_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteSkill(habllaAgentId, skillId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/skill/{skill_id}", { path: { hablla_agent_id: habllaAgentId, skill_id: skillId }, query: opts.query });
    }
    /**
     * getAgentSkillById.
     * @method GET /v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/skill/{skill_id}
     * @remarks Documented query: filters (extra keys allowed).
     */
    getAgentSkillById(habllaAgentId, skillId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/skill/{skill_id}", { path: { hablla_agent_id: habllaAgentId, skill_id: skillId }, query: opts.query, queryFormat: "json" });
    }
    /**
     * putSkill.
     * @method PUT /v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/skill/{skill_id}
     * @remarks Any query params may be sent (none documented).
     */
    putSkill(habllaAgentId, skillId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/skill/{skill_id}", { path: { hablla_agent_id: habllaAgentId, skill_id: skillId }, body, query: opts.query });
    }
    /**
     * getAllAgentHistory.
     * @method GET /v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/history
     * @remarks Documented query: filters (extra keys allowed).
     */
    getAllAgentHistory(habllaAgentId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/history", { path: { hablla_agent_id: habllaAgentId }, query: opts.query, queryFormat: "json" });
    }
    /**
     * getAllSkillsAgent.
     * @method GET /v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/skill
     * @remarks Documented query: filters (extra keys allowed).
     */
    getAllSkillsAgent(habllaAgentId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/skill", { path: { hablla_agent_id: habllaAgentId }, query: opts.query });
    }
    /**
     * skill.
     * @method POST /v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/skill
     * @remarks Any query params may be sent (none documented).
     */
    skill(habllaAgentId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}/skill", { path: { hablla_agent_id: habllaAgentId }, body, query: opts.query });
    }
    /**
     * deleteHabllaAgent.
     * @method DELETE /v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteHabllaAgent(habllaAgentId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}", { path: { hablla_agent_id: habllaAgentId }, query: opts.query });
    }
    /**
     * getHabllaAgent.
     * @method GET /v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}
     * @remarks Any query params may be sent (none documented).
     */
    getHabllaAgent(habllaAgentId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}", { path: { hablla_agent_id: habllaAgentId }, query: opts.query });
    }
    /**
     * updateHabllaAgent.
     * @method PUT /v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateHabllaAgent(habllaAgentId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/hablla-agent/{hablla_agent_id}", { path: { hablla_agent_id: habllaAgentId }, body, query: opts.query });
    }
    /**
     * listHabllaAgent.
     * @method GET /v1/workspaces/{workspace_id}/hablla-agent
     * @remarks Documented query: filters (extra keys allowed).
     */
    listHabllaAgent(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/hablla-agent", { query: opts.query });
    }
    /**
     * createHabllaAgent.
     * @method POST /v1/workspaces/{workspace_id}/hablla-agent
     * @remarks Any query params may be sent (none documented).
     */
    createHabllaAgent(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/hablla-agent", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_habllaDocs.ts
  var HabllaDocs = class extends Resource {
    /**
     * updateHabllaDoc.
     * @method PUT /v1/hablla-docs/{hablla_doc_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateHabllaDoc(habllaDocId, body, opts = {}) {
      return this.http.put("/v1/hablla-docs/{hablla_doc_id}", { path: { hablla_doc_id: habllaDocId }, body, query: opts.query });
    }
    /**
     * listHabllaDocs.
     * @method GET /v1/hablla-docs
     * @remarks Documented query: filters (extra keys allowed).
     */
    listHabllaDocs(opts = {}) {
      return this.http.get("/v1/hablla-docs", { query: opts.query });
    }
    /**
     * createHabllaDoc.
     * @method POST /v1/hablla-docs
     * @remarks Any query params may be sent (none documented).
     */
    createHabllaDoc(body, opts = {}) {
      return this.http.post("/v1/hablla-docs", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_habllaReports.ts
  var HabllaReports = class extends Resource {
    /**
     * getMonthlySessionsTotals.
     * @method GET /v1/workspaces/hablla-reports/reports/alloy-reports/admin/monthly-sessions-totals
     * @remarks Documented query: filters (extra keys allowed).
     */
    getMonthlySessionsTotals(opts = {}) {
      return this.http.get("/v1/workspaces/hablla-reports/reports/alloy-reports/admin/monthly-sessions-totals", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_holidays.ts
  var Holidays = class extends Resource {
    /**
     * Delete holiday by id.
     * @method DELETE /v1/workspaces/{workspace_id}/holidays/{holiday_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteHoliday(holidayId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/holidays/{holiday_id}", { path: { holiday_id: holidayId }, query: opts.query });
    }
    /**
     * Get holiday by id.
     * @method GET /v1/workspaces/{workspace_id}/holidays/{id}
     * @remarks Any query params may be sent (none documented).
     */
    getHoliday(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/holidays/{id}", { path: { id }, query: opts.query });
    }
    /**
     * Update holiday by id.
     * @method PUT /v1/workspaces/{workspace_id}/holidays/{holiday_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateHoliday(holidayId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/holidays/{holiday_id}", { path: { holiday_id: holidayId }, body, query: opts.query });
    }
    /**
     * Get holiday by filter.
     * @method GET /v1/workspaces/{workspace_id}/holidays/filter
     * @remarks Documented query: name, date, nameOrDate (extra keys allowed).
     */
    getFilter(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/holidays/filter", { query: opts.query });
    }
    /**
     * Get all holidays.
     * @method GET /v1/workspaces/{workspace_id}/holidays
     * @remarks Documented query: filters, page, limit, order, direction_order, name, date (extra keys allowed).
     */
    listHolidays(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/holidays", { query: opts.query });
    }
    /**
     * Create a new holiday.
     * @method POST /v1/workspaces/{workspace_id}/holidays
     * @remarks Any query params may be sent (none documented).
     */
    createHoliday(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/holidays", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_htm.ts
  var Htm = class extends Resource {
    /**
     * deleteContainer.
     * @method DELETE /v1/workspaces/{workspace_id}/htm/container/{container_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteContainer(containerId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/htm/container/{container_id}", { path: { container_id: containerId }, query: opts.query });
    }
    /**
     * getHTMById.
     * @method GET /v1/workspaces/{workspace_id}/htm/container/{container_id}
     * @remarks Any query params may be sent (none documented).
     */
    getHTMById(containerId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/htm/container/{container_id}", { path: { container_id: containerId }, query: opts.query });
    }
    /**
     * putContainer.
     * @method PUT /v1/workspaces/{workspace_id}/htm/container/{container_id}
     * @remarks Any query params may be sent (none documented).
     */
    putContainer(containerId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/htm/container/{container_id}", { path: { container_id: containerId }, body, query: opts.query });
    }
    /**
     * deleteTag.
     * @method DELETE /v1/workspaces/{workspace_id}/htm/tag/{tag_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteTag(tagId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/htm/tag/{tag_id}", { path: { tag_id: tagId }, query: opts.query });
    }
    /**
     * getHTMTagsById.
     * @method GET /v1/workspaces/{workspace_id}/htm/tag/{tag_id}
     * @remarks Any query params may be sent (none documented).
     */
    getHTMTagsById(tagId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/htm/tag/{tag_id}", { path: { tag_id: tagId }, query: opts.query });
    }
    /**
     * putTag.
     * @method PUT /v1/workspaces/{workspace_id}/htm/tag/{tag_id}
     * @remarks Any query params may be sent (none documented).
     */
    putTag(tagId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/htm/tag/{tag_id}", { path: { tag_id: tagId }, body, query: opts.query });
    }
    /**
     * getAllHTM.
     * @method GET /v1/workspaces/{workspace_id}/htm/container
     * @remarks Documented query: filters (extra keys allowed).
     */
    getAllHTM(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/htm/container", { query: opts.query });
    }
    /**
     * container.
     * @method POST /v1/workspaces/{workspace_id}/htm/container
     * @remarks Any query params may be sent (none documented).
     */
    container(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/htm/container", { body, query: opts.query });
    }
    /**
     * getAllHTMTags.
     * @method GET /v1/workspaces/{workspace_id}/htm/tag
     * @remarks Documented query: filters (extra keys allowed).
     */
    getAllHTMTags(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/htm/tag", { query: opts.query });
    }
    /**
     * tag.
     * @method POST /v1/workspaces/{workspace_id}/htm/tag
     * @remarks Any query params may be sent (none documented).
     */
    tag(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/htm/tag", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_import.ts
  var Import = class extends Resource {
    /**
     * createImport.
     * @method POST /v1/workspaces/{workspace_id}/import
     * @remarks Any query params may be sent (none documented).
     * @param file The spreadsheet file part (sent under the `file` field).
     * @param fields Extra form-data text fields to send alongside the file.
     */
    createImport(file, fields, opts = {}) {
      const body = { kind: "multipart", fields, files: { file } };
      return this.http.post("/v1/workspaces/{workspace_id}/import", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_invoices.ts
  var Invoices = class extends Resource {
    /**
     * deleteInvoice.
     * @method DELETE /v1/workspaces/{workspace_id}/invoices/{invoice_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteInvoice(invoiceId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/invoices/{invoice_id}", { path: { invoice_id: invoiceId }, query: opts.query });
    }
    /**
     * getInvoice.
     * @method GET /v1/workspaces/{workspace_id}/invoices/{invoice_id}
     * @remarks Any query params may be sent (none documented).
     */
    getInvoice(invoiceId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/invoices/{invoice_id}", { path: { invoice_id: invoiceId }, query: opts.query });
    }
    /**
     * updateInvoice.
     * @method PUT /v1/workspaces/{workspace_id}/invoices/{invoice_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateInvoice(invoiceId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/invoices/{invoice_id}", { path: { invoice_id: invoiceId }, body, query: opts.query });
    }
    /**
     * listInvoices.
     * @method GET /v1/workspaces/{workspace_id}/invoices
     * @remarks Documented query: filters (extra keys allowed).
     */
    listInvoices(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/invoices", { query: opts.query });
    }
    /**
     * createInvoice.
     * @method POST /v1/workspaces/{workspace_id}/invoices
     * @remarks Any query params may be sent (none documented).
     */
    createInvoice(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/invoices", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_knowledgeBases.ts
  var KnowledgeBases = class extends Resource {
    /**
     * deleteSources.
     * @method DELETE /v1/workspaces/{workspace_id}/knowledge-bases/{knowledge_base_id}/sources/{source_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteSources(knowledgeBaseId, sourceId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/knowledge-bases/{knowledge_base_id}/sources/{source_id}", { path: { knowledge_base_id: knowledgeBaseId, source_id: sourceId }, query: opts.query });
    }
    /**
     * getSource.
     * @method GET /v1/workspaces/{workspace_id}/knowledge-bases/{knowledge_base_id}/sources/{source_id}
     * @remarks Any query params may be sent (none documented).
     */
    getSource(knowledgeBaseId, sourceId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/knowledge-bases/{knowledge_base_id}/sources/{source_id}", { path: { knowledge_base_id: knowledgeBaseId, source_id: sourceId }, query: opts.query });
    }
    /**
     * putSources.
     * @method PUT /v1/workspaces/{workspace_id}/knowledge-bases/{knowledge_base_id}/sources/{source_id}
     * @remarks Any query params may be sent (none documented).
     */
    putSources(knowledgeBaseId, sourceId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/knowledge-bases/{knowledge_base_id}/sources/{source_id}", { path: { knowledge_base_id: knowledgeBaseId, source_id: sourceId }, body, query: opts.query });
    }
    /**
     * getSources.
     * @method GET /v1/workspaces/{workspace_id}/knowledge-bases/{knowledge_base_id}/sources
     * @remarks Documented query: filters (extra keys allowed).
     */
    getSources(knowledgeBaseId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/knowledge-bases/{knowledge_base_id}/sources", { path: { knowledge_base_id: knowledgeBaseId }, query: opts.query });
    }
    /**
     * sources.
     * @method POST /v1/workspaces/{workspace_id}/knowledge-bases/{knowledge_base_id}/sources
     * @remarks Any query params may be sent (none documented).
     */
    sources(knowledgeBaseId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/knowledge-bases/{knowledge_base_id}/sources", { path: { knowledge_base_id: knowledgeBaseId }, body, query: opts.query });
    }
    /**
     * deleteKnowledgeBase.
     * @method DELETE /v1/workspaces/{workspace_id}/knowledge-bases/{knowledge_base_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteKnowledgeBase(knowledgeBaseId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/knowledge-bases/{knowledge_base_id}", { path: { knowledge_base_id: knowledgeBaseId }, query: opts.query });
    }
    /**
     * getKnowledgeBase.
     * @method GET /v1/workspaces/{workspace_id}/knowledge-bases/{knowledge_base_id}
     * @remarks Any query params may be sent (none documented).
     */
    getKnowledgeBase(knowledgeBaseId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/knowledge-bases/{knowledge_base_id}", { path: { knowledge_base_id: knowledgeBaseId }, query: opts.query });
    }
    /**
     * updateKnowledgeBase.
     * @method PUT /v1/workspaces/{workspace_id}/knowledge-bases/{knowledge_base_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateKnowledgeBase(knowledgeBaseId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/knowledge-bases/{knowledge_base_id}", { path: { knowledge_base_id: knowledgeBaseId }, body, query: opts.query });
    }
    /**
     * listKnowledgeBases.
     * @method GET /v1/workspaces/{workspace_id}/knowledge-bases
     * @remarks Documented query: filters (extra keys allowed).
     */
    listKnowledgeBases(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/knowledge-bases", { query: opts.query });
    }
    /**
     * createKnowledgeBase.
     * @method POST /v1/workspaces/{workspace_id}/knowledge-bases
     * @remarks Any query params may be sent (none documented).
     */
    createKnowledgeBase(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/knowledge-bases", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_legalEntities.ts
  var LegalEntities = class extends Resource {
    /**
     * getUser.
     * @method GET /v1/legal-entities/user/{user_id}
     * @remarks Any query params may be sent (none documented).
     */
    getUser(userId, opts = {}) {
      return this.http.get("/v1/legal-entities/user/{user_id}", { path: { user_id: userId }, query: opts.query });
    }
    /**
     * updateLegalEntity.
     * @method PUT /v1/legal-entities/{legal_entity_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateLegalEntity(legalEntityId, body, opts = {}) {
      return this.http.put("/v1/legal-entities/{legal_entity_id}", { path: { legal_entity_id: legalEntityId }, body, query: opts.query });
    }
    /**
     * listLegalEntities.
     * @method GET /v1/legal-entities
     * @remarks Any query params may be sent (none documented).
     */
    listLegalEntities(opts = {}) {
      return this.http.get("/v1/legal-entities", { query: opts.query });
    }
    /**
     * createLegalEntity.
     * @method POST /v1/legal-entities
     * @remarks Any query params may be sent (none documented).
     */
    createLegalEntity(body, opts = {}) {
      return this.http.post("/v1/legal-entities", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_linkInBio.ts
  var LinkInBio = class extends Resource {
    /**
     * deleteLinkInBio.
     * @method DELETE /v1/workspaces/{workspace_id}/link-in-bio/{link_in_bio_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteLinkInBio(linkInBioId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/link-in-bio/{link_in_bio_id}", { path: { link_in_bio_id: linkInBioId }, query: opts.query });
    }
    /**
     * getLinkInBio.
     * @method GET /v1/workspaces/{workspace_id}/link-in-bio/{link_in_bio_id}
     * @remarks Any query params may be sent (none documented).
     */
    getLinkInBio(linkInBioId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/link-in-bio/{link_in_bio_id}", { path: { link_in_bio_id: linkInBioId }, query: opts.query });
    }
    /**
     * updateLinkInBio.
     * @method PUT /v1/workspaces/{workspace_id}/link-in-bio/{link_in_bio_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateLinkInBio(linkInBioId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/link-in-bio/{link_in_bio_id}", { path: { link_in_bio_id: linkInBioId }, body, query: opts.query });
    }
    /**
     * listLinkInBio.
     * @method GET /v1/workspaces/{workspace_id}/link-in-bio
     * @remarks Documented query: filters (extra keys allowed).
     */
    listLinkInBio(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/link-in-bio", { query: opts.query });
    }
    /**
     * createLinkInBio.
     * @method POST /v1/workspaces/{workspace_id}/link-in-bio
     * @remarks Any query params may be sent (none documented).
     */
    createLinkInBio(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/link-in-bio", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_messages.ts
  var Messages = class extends Resource {
    /**
     * Mark message as read.
     * @method PATCH /v1/workspaces/{workspace_id}/connections/{connection_id}/messages/read
     * @remarks Any query params may be sent (none documented).
     */
    patchRead(connectionId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/connections/{connection_id}/messages/read", { path: { connection_id: connectionId }, body, query: opts.query });
    }
    /**
     * Send email messages by bot.
     * @method POST /v1/workspaces/{workspace_id}/connections/{connection_id}/messages-bot
     * @remarks Any query params may be sent (none documented).
     */
    MessagesController_createMessagesConnByBot_v1(connectionId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/connections/{connection_id}/messages-bot", { path: { connection_id: connectionId }, body, query: opts.query });
    }
    /**
     * Create a message connections by a bot.
     * @method POST /v1/workspaces/{workspace_id}/connections/{connection_id}/messages-message-bot
     * @remarks Any query params may be sent (none documented).
     */
    messagesMessageBot(connectionId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/connections/{connection_id}/messages-message-bot", { path: { connection_id: connectionId }, body, query: opts.query });
    }
    /**
     * Create a message templates connections by a bot.
     * @method POST /v1/workspaces/{workspace_id}/connections/{connection_id}/messages-templates-bot
     * @remarks Any query params may be sent (none documented).
     */
    messagesTemplatesBot(connectionId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/connections/{connection_id}/messages-templates-bot", { path: { connection_id: connectionId }, body, query: opts.query });
    }
    /**
     * Create a message using bot.
     * @method POST /v1/workspaces/{workspace_id}/services/{service_id}/messages-bot
     * @remarks Any query params may be sent (none documented).
     */
    MessagesController_createByBot_v1(serviceId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/services/{service_id}/messages-bot", { path: { service_id: serviceId }, body, query: opts.query });
    }
    /**
     * Create a comment message on a service using workspace token.
     * @method POST /v1/workspaces/{workspace_id}/services/{service_id}/messages-comment
     * @remarks Any query params may be sent (none documented).
     */
    messagesComment(serviceId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/services/{service_id}/messages-comment", { path: { service_id: serviceId }, body, query: opts.query });
    }
    /**
     * Send message to a group.
     * @method POST /v1/workspaces/{workspace_id}/room/{room_id}
     * @remarks Any query params may be sent (none documented).
     */
    createRoom(roomId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/room/{room_id}", { path: { room_id: roomId }, body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_newPrefix.ts
  var NewPrefix = class extends Resource {
    /**
     * createNewPrefix.
     * @method POST /v1/workspaces/{workspace_id}/new-prefix
     * @remarks Any query params may be sent (none documented).
     */
    createNewPrefix(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/new-prefix", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_nps.ts
  var Nps = class extends Resource {
    /**
     * deleteRatings.
     * @method DELETE /v1/workspaces/{workspace_id}/nps/{np_id}/ratings/{rating_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteRatings(npId, ratingId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/nps/{np_id}/ratings/{rating_id}", { path: { np_id: npId, rating_id: ratingId }, query: opts.query });
    }
    /**
     * putRatings.
     * @method PUT /v1/workspaces/{workspace_id}/nps/{np_id}/ratings/{rating_id}
     * @remarks Any query params may be sent (none documented).
     */
    putRatings(npId, ratingId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/nps/{np_id}/ratings/{rating_id}", { path: { np_id: npId, rating_id: ratingId }, body, query: opts.query });
    }
    /**
     * getRatings.
     * @method GET /v1/workspaces/{workspace_id}/nps/{np_id}/ratings
     * @remarks Documented query: filters (extra keys allowed).
     */
    getRatings(npId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/nps/{np_id}/ratings", { path: { np_id: npId }, query: opts.query });
    }
    /**
     * ratings.
     * @method POST /v1/workspaces/{workspace_id}/nps/{np_id}/ratings
     * @remarks Any query params may be sent (none documented).
     */
    ratings(npId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/nps/{np_id}/ratings", { path: { np_id: npId }, body, query: opts.query });
    }
    /**
     * patchStatus.
     * @method PATCH /v1/workspaces/{workspace_id}/nps/{np_id}/status
     * @remarks Any query params may be sent (none documented).
     */
    patchStatus(npId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/nps/{np_id}/status", { path: { np_id: npId }, body, query: opts.query });
    }
    /**
     * deleteNp.
     * @method DELETE /v1/workspaces/{workspace_id}/nps/{np_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteNp(npId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/nps/{np_id}", { path: { np_id: npId }, query: opts.query });
    }
    /**
     * getNp.
     * @method GET /v1/workspaces/{workspace_id}/nps/{np_id}
     * @remarks Any query params may be sent (none documented).
     */
    getNp(npId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/nps/{np_id}", { path: { np_id: npId }, query: opts.query });
    }
    /**
     * updateNp.
     * @method PUT /v1/workspaces/{workspace_id}/nps/{np_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateNp(npId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/nps/{np_id}", { path: { np_id: npId }, body, query: opts.query });
    }
    /**
     * listNps.
     * @method GET /v1/workspaces/{workspace_id}/nps
     * @remarks Documented query: filters (extra keys allowed).
     */
    listNps(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/nps", { query: opts.query });
    }
    /**
     * createNp.
     * @method POST /v1/workspaces/{workspace_id}/nps
     * @remarks Any query params may be sent (none documented).
     */
    createNp(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/nps", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_officeHours.ts
  var OfficeHours = class extends Resource {
    /**
     * deleteOfficeHour.
     * @method DELETE /v1/workspaces/{workspace_id}/office-hours/{office_hour_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteOfficeHour(officeHourId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/office-hours/{office_hour_id}", { path: { office_hour_id: officeHourId }, query: opts.query });
    }
    /**
     * getOfficeHour.
     * @method GET /v1/workspaces/{workspace_id}/office-hours/{office_hour_id}
     * @remarks Any query params may be sent (none documented).
     */
    getOfficeHour(officeHourId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/office-hours/{office_hour_id}", { path: { office_hour_id: officeHourId }, query: opts.query });
    }
    /**
     * updateOfficeHour.
     * @method PUT /v1/workspaces/{workspace_id}/office-hours/{office_hour_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateOfficeHour(officeHourId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/office-hours/{office_hour_id}", { path: { office_hour_id: officeHourId }, body, query: opts.query });
    }
    /**
     * listOfficeHours.
     * @method GET /v1/workspaces/{workspace_id}/office-hours
     * @remarks Documented query: filters (extra keys allowed).
     */
    listOfficeHours(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/office-hours", { query: opts.query });
    }
    /**
     * createOfficeHour.
     * @method POST /v1/workspaces/{workspace_id}/office-hours
     * @remarks Any query params may be sent (none documented).
     */
    createOfficeHour(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/office-hours", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_organizations.ts
  var Organizations = class extends Resource {
    /**
     * Add person to organization by id.
     * @method PATCH /v1/workspaces/{workspace_id}/organizations/{organization_id}/add-persons
     * @remarks Documented query: persons (extra keys allowed).
     */
    addPersons(organizationId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/organizations/{organization_id}/add-persons", { path: { organization_id: organizationId }, body, query: opts.query });
    }
    /**
     * Add tags to org by id.
     * @method PUT /v1/workspaces/{workspace_id}/organizations/{organization_id}/add-tags
     * @remarks Documented query: tags (extra keys allowed).
     */
    addTags(organizationId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/organizations/{organization_id}/add-tags", { path: { organization_id: organizationId }, body, query: opts.query });
    }
    /**
     * Get all organization costs.
     * @method GET /v1/workspaces/{workspace_id}/organizations/{organization_id}/costs
     * @remarks Documented query: filters, page, limit, order, direction_order, entity_type, start_date, end_date, populate (extra keys allowed).
     */
    getCosts(organizationId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/organizations/{organization_id}/costs", { path: { organization_id: organizationId }, query: opts.query });
    }
    /**
     * Get all persons by organization id.
     * @method GET /v1/workspaces/{workspace_id}/organizations/{id}/persons
     * @remarks Documented query: page, limit, order, direction_order, search, name, email, phone, tax_id, legal_name, status, updated_at, custom_fields, user, tags, populate (extra keys allowed).
     */
    getPersons(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/organizations/{id}/persons", { path: { id }, query: opts.query });
    }
    /**
     * Remove person from organization by id.
     * @method PATCH /v1/workspaces/{workspace_id}/organizations/{organization_id}/remove-persons
     * @remarks Documented query: persons (extra keys allowed).
     */
    removePersons(organizationId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/organizations/{organization_id}/remove-persons", { path: { organization_id: organizationId }, body, query: opts.query });
    }
    /**
     * Remove tags on organization by id.
     * @method PUT /v1/workspaces/{workspace_id}/organizations/{organization_id}/remove-tags
     * @remarks Documented query: tags (extra keys allowed).
     */
    removeTags(organizationId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/organizations/{organization_id}/remove-tags", { path: { organization_id: organizationId }, body, query: opts.query });
    }
    /**
     * Get all organizations segmentations.
     * @method GET /v1/workspaces/{workspace_id}/organizations/{organization_id}/segmentations
     * @remarks Documented query: filters, page, limit, order, direction_order, populate, search (extra keys allowed).
     */
    getSegmentations(organizationId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/organizations/{organization_id}/segmentations", { path: { organization_id: organizationId }, query: opts.query });
    }
    /**
     * Delete organization by id.
     * @method DELETE /v1/workspaces/{workspace_id}/organizations/{id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteOrganization(id, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/organizations/{id}", { path: { id }, query: opts.query });
    }
    /**
     * Get organization by id.
     * @method GET /v1/workspaces/{workspace_id}/organizations/{organization_id}
     * @remarks Documented query: filters, populate (extra keys allowed).
     */
    getOrganization(organizationId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/organizations/{organization_id}", { path: { organization_id: organizationId }, query: opts.query });
    }
    /**
     * Update organization by id.
     * @method PUT /v1/workspaces/{workspace_id}/organizations/{organization_id}
     * @remarks Documented query: populate (extra keys allowed).
     */
    updateOrganization(organizationId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/organizations/{organization_id}", { path: { organization_id: organizationId }, body, query: opts.query });
    }
    /**
     * Create a new batch action.
     * @method POST /v1/workspaces/{workspace_id}/organizations/batch
     * @remarks Any query params may be sent (none documented).
     */
    batch(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/organizations/batch", { body, query: opts.query });
    }
    /**
     * Check if the if value is duplicate.
     * @method GET /v1/workspaces/{workspace_id}/organizations/check-duplicate
     * @remarks Documented query: email, phone, tax_id, id (extra keys allowed).
     */
    getCheckDuplicate(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/organizations/check-duplicate", { query: opts.query });
    }
    /**
     * Create or update a organization.
     * @method POST /v1/workspaces/{workspace_id}/organizations/create-or-update
     * @remarks Any query params may be sent (none documented).
     */
    createOrUpdate(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/organizations/create-or-update", { body, query: opts.query });
    }
    /**
     * Get organization by filter.
     * @method GET /v1/workspaces/{workspace_id}/organizations/filter
     * @remarks Documented query: email, phone, custom_fields, tax_id (extra keys allowed).
     */
    getFilter(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/organizations/filter", { query: opts.query });
    }
    /**
     * Organizations merge.
     * @method POST /v1/workspaces/{workspace_id}/organizations/merge
     * @remarks Any query params may be sent (none documented).
     */
    merge(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/organizations/merge", { body, query: opts.query });
    }
    /**
     * Get all organizations.
     * @method GET /v1/workspaces/{workspace_id}/organizations
     * @remarks Documented query: filters, page, limit, order, direction_order, search, name, email, phone, tax_id, legal_name, status, updated_at, custom_fields, user, tags, populate (extra keys allowed).
     */
    listOrganizations(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/organizations", { query: opts.query });
    }
    /**
     * Create a organization.
     * @method POST /v1/workspaces/{workspace_id}/organizations
     * @remarks Any query params may be sent (none documented).
     */
    createOrganization(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/organizations", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_pages.ts
  var Pages = class extends Resource {
    /**
     * Set page project editor.
     * @method PATCH /v1/workspaces/{workspace_id}/pages-projects/{id}/editor-version
     * @remarks Any query params may be sent (none documented).
     */
    patchEditorVersion(id, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/pages-projects/{id}/editor-version", { path: { id }, body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_pagesProjects.ts
  var PagesProjects = class extends Resource {
    /**
     * Restore a page from a page history by id.
     * @method PUT /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}/page-history/{page_history_id}
     * @remarks Any query params may be sent (none documented).
     */
    putPageHistory(pagesProjectId, pageId, pageHistoryId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}/page-history/{page_history_id}", { path: { pages_project_id: pagesProjectId, page_id: pageId, page_history_id: pageHistoryId }, body, query: opts.query });
    }
    /**
     * Set a page to be used with manual editor.
     * @method PATCH /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}/enable-manual-editor
     * @remarks Any query params may be sent (none documented).
     */
    enableManualEditor(pagesProjectId, pageId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}/enable-manual-editor", { path: { pages_project_id: pagesProjectId, page_id: pageId }, body, query: opts.query });
    }
    /**
     * Get all page version history from a page by id.
     * @method GET /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}/page-history
     * @remarks Documented query: filters, page, limit, order, user, direction_order, populate (extra keys allowed).
     */
    getPageHistory(pagesProjectId, pageId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}/page-history", { path: { pages_project_id: pagesProjectId, page_id: pageId }, query: opts.query });
    }
    /**
     * putPublic.
     * @method PUT /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}/public
     * @remarks Any query params may be sent (none documented).
     */
    putPublic(pagesProjectId, pageId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}/public", { path: { pages_project_id: pagesProjectId, page_id: pageId }, body, query: opts.query });
    }
    /**
     * Republish page by id.
     * @method PATCH /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}/republish
     * @remarks Any query params may be sent (none documented).
     */
    republishPageById(pagesProjectId, pageId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}/republish", { path: { pages_project_id: pagesProjectId, page_id: pageId }, body, query: opts.query });
    }
    /**
     * Unpublish page by id.
     * @method PATCH /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}/unpublish
     * @remarks Any query params may be sent (none documented).
     */
    patchUnpublish(pagesProjectId, pageId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}/unpublish", { path: { pages_project_id: pagesProjectId, page_id: pageId }, body, query: opts.query });
    }
    /**
     * Delete page by id.
     * @method DELETE /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}
     * @remarks Any query params may be sent (none documented).
     */
    deletePages(pagesProjectId, pageId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}", { path: { pages_project_id: pagesProjectId, page_id: pageId }, query: opts.query });
    }
    /**
     * Get a page by id.
     * @method GET /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}
     * @remarks Any query params may be sent (none documented).
     */
    getPage(pagesProjectId, pageId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}", { path: { pages_project_id: pagesProjectId, page_id: pageId }, query: opts.query });
    }
    /**
     * Update page by id.
     * @method PUT /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}
     * @remarks Any query params may be sent (none documented).
     */
    putPages(pagesProjectId, pageId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages/{page_id}", { path: { pages_project_id: pagesProjectId, page_id: pageId }, body, query: opts.query });
    }
    /**
     * Remove a page project shared components by id.
     * @method DELETE /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/shared-components/{shared_component_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteSharedComponents(pagesProjectId, sharedComponentId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/shared-components/{shared_component_id}", { path: { pages_project_id: pagesProjectId, shared_component_id: sharedComponentId }, query: opts.query });
    }
    /**
     * Get a page project shared component by id.
     * @method GET /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/shared-components/{shared_component_id}
     * @remarks Any query params may be sent (none documented).
     */
    getSharedComponent(pagesProjectId, sharedComponentId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/shared-components/{shared_component_id}", { path: { pages_project_id: pagesProjectId, shared_component_id: sharedComponentId }, query: opts.query });
    }
    /**
     * Update a page project shared components by id.
     * @method PUT /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/shared-components/{shared_component_id}
     * @remarks Any query params may be sent (none documented).
     */
    putSharedComponents(pagesProjectId, sharedComponentId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/shared-components/{shared_component_id}", { path: { pages_project_id: pagesProjectId, shared_component_id: sharedComponentId }, body, query: opts.query });
    }
    /**
     * Get all public pages templates.
     * @method GET /v1/workspaces/{workspace_id}/pages-projects/pages/templates/public
     * @remarks Documented query: filters, page, limit, order, project, slug, name, direction_order, is_template, category, populate, uploaded_on_github (extra keys allowed).
     */
    getPublic(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/pages-projects/pages/templates/public", { query: opts.query });
    }
    /**
     * Get all workspace pages templates.
     * @method GET /v1/workspaces/{workspace_id}/pages-projects/pages/templates/workspace
     * @remarks Documented query: filters, page, limit, order, project, slug, name, direction_order, is_template, category, populate, uploaded_on_github (extra keys allowed).
     */
    getWorkspace(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/pages-projects/pages/templates/workspace", { query: opts.query });
    }
    /**
     * Add custom domain from page project by id.
     * @method PATCH /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/add-domain
     * @remarks Any query params may be sent (none documented).
     */
    addDomain(pagesProjectId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/add-domain", { path: { pages_project_id: pagesProjectId }, body, query: opts.query });
    }
    /**
     * Use AI.
     * @method POST /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/ai
     * @remarks Any query params may be sent (none documented).
     */
    ai(pagesProjectId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/ai", { path: { pages_project_id: pagesProjectId }, body, query: opts.query });
    }
    /**
     * Get all pages.
     * @method GET /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages
     * @remarks Documented query: filters, page, limit, order, project, slug, name, direction_order, is_template, category, populate, uploaded_on_github (extra keys allowed).
     */
    getPages(pagesProjectId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages", { path: { pages_project_id: pagesProjectId }, query: opts.query });
    }
    /**
     * Create a new page.
     * @method POST /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages
     * @remarks Any query params may be sent (none documented).
     */
    pages(pagesProjectId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/pages", { path: { pages_project_id: pagesProjectId }, body, query: opts.query });
    }
    /**
     * Publish page by id.
     * @method GET /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/publish
     * @remarks Any query params may be sent (none documented).
     */
    publish(pagesProjectId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/publish", { path: { pages_project_id: pagesProjectId }, query: opts.query });
    }
    /**
     * Remove custom domain from page project by id.
     * @method PATCH /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/remove-domain
     * @remarks Any query params may be sent (none documented).
     */
    removeDomain(pagesProjectId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/remove-domain", { path: { pages_project_id: pagesProjectId }, body, query: opts.query });
    }
    /**
     * Republish a page project by id.
     * @method PATCH /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/republish
     * @remarks Any query params may be sent (none documented).
     */
    republishPagesProjectById(pagesProjectId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/republish", { path: { pages_project_id: pagesProjectId }, body, query: opts.query });
    }
    /**
     * Get a page shared components by id.
     * @method GET /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/shared-components
     * @remarks Any query params may be sent (none documented).
     */
    getSharedComponents(pagesProjectId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/shared-components", { path: { pages_project_id: pagesProjectId }, query: opts.query });
    }
    /**
     * Add a page project shared components.
     * @method POST /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/shared-components
     * @remarks Any query params may be sent (none documented).
     */
    sharedComponents(pagesProjectId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/shared-components", { path: { pages_project_id: pagesProjectId }, body, query: opts.query });
    }
    /**
     * Unpublish a page project by id.
     * @method PUT /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/unpublish
     * @remarks Any query params may be sent (none documented).
     */
    putUnpublish(pagesProjectId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}/unpublish", { path: { pages_project_id: pagesProjectId }, body, query: opts.query });
    }
    /**
     * Delete page project by id.
     * @method DELETE /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}
     * @remarks Any query params may be sent (none documented).
     */
    deletePagesProject(pagesProjectId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}", { path: { pages_project_id: pagesProjectId }, query: opts.query });
    }
    /**
     * Get pages project by id.
     * @method GET /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}
     * @remarks Any query params may be sent (none documented).
     */
    getPagesProject(pagesProjectId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}", { path: { pages_project_id: pagesProjectId }, query: opts.query });
    }
    /**
     * patchPagesProject.
     * @method PATCH /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}
     * @remarks Any query params may be sent (none documented).
     */
    patchPagesProject(pagesProjectId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}", { path: { pages_project_id: pagesProjectId }, body, query: opts.query });
    }
    /**
     * Update page project by id.
     * @method PUT /v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}
     * @remarks Any query params may be sent (none documented).
     */
    updatePagesProject(pagesProjectId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/pages-projects/{pages_project_id}", { path: { pages_project_id: pagesProjectId }, body, query: opts.query });
    }
    /**
     * Get all pages projects.
     * @method GET /v1/workspaces/{workspace_id}/pages-projects
     * @remarks Documented query: filters, page, limit, order, name, direction_order, populate (extra keys allowed).
     */
    listPagesProjects(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/pages-projects", { query: opts.query });
    }
    /**
     * Create a page project.
     * @method POST /v1/workspaces/{workspace_id}/pages-projects
     * @remarks Any query params may be sent (none documented).
     */
    createPagesProject(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/pages-projects", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_partner.ts
  var Partner = class extends Resource {
    /**
     * createPartner.
     * @method POST /v1/workspaces/partner
     * @remarks Any query params may be sent (none documented).
     */
    createPartner(body, opts = {}) {
      return this.http.post("/v1/workspaces/partner", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_partnered.ts
  var Partnered = class extends Resource {
    /**
     * listPartnered.
     * @method GET /v1/workspaces/{workspace_id}/partnered
     * @remarks Documented query: filters (extra keys allowed).
     */
    listPartnered(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/partnered", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_partners.ts
  var Partners = class extends Resource {
    /**
     * deletePartner.
     * @method DELETE /v1/partners/{partner_id}
     * @remarks Any query params may be sent (none documented).
     */
    deletePartner(partnerId, opts = {}) {
      return this.http.delete("/v1/partners/{partner_id}", { path: { partner_id: partnerId }, query: opts.query });
    }
    /**
     * updatePartner.
     * @method PUT /v1/partners/{partner_id}
     * @remarks Any query params may be sent (none documented).
     */
    updatePartner(partnerId, body, opts = {}) {
      return this.http.put("/v1/partners/{partner_id}", { path: { partner_id: partnerId }, body, query: opts.query });
    }
    /**
     * listPartners.
     * @method GET /v1/partners
     * @remarks Documented query: filters (extra keys allowed).
     */
    listPartners(opts = {}) {
      return this.http.get("/v1/partners", { query: opts.query });
    }
    /**
     * createPartner.
     * @method POST /v1/partners
     * @remarks Any query params may be sent (none documented).
     */
    createPartner(body, opts = {}) {
      return this.http.post("/v1/partners", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_paymentIntervals.ts
  var PaymentIntervals = class extends Resource {
    /**
     * deletePaymentInterval.
     * @method DELETE /v1/workspaces/{workspace_id}/payment-intervals/{payment_interval_id}
     * @remarks Any query params may be sent (none documented).
     */
    deletePaymentInterval(paymentIntervalId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/payment-intervals/{payment_interval_id}", { path: { payment_interval_id: paymentIntervalId }, query: opts.query });
    }
    /**
     * getPaymentInterval.
     * @method GET /v1/workspaces/{workspace_id}/payment-intervals/{payment_interval_id}
     * @remarks Any query params may be sent (none documented).
     */
    getPaymentInterval(paymentIntervalId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/payment-intervals/{payment_interval_id}", { path: { payment_interval_id: paymentIntervalId }, query: opts.query });
    }
    /**
     * updatePaymentInterval.
     * @method PUT /v1/workspaces/{workspace_id}/payment-intervals/{payment_interval_id}
     * @remarks Any query params may be sent (none documented).
     */
    updatePaymentInterval(paymentIntervalId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/payment-intervals/{payment_interval_id}", { path: { payment_interval_id: paymentIntervalId }, body, query: opts.query });
    }
    /**
     * listPaymentIntervals.
     * @method GET /v1/workspaces/{workspace_id}/payment-intervals
     * @remarks Documented query: filters (extra keys allowed).
     */
    listPaymentIntervals(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/payment-intervals", { query: opts.query });
    }
    /**
     * createPaymentInterval.
     * @method POST /v1/workspaces/{workspace_id}/payment-intervals
     * @remarks Any query params may be sent (none documented).
     */
    createPaymentInterval(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/payment-intervals", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_permissions.ts
  var Permissions = class extends Resource {
    /**
     * deletePermission.
     * @method DELETE /v1/workspaces/{workspace_id}/permissions/{permission_id}
     * @remarks Any query params may be sent (none documented).
     */
    deletePermission(permissionId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/permissions/{permission_id}", { path: { permission_id: permissionId }, query: opts.query });
    }
    /**
     * getPermission.
     * @method GET /v1/workspaces/{workspace_id}/permissions/{permission_id}
     * @remarks Any query params may be sent (none documented).
     */
    getPermission(permissionId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/permissions/{permission_id}", { path: { permission_id: permissionId }, query: opts.query });
    }
    /**
     * updatePermission.
     * @method PUT /v1/workspaces/{workspace_id}/permissions/{permission_id}
     * @remarks Any query params may be sent (none documented).
     */
    updatePermission(permissionId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/permissions/{permission_id}", { path: { permission_id: permissionId }, body, query: opts.query });
    }
    /**
     * listPermissions.
     * @method GET /v1/workspaces/{workspace_id}/permissions
     * @remarks Documented query: filters (extra keys allowed).
     */
    listPermissions(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/permissions", { query: opts.query });
    }
    /**
     * createPermission.
     * @method POST /v1/workspaces/{workspace_id}/permissions
     * @remarks Any query params may be sent (none documented).
     */
    createPermission(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/permissions", { body, query: opts.query });
    }
    /**
     * updateWorkspacePermissions.
     * @method PUT /v1/workspaces/{workspace_id}/permissions
     * @remarks Any query params may be sent (none documented).
     */
    updateWorkspacePermissions(body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/permissions", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_persons.ts
  var Persons = class extends Resource {
    /**
     * Add email to person by id.
     * @method PUT /v1/workspaces/{workspace_id}/persons/{id}/add-emails
     * @remarks Any query params may be sent (none documented).
     */
    addEmails(id, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/persons/{id}/add-emails", { path: { id }, body, query: opts.query });
    }
    /**
     * Add followers to person by id.
     * @method PUT /v1/workspaces/{workspace_id}/persons/{person_id}/add-followers
     * @remarks Any query params may be sent (none documented).
     */
    addFollowers(personId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/persons/{person_id}/add-followers", { path: { person_id: personId }, body, query: opts.query });
    }
    /**
     * Add organization to person by id.
     * @method PUT /v1/workspaces/{workspace_id}/persons/{id}/add-organizations
     * @remarks Any query params may be sent (none documented).
     */
    addOrganizations(id, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/persons/{id}/add-organizations", { path: { id }, body, query: opts.query });
    }
    /**
     * Add phones to person by id.
     * @method PUT /v1/workspaces/{workspace_id}/persons/{id}/add-phones
     * @remarks Any query params may be sent (none documented).
     */
    addPhones(id, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/persons/{id}/add-phones", { path: { id }, body, query: opts.query });
    }
    /**
     * Add sector to person by id.
     * @method PUT /v1/workspaces/{workspace_id}/persons/{person_id}/add-sectors
     * @remarks Any query params may be sent (none documented).
     */
    addSectors(personId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/persons/{person_id}/add-sectors", { path: { person_id: personId }, body, query: opts.query });
    }
    /**
     * Add tags on person by id.
     * @method PUT /v1/workspaces/{workspace_id}/persons/{person_id}/add-tags
     * @remarks Any query params may be sent (none documented).
     */
    addTags(personId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/persons/{person_id}/add-tags", { path: { person_id: personId }, body, query: opts.query });
    }
    /**
     * Add user on person.
     * @method PUT /v1/workspaces/{workspace_id}/persons/{person_id}/add-users
     * @remarks Any query params may be sent (none documented).
     */
    addUsers(personId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/persons/{person_id}/add-users", { path: { person_id: personId }, body, query: opts.query });
    }
    /**
     * Add a new webchat_id on person by id.
     * @method PATCH /v1/workspaces/{workspace_id}/persons/{id}/add-webchat
     * @remarks Any query params may be sent (none documented).
     */
    addWebchat(id, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/persons/{id}/add-webchat", { path: { id }, body, query: opts.query });
    }
    /**
     * Block person by id.
     * @method PATCH /v1/workspaces/{workspace_id}/persons/{person_id}/block
     * @remarks Any query params may be sent (none documented).
     */
    patchBlock(personId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/persons/{person_id}/block", { path: { person_id: personId }, body, query: opts.query });
    }
    /**
     * Get all persons.
     * @method GET /v1/workspaces/{workspace_id}/persons/{person_id}/costs
     * @remarks Documented query: filters, page, limit, order, direction_order, entity_type, start_date, end_date, populate (extra keys allowed).
     */
    getCosts(personId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/persons/{person_id}/costs", { path: { person_id: personId }, query: opts.query });
    }
    /**
     * Get duplicated persons by id.
     * @method GET /v1/workspaces/{workspace_id}/persons/{person_id}/duplicates
     * @remarks Documented query: page, limit, order, direction_order (extra keys allowed).
     */
    getDuplicates(personId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/persons/{person_id}/duplicates", { path: { person_id: personId }, query: opts.query });
    }
    /**
     * Get email from person by id.
     * @method GET /v1/workspaces/{workspace_id}/persons/{id}/emails
     * @remarks Documented query: page, limit, order, direction_order, to, subject, text, user, person, service, populate (extra keys allowed).
     */
    getEmails(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/persons/{id}/emails", { path: { id }, query: opts.query });
    }
    /**
     * Get person opened-services by id.
     * @method GET /v1/workspaces/{workspace_id}/persons/{person_id}/opened-services
     * @remarks Documented query: connection (extra keys allowed).
     */
    getOpenedServices(personId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/persons/{person_id}/opened-services", { path: { person_id: personId }, query: opts.query });
    }
    /**
     * Remove email from person by id.
     * @method PUT /v1/workspaces/{workspace_id}/persons/{id}/remove-email
     * @remarks Any query params may be sent (none documented).
     */
    removeEmail(id, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/persons/{id}/remove-email", { path: { id }, body, query: opts.query });
    }
    /**
     * Remove followers from person by id.
     * @method PUT /v1/workspaces/{workspace_id}/persons/{person_id}/remove-followers
     * @remarks Any query params may be sent (none documented).
     */
    removeFollowers(personId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/persons/{person_id}/remove-followers", { path: { person_id: personId }, body, query: opts.query });
    }
    /**
     * Remove organization on person by id.
     * @method PUT /v1/workspaces/{workspace_id}/persons/{person_id}/remove-organizations
     * @remarks Any query params may be sent (none documented).
     */
    removeOrganizations(personId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/persons/{person_id}/remove-organizations", { path: { person_id: personId }, body, query: opts.query });
    }
    /**
     * Remove sectors from person by id.
     * @method PUT /v1/workspaces/{workspace_id}/persons/{person_id}/remove-sectors
     * @remarks Any query params may be sent (none documented).
     */
    removeSectors(personId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/persons/{person_id}/remove-sectors", { path: { person_id: personId }, body, query: opts.query });
    }
    /**
     * Remove tags from person by id.
     * @method PUT /v1/workspaces/{workspace_id}/persons/{person_id}/remove-tags
     * @remarks Any query params may be sent (none documented).
     */
    removeTags(personId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/persons/{person_id}/remove-tags", { path: { person_id: personId }, body, query: opts.query });
    }
    /**
     * Remove user on person by id.
     * @method PUT /v1/workspaces/{workspace_id}/persons/{person_id}/remove-users
     * @remarks Any query params may be sent (none documented).
     */
    removeUsers(personId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/persons/{person_id}/remove-users", { path: { person_id: personId }, body, query: opts.query });
    }
    /**
     * Remove a webchat from person by webchat_id.
     * @method PATCH /v1/workspaces/{workspace_id}/persons/{id}/remove-webchat
     * @remarks Any query params may be sent (none documented).
     */
    removeWebchat(id, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/persons/{id}/remove-webchat", { path: { id }, body, query: opts.query });
    }
    /**
     * Get all persons segmentations.
     * @method GET /v1/workspaces/{workspace_id}/persons/{person_id}/segmentations
     * @remarks Documented query: filters, page, limit, order, direction_order, populate, search (extra keys allowed).
     */
    getSegmentations(personId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/persons/{person_id}/segmentations", { path: { person_id: personId }, query: opts.query });
    }
    /**
     * Get person services-counters by id.
     * @method GET /v1/workspaces/{workspace_id}/persons/{person_id}/services-counters
     * @remarks Documented query: status (extra keys allowed).
     */
    getServicesCounters(personId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/persons/{person_id}/services-counters", { path: { person_id: personId }, query: opts.query });
    }
    /**
     * Add user (by phone) to person by id.
     * @method PATCH /v1/workspaces/{workspace_id}/persons/{id}/take
     * @remarks Any query params may be sent (none documented).
     */
    patchTake(id, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/persons/{id}/take", { path: { id }, body, query: opts.query });
    }
    /**
     * Update an webchat on person by webchat_id.
     * @method PATCH /v1/workspaces/{workspace_id}/persons/{id}/update-webchat
     * @remarks Any query params may be sent (none documented).
     */
    updateWebchat(id, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/persons/{id}/update-webchat", { path: { id }, body, query: opts.query });
    }
    /**
     * Get person by webchat id.
     * @method GET /v1/workspaces/{workspace_id}/persons/webchat/{webchat_id}
     * @remarks Documented query: populate (extra keys allowed).
     */
    getWebchat(webchatId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/persons/webchat/{webchat_id}", { path: { webchat_id: webchatId }, query: opts.query });
    }
    /**
     * Get person by id.
     * @method GET /v1/workspaces/{workspace_id}/persons/{person_id}
     * @remarks Documented query: populate (extra keys allowed).
     */
    getPerson(personId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/persons/{person_id}", { path: { person_id: personId }, query: opts.query });
    }
    /**
     * Update person by id.
     * @method PUT /v1/workspaces/{workspace_id}/persons/{person_id}
     * @remarks Documented query: populate (extra keys allowed).
     */
    updatePerson(personId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/persons/{person_id}", { path: { person_id: personId }, body, query: opts.query });
    }
    /**
     * Associate a UUID with a person.
     * @method POST /v1/workspaces/{workspace_id}/persons/associate-uuid
     * @remarks Any query params may be sent (none documented).
     */
    associateUuid(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/persons/associate-uuid", { body, query: opts.query });
    }
    /**
     * Create a new batch action.
     * @method POST /v1/workspaces/{workspace_id}/persons/batch
     * @remarks Any query params may be sent (none documented).
     */
    batch(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/persons/batch", { body, query: opts.query });
    }
    /**
     * Check if the if value is duplicate.
     * @method GET /v1/workspaces/{workspace_id}/persons/check-duplicate
     * @remarks Documented query: filters, email, phone, ssn, id (extra keys allowed).
     */
    getCheckDuplicate(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/persons/check-duplicate", { query: opts.query });
    }
    /**
     * Create or update a person.
     * @method POST /v1/workspaces/{workspace_id}/persons/create-or-update
     * @remarks Any query params may be sent (none documented).
     */
    createOrUpdate(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/persons/create-or-update", { body, query: opts.query });
    }
    /**
     * Get person by filter.
     * @method GET /v1/workspaces/{workspace_id}/persons/filter
     * @remarks Documented query: email, phone, custom_fields, ssn (extra keys allowed).
     */
    getFilter(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/persons/filter", { query: opts.query });
    }
    /**
     * Person merge.
     * @method POST /v1/workspaces/{workspace_id}/persons/merge
     * @remarks Any query params may be sent (none documented).
     */
    merge(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/persons/merge", { body, query: opts.query });
    }
    /**
     * Unsubscribe a key.
     * @method POST /v1/workspaces/{workspace_id}/persons/unsubscribe
     * @remarks Any query params may be sent (none documented).
     */
    unsubscribe(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/persons/unsubscribe", { body, query: opts.query });
    }
    /**
     * Get all persons.
     * @method GET /v1/workspaces/{workspace_id}/persons
     * @remarks Documented query: page, limit, next, previous, order, direction_order, name, email, phone, search, full_search, start_date, end_date, field_date, updated_at, custom_fields, has_duplicate_keys, is_blocked, customer_status, organization, tags, populate, ssn, users (extra keys allowed).
     */
    listPersonsV1(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/persons", { query: opts.query });
    }
    /**
     * Create a new person.
     * @method POST /v1/workspaces/{workspace_id}/persons
     * @remarks Any query params may be sent (none documented).
     */
    createPerson(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/persons", { body, query: opts.query });
    }
    /**
     * Get all persons (V2).
     * @method GET /v2/workspaces/{workspace_id}/persons
     * @remarks Documented query: filters, page, limit, next, previous, order, direction_order, name, email, phone, search, full_search, start_date, end_date, field_date, updated_at, custom_fields, has_duplicate_keys, is_blocked, customer_status, organization, tags, populate, ssn, users (extra keys allowed).
     */
    listPersons(opts = {}) {
      return this.http.get("/v2/workspaces/{workspace_id}/persons", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_plans.ts
  var Plans = class extends Resource {
    /**
     * deletePlan.
     * @method DELETE /v1/plans/{plan_id}
     * @remarks Any query params may be sent (none documented).
     */
    deletePlan(planId, opts = {}) {
      return this.http.delete("/v1/plans/{plan_id}", { path: { plan_id: planId }, query: opts.query });
    }
    /**
     * getPlan.
     * @method GET /v1/plans/{plan_id}
     * @remarks Any query params may be sent (none documented).
     */
    getPlan(planId, opts = {}) {
      return this.http.get("/v1/plans/{plan_id}", { path: { plan_id: planId }, query: opts.query });
    }
    /**
     * updatePlan.
     * @method PUT /v1/plans/{plan_id}
     * @remarks Any query params may be sent (none documented).
     */
    updatePlan(planId, body, opts = {}) {
      return this.http.put("/v1/plans/{plan_id}", { path: { plan_id: planId }, body, query: opts.query });
    }
    /**
     * patchActivate.
     * @method PATCH /v1/workspaces/{workspace_id}/plans/activate
     * @remarks Any query params may be sent (none documented).
     */
    patchActivate(body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/plans/activate", { body, query: opts.query });
    }
    /**
     * getSubscribe.
     * @method GET /v1/workspaces/{workspace_id}/plans/subscribe
     * @remarks Any query params may be sent (none documented).
     */
    getSubscribe(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/plans/subscribe", { query: opts.query });
    }
    /**
     * unsubscribe.
     * @method DELETE /v1/workspaces/{workspace_id}/plans/unsubscribe
     * @remarks Any query params may be sent (none documented).
     */
    unsubscribe(opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/plans/unsubscribe", { query: opts.query });
    }
    /**
     * listPlans.
     * @method GET /v1/plans
     * @remarks Documented query: filters (extra keys allowed).
     */
    listPlans(opts = {}) {
      return this.http.get("/v1/plans", { query: opts.query });
    }
    /**
     * createPlan.
     * @method POST /v1/plans
     * @remarks Any query params may be sent (none documented).
     */
    createPlan(body, opts = {}) {
      return this.http.post("/v1/plans", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_products.ts
  var Products = class extends Resource {
    /**
     * Delete product by id.
     * @method DELETE /v1/workspaces/{workspace_id}/products/{product_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteProduct(productId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/products/{product_id}", { path: { product_id: productId }, query: opts.query });
    }
    /**
     * Get product by id.
     * @method GET /v1/workspaces/{workspace_id}/products/{product_id}
     * @remarks Any query params may be sent (none documented).
     */
    getProduct(productId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/products/{product_id}", { path: { product_id: productId }, query: opts.query });
    }
    /**
     * Update product by id.
     * @method PUT /v1/workspaces/{workspace_id}/products/{product_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateProduct(productId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/products/{product_id}", { path: { product_id: productId }, body, query: opts.query });
    }
    /**
     * Get all products.
     * @method GET /v1/workspaces/{workspace_id}/products
     * @remarks Documented query: filters, page, limit, order, direction_order, name, sku, exact_sku, product_group, product_groups, search, updated_at, populate, custom_fields, is_active, currency_code (extra keys allowed).
     */
    listProducts(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/products", { query: opts.query });
    }
    /**
     * Create a product.
     * @method POST /v1/workspaces/{workspace_id}/products
     * @remarks Any query params may be sent (none documented).
     */
    createProduct(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/products", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_productsGroups.ts
  var ProductsGroups = class extends Resource {
    /**
     * getParent.
     * @method GET /v1/workspaces/{workspace_id}/products-groups/check/code/{code_id}/parent/{parent_id}
     * @remarks Documented query: product_group (extra keys allowed).
     */
    getParent(codeId, parentId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/products-groups/check/code/{code_id}/parent/{parent_id}", { path: { code_id: codeId, parent_id: parentId }, query: opts.query });
    }
    /**
     * getCode.
     * @method GET /v1/workspaces/{workspace_id}/products-groups/check/code/{code_id}
     * @remarks Documented query: product_group (extra keys allowed).
     */
    getCode(codeId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/products-groups/check/code/{code_id}", { path: { code_id: codeId }, query: opts.query });
    }
    /**
     * deleteProductsGroup.
     * @method DELETE /v1/workspaces/{workspace_id}/products-groups/{products_group_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteProductsGroup(productsGroupId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/products-groups/{products_group_id}", { path: { products_group_id: productsGroupId }, query: opts.query });
    }
    /**
     * getProductsGroup.
     * @method GET /v1/workspaces/{workspace_id}/products-groups/{products_group_id}
     * @remarks Any query params may be sent (none documented).
     */
    getProductsGroup(productsGroupId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/products-groups/{products_group_id}", { path: { products_group_id: productsGroupId }, query: opts.query });
    }
    /**
     * updateProductsGroup.
     * @method PUT /v1/workspaces/{workspace_id}/products-groups/{products_group_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateProductsGroup(productsGroupId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/products-groups/{products_group_id}", { path: { products_group_id: productsGroupId }, body, query: opts.query });
    }
    /**
     * listProductsGroups.
     * @method GET /v1/workspaces/{workspace_id}/products-groups
     * @remarks Documented query: filters (extra keys allowed).
     */
    listProductsGroups(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/products-groups", { query: opts.query });
    }
    /**
     * createProductsGroup.
     * @method POST /v1/workspaces/{workspace_id}/products-groups
     * @remarks Any query params may be sent (none documented).
     */
    createProductsGroup(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/products-groups", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_productsPrices.ts
  var ProductsPrices = class extends Resource {
    /**
     * deleteItem.
     * @method DELETE /v1/workspaces/{workspace_id}/products-prices/{products_price_id}/item/{item_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteItem(productsPriceId, itemId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/products-prices/{products_price_id}/item/{item_id}", { path: { products_price_id: productsPriceId, item_id: itemId }, query: opts.query });
    }
    /**
     * getProductPricesItemById.
     * @method GET /v1/workspaces/{workspace_id}/products-prices/{products_price_id}/item/{item_id}
     * @remarks Any query params may be sent (none documented).
     */
    getProductPricesItemById(productsPriceId, itemId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/products-prices/{products_price_id}/item/{item_id}", { path: { products_price_id: productsPriceId, item_id: itemId }, query: opts.query });
    }
    /**
     * putItem.
     * @method PUT /v1/workspaces/{workspace_id}/products-prices/{products_price_id}/item/{item_id}
     * @remarks Any query params may be sent (none documented).
     */
    putItem(productsPriceId, itemId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/products-prices/{products_price_id}/item/{item_id}", { path: { products_price_id: productsPriceId, item_id: itemId }, body, query: opts.query });
    }
    /**
     * getAllProductPricesItems.
     * @method GET /v1/workspaces/{workspace_id}/products-prices/{products_price_id}/item
     * @remarks Documented query: filters (extra keys allowed).
     */
    getAllProductPricesItems(productsPriceId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/products-prices/{products_price_id}/item", { path: { products_price_id: productsPriceId }, query: opts.query });
    }
    /**
     * item.
     * @method POST /v1/workspaces/{workspace_id}/products-prices/{products_price_id}/item
     * @remarks Any query params may be sent (none documented).
     */
    item(productsPriceId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/products-prices/{products_price_id}/item", { path: { products_price_id: productsPriceId }, body, query: opts.query });
    }
    /**
     * deleteProductsPrice.
     * @method DELETE /v1/workspaces/{workspace_id}/products-prices/{products_price_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteProductsPrice(productsPriceId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/products-prices/{products_price_id}", { path: { products_price_id: productsPriceId }, query: opts.query });
    }
    /**
     * getProductsPrice.
     * @method GET /v1/workspaces/{workspace_id}/products-prices/{products_price_id}
     * @remarks Any query params may be sent (none documented).
     */
    getProductsPrice(productsPriceId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/products-prices/{products_price_id}", { path: { products_price_id: productsPriceId }, query: opts.query });
    }
    /**
     * updateProductsPrice.
     * @method PUT /v1/workspaces/{workspace_id}/products-prices/{products_price_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateProductsPrice(productsPriceId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/products-prices/{products_price_id}", { path: { products_price_id: productsPriceId }, body, query: opts.query });
    }
    /**
     * listProductsPrices.
     * @method GET /v1/workspaces/{workspace_id}/products-prices
     * @remarks Documented query: filters (extra keys allowed).
     */
    listProductsPrices(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/products-prices", { query: opts.query });
    }
    /**
     * createProductsPrice.
     * @method POST /v1/workspaces/{workspace_id}/products-prices
     * @remarks Any query params may be sent (none documented).
     */
    createProductsPrice(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/products-prices", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_queueItems.ts
  var QueueItems = class extends Resource {
    /**
     * Delete queue item by key.
     * @method DELETE /v1/workspaces/{workspace_id}/queues/{queue_id}/queue-items/key/{key}
     * @remarks Any query params may be sent (none documented).
     */
    deleteKey(queueId, key, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/queues/{queue_id}/queue-items/key/{key}", { path: { queue_id: queueId, key }, query: opts.query });
    }
    /**
     * Get queue item by id.
     * @method GET /v1/workspaces/{workspace_id}/queues/{queue_id}/queue-items/{id}
     * @remarks Any query params may be sent (none documented).
     */
    getQueueItem(queueId, id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/queues/{queue_id}/queue-items/{id}", { path: { queue_id: queueId, id }, query: opts.query });
    }
  };

  // src/sdk/resources/gen_queues.ts
  var Queues = class extends Resource {
    /**
     * Delete queue item.
     * @method DELETE /v1/workspaces/{workspace_id}/queues/{queue_id}/queue-items/{queue_item_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteQueueItems(queueId, queueItemId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/queues/{queue_id}/queue-items/{queue_item_id}", { path: { queue_id: queueId, queue_item_id: queueItemId }, query: opts.query });
    }
    /**
     * Update queue item by id.
     * @method PUT /v1/workspaces/{workspace_id}/queues/{queue_id}/queue-items/{queue_item_id}
     * @remarks Any query params may be sent (none documented).
     */
    putQueueItems(queueId, queueItemId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/queues/{queue_id}/queue-items/{queue_item_id}", { path: { queue_id: queueId, queue_item_id: queueItemId }, body, query: opts.query });
    }
    /**
     * Clear queue by id.
     * @method GET /v1/workspaces/{workspace_id}/queues/{id}/clear
     * @remarks Any query params may be sent (none documented).
     */
    getClear(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/queues/{id}/clear", { path: { id }, query: opts.query });
    }
    /**
     * Get all queue logs.
     * @method GET /v1/workspaces/{workspace_id}/queues/{id}/logs
     * @remarks Documented query: page, limit, order, direction_order, key, populate (extra keys allowed).
     */
    getLogs(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/queues/{id}/logs", { path: { id }, query: opts.query });
    }
    /**
     * Get queue by id.
     * @method GET /v1/workspaces/{workspace_id}/queues/{id}/next
     * @remarks Any query params may be sent (none documented).
     */
    getNext(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/queues/{id}/next", { path: { id }, query: opts.query });
    }
    /**
     * Get all queue items.
     * @method GET /v1/workspaces/{workspace_id}/queues/{queue_id}/queue-items
     * @remarks Documented query: filters, page, limit, order, direction_order, key, populate (extra keys allowed).
     */
    getQueueItems(queueId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/queues/{queue_id}/queue-items", { path: { queue_id: queueId }, query: opts.query });
    }
    /**
     * Create a queue item.
     * @method POST /v1/workspaces/{workspace_id}/queues/{queue_id}/queue-items
     * @remarks Any query params may be sent (none documented).
     */
    queueItems(queueId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/queues/{queue_id}/queue-items", { path: { queue_id: queueId }, body, query: opts.query });
    }
    /**
     * Reset queue by id.
     * @method GET /v1/workspaces/{workspace_id}/queues/{id}/reset
     * @remarks Any query params may be sent (none documented).
     */
    getReset(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/queues/{id}/reset", { path: { id }, query: opts.query });
    }
    /**
     * Sync queue by id.
     * @method GET /v1/workspaces/{workspace_id}/queues/{queue_id}/sync
     * @remarks Any query params may be sent (none documented).
     */
    sync(queueId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/queues/{queue_id}/sync", { path: { queue_id: queueId }, query: opts.query });
    }
    /**
     * Delete queue by id.
     * @method DELETE /v1/workspaces/{workspace_id}/queues/{queue_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteQueue(queueId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/queues/{queue_id}", { path: { queue_id: queueId }, query: opts.query });
    }
    /**
     * Get queue by id.
     * @method GET /v1/workspaces/{workspace_id}/queues/{queue_id}
     * @remarks Any query params may be sent (none documented).
     */
    getQueue(queueId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/queues/{queue_id}", { path: { queue_id: queueId }, query: opts.query });
    }
    /**
     * Update queue by id.
     * @method PUT /v1/workspaces/{workspace_id}/queues/{queue_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateQueue(queueId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/queues/{queue_id}", { path: { queue_id: queueId }, body, query: opts.query });
    }
    /**
     * Get all queues.
     * @method GET /v1/workspaces/{workspace_id}/queues
     * @remarks Documented query: filters, page, limit, order, direction_order, name, populate (extra keys allowed).
     */
    listQueues(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/queues", { query: opts.query });
    }
    /**
     * Create a queue.
     * @method POST /v1/workspaces/{workspace_id}/queues
     * @remarks Any query params may be sent (none documented).
     */
    createQueue(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/queues", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_quotation.ts
  var Quotation = class extends Resource {
    /**
     * publish.
     * @method POST /v1/workspaces/{workspace_id}/quotation/publish
     * @remarks Any query params may be sent (none documented).
     */
    publish(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/quotation/publish", { body, query: opts.query });
    }
    /**
     * getQuotationAll.
     * @method GET /v1/workspaces/{workspace_id}/quotation
     * @remarks Documented query: filters (extra keys allowed).
     */
    getQuotationAll(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/quotation", { query: opts.query });
    }
    /**
     * getQuotationPublic.
     * @method GET /v1/quotation
     * @remarks Any query params may be sent (none documented).
     */
    getQuotationPublic(opts = {}) {
      return this.http.get("/v1/quotation", { query: opts.query });
    }
    /**
     * patchQuotation.
     * @method PATCH /v1/quotation
     * @remarks Any query params may be sent (none documented).
     */
    patchQuotation(body, opts = {}) {
      return this.http.patch("/v1/quotation", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_reasons.ts
  var Reasons = class extends Resource {
    /**
     * Get reason by id.
     * @method GET /v1/workspaces/{workspace_id}/reasons/{reason_id}
     * @remarks Any query params may be sent (none documented).
     */
    getReason(reasonId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reasons/{reason_id}", { path: { reason_id: reasonId }, query: opts.query });
    }
    /**
     * Update reason by id.
     * @method PUT /v1/workspaces/{workspace_id}/reasons/{reason_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateReason(reasonId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/reasons/{reason_id}", { path: { reason_id: reasonId }, body, query: opts.query });
    }
    /**
     * Get all reasons.
     * @method GET /v1/workspaces/{workspace_id}/reasons
     * @remarks Documented query: filters, page, limit, order, direction_order, name, sector, type, populate (extra keys allowed).
     */
    listReasons(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reasons", { query: opts.query });
    }
    /**
     * Create a reason.
     * @method POST /v1/workspaces/{workspace_id}/reasons
     * @remarks Any query params may be sent (none documented).
     */
    createReason(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/reasons", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_removeUser.ts
  var RemoveUser = class extends Resource {
    /**
     * deleteRemoveUser.
     * @method DELETE /v1/workspaces/{workspace_id}/remove-user
     * @remarks Documented query: id, data (extra keys allowed).
     */
    deleteRemoveUser(opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/remove-user", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_reports.ts
  var Reports = class extends Resource {
    /**
     * getCrmListMetrics.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/board/{board_id}/list
     * @remarks Documented query: filters (extra keys allowed).
     */
    getCrmListMetrics(boardId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/board/{board_id}/list", { path: { board_id: boardId }, query: opts.query });
    }
    /**
     * getBoard.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/board/{board_id}
     * @remarks Documented query: filters (extra keys allowed).
     */
    getBoard(boardId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/board/{board_id}", { path: { board_id: boardId }, query: opts.query });
    }
    /**
     * getCard.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/card/{card_id}
     * @remarks Documented query: filters (extra keys allowed).
     */
    getCard(cardId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/card/{card_id}", { path: { card_id: cardId }, query: opts.query });
    }
    /**
     * getChurn.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/cscx-metrics/churn
     * @remarks Documented query: filters (extra keys allowed).
     */
    getChurn(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/cscx-metrics/churn", { query: opts.query });
    }
    /**
     * getOrganizations.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/cscx-metrics/organizations
     * @remarks Documented query: filters (extra keys allowed).
     */
    getOrganizations(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/cscx-metrics/organizations", { query: opts.query });
    }
    /**
     * getUser.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/user/{user_id}
     * @remarks Documented query: filters (extra keys allowed).
     */
    getUser(userId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/user/{user_id}", { path: { user_id: userId }, query: opts.query });
    }
    /**
     * getCount.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/users/count
     * @remarks Documented query: filters (extra keys allowed).
     */
    getCount(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/users/count", { query: opts.query });
    }
    /**
     * getCscxMetrics.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/cscx-metrics
     * @remarks Documented query: filters (extra keys allowed).
     */
    getCscxMetrics(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/cscx-metrics", { query: opts.query });
    }
    /**
     * getUsers.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/users
     * @remarks Documented query: filters (extra keys allowed).
     */
    getUsers(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/metrics/users", { query: opts.query });
    }
    /**
     * getCounts.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/nps/{np_id}/counts
     * @remarks Any query params may be sent (none documented).
     */
    getCounts(npId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/nps/{np_id}/counts", { path: { np_id: npId }, query: opts.query });
    }
    /**
     * getNpsSummary.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/nps/{np_id}/summary
     * @remarks Any query params may be sent (none documented).
     */
    getNpsSummary(npId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/nps/{np_id}/summary", { path: { np_id: npId }, query: opts.query });
    }
    /**
     * getDacList.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/plans/dac/list
     * @remarks Documented query: filters (extra keys allowed).
     */
    getDacList(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/plans/dac/list", { query: opts.query });
    }
    /**
     * getCampaignEmailsStatsList.
     * @method POST /v1/workspaces/{workspace_id}/reports/alloy-reports/segmentations/email-stats/list
     * @remarks Documented query: filters (extra keys allowed).
     */
    getCampaignEmailsStatsList(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/reports/alloy-reports/segmentations/email-stats/list", { body, query: opts.query });
    }
    /**
     * getCampaignMessagesStatsList.
     * @method POST /v1/workspaces/{workspace_id}/reports/alloy-reports/segmentations/message-stats/list
     * @remarks Documented query: filters (extra keys allowed).
     */
    getCampaignMessagesStatsList(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/reports/alloy-reports/segmentations/message-stats/list", { body, query: opts.query });
    }
    /**
     * Get all desahboard users counters by me.
     * @method GET /v1/workspaces/{workspace_id}/reports/services/dashboard/users-counters/me
     * @remarks Documented query: page, limit, user, sector, connection, is_available, is_available_to_call (extra keys allowed).
     */
    ReportsController_getAllDashBoardUsersCountersMe_v1(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/services/dashboard/users-counters/me", { query: opts.query });
    }
    /**
     * getMonthlySessionsStates.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/admin/monthly-sessions-states
     * @remarks Documented query: filters (extra keys allowed).
     */
    getMonthlySessionsStates(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/admin/monthly-sessions-states", { query: opts.query });
    }
    /**
     * getUsersUsingCrm.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/admin/users-using-crm
     * @remarks Documented query: filters (extra keys allowed).
     */
    getUsersUsingCrm(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/admin/users-using-crm", { query: opts.query });
    }
    /**
     * getUsersUsingServices.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/admin/users-using-services
     * @remarks Documented query: filters (extra keys allowed).
     */
    getUsersUsingServices(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/admin/users-using-services", { query: opts.query });
    }
    /**
     * getEmailsFunnel.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/emails-funnel
     * @remarks Documented query: filters (extra keys allowed).
     */
    getEmailsFunnel(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/emails-funnel", { query: opts.query });
    }
    /**
     * getEmailsHistograms.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/emails-histograms
     * @remarks Documented query: filters (extra keys allowed).
     */
    getEmailsHistograms(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/emails-histograms", { query: opts.query });
    }
    /**
     * getEmailsKpi.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/emails-kpi
     * @remarks Documented query: filters (extra keys allowed).
     */
    getEmailsKpi(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/emails-kpi", { query: opts.query });
    }
    /**
     * getEmailsList.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/emails-list
     * @remarks Documented query: filters (extra keys allowed).
     */
    getEmailsList(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/emails-list", { query: opts.query });
    }
    /**
     * getEmailsReadHeatmap.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/emails-read-heatmap
     * @remarks Documented query: filters (extra keys allowed).
     */
    getEmailsReadHeatmap(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/emails-read-heatmap", { query: opts.query });
    }
    /**
     * getEmailsTimeline.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/emails-timeline
     * @remarks Documented query: filters (extra keys allowed).
     */
    getEmailsTimeline(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/emails-timeline", { query: opts.query });
    }
    /**
     * getMessagesFunnel.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/messages-funnel
     * @remarks Documented query: filters (extra keys allowed).
     */
    getMessagesFunnel(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/messages-funnel", { query: opts.query });
    }
    /**
     * getMessagesHistograms.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/messages-histograms
     * @remarks Documented query: filters (extra keys allowed).
     */
    getMessagesHistograms(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/messages-histograms", { query: opts.query });
    }
    /**
     * getMessagesKpi.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/messages-kpi
     * @remarks Documented query: filters (extra keys allowed).
     */
    getMessagesKpi(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/messages-kpi", { query: opts.query });
    }
    /**
     * getMessagesList.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/messages-list
     * @remarks Documented query: filters (extra keys allowed).
     */
    getMessagesList(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/messages-list", { query: opts.query });
    }
    /**
     * getMessagesReadHeatmap.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/messages-read-heatmap
     * @remarks Documented query: filters (extra keys allowed).
     */
    getMessagesReadHeatmap(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/messages-read-heatmap", { query: opts.query });
    }
    /**
     * getMessagesTimeline.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/messages-timeline
     * @remarks Documented query: filters (extra keys allowed).
     */
    getMessagesTimeline(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/campaigns/messages-timeline", { query: opts.query });
    }
    /**
     * getCampaignCardsCount.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/campaign-cards-count
     * @remarks Documented query: filters (extra keys allowed).
     */
    getCampaignCardsCount(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/campaign-cards-count", { query: opts.query });
    }
    /**
     * getCardsCount.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/cards-count
     * @remarks Documented query: filters (extra keys allowed).
     */
    getCardsCount(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/cards-count", { query: opts.query });
    }
    /**
     * getCardsCountUsers.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/cards-count-users
     * @remarks Documented query: filters (extra keys allowed).
     */
    getCardsCountUsers(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/cards-count-users", { query: opts.query });
    }
    /**
     * getCardsCountersByDay.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/cards-counters-by-day
     * @remarks Documented query: filters (extra keys allowed).
     */
    getCardsCountersByDay(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/cards-counters-by-day", { query: opts.query });
    }
    /**
     * getCardsUserMetrics.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/cards-user-metrics
     * @remarks Documented query: filters (extra keys allowed).
     */
    getCardsUserMetrics(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/cards-user-metrics", { query: opts.query });
    }
    /**
     * getFunnelCount.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/funnel-count
     * @remarks Documented query: filters (extra keys allowed).
     */
    getFunnelCount(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/funnel-count", { query: opts.query });
    }
    /**
     * getRepCardsMonthlyHistory.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/monthly-history
     * @remarks Documented query: filters (extra keys allowed).
     */
    getRepCardsMonthlyHistory(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/monthly-history", { query: opts.query });
    }
    /**
     * getRepCardsReasons.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/reasons
     * @remarks Documented query: filters (extra keys allowed).
     */
    getRepCardsReasons(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/reasons", { query: opts.query });
    }
    /**
     * getSourceCardsCount.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/source-cards-count
     * @remarks Documented query: filters (extra keys allowed).
     */
    getSourceCardsCount(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/source-cards-count", { query: opts.query });
    }
    /**
     * getTotalSalesBySectorUser.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/cards/total-sales-by-sector-user
     * @remarks Documented query: filters (extra keys allowed).
     */
    getTotalSalesBySectorUser(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/cards/total-sales-by-sector-user", { query: opts.query });
    }
    /**
     * getByModelToday.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/completions/by-model-today
     * @remarks Documented query: filters (extra keys allowed).
     */
    getByModelToday(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/completions/by-model-today", { query: opts.query });
    }
    /**
     * getRepFlowsExecutionsMonthlyHistory.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/flows-executions/monthly-history
     * @remarks Documented query: filters (extra keys allowed).
     */
    getRepFlowsExecutionsMonthlyHistory(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/flows-executions/monthly-history", { query: opts.query });
    }
    /**
     * getNodesCount.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/flows-executions/nodes-count
     * @remarks Documented query: filters (extra keys allowed).
     */
    getNodesCount(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/flows-executions/nodes-count", { query: opts.query });
    }
    /**
     * getRepFlowsExecutionsResume.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/flows-executions/resume
     * @remarks Documented query: filters (extra keys allowed).
     */
    getRepFlowsExecutionsResume(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/flows-executions/resume", { query: opts.query });
    }
    /**
     * getDomainRankOverview.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/marketing/domain-rank-overview
     * @remarks Any query params may be sent (none documented).
     */
    getDomainRankOverview(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/marketing/domain-rank-overview", { query: opts.query });
    }
    /**
     * getInstantPages.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/marketing/instant-pages
     * @remarks Any query params may be sent (none documented).
     */
    getInstantPages(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/marketing/instant-pages", { query: opts.query });
    }
    /**
     * getScreenshot.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/marketing/screenshot
     * @remarks Any query params may be sent (none documented).
     */
    getScreenshot(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/marketing/screenshot", { query: opts.query });
    }
    /**
     * getRepPersonsTags.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/persons/tags
     * @remarks Documented query: filters (extra keys allowed).
     */
    getRepPersonsTags(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/persons/tags", { query: opts.query });
    }
    /**
     * getCharges.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/plans/charges
     * @remarks Documented query: filters (extra keys allowed).
     */
    getCharges(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/plans/charges", { query: opts.query });
    }
    /**
     * getChargesPartners.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/plans/charges-partners
     * @remarks Documented query: filters (extra keys allowed).
     */
    getChargesPartners(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/plans/charges-partners", { query: opts.query });
    }
    /**
     * getChargesV2.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/plans/charges-v2
     * @remarks Documented query: filters (extra keys allowed).
     */
    getChargesV2(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/plans/charges-v2", { query: opts.query });
    }
    /**
     * getChargesV3.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/plans/charges-v3
     * @remarks Documented query: filters (extra keys allowed).
     */
    getChargesV3(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/plans/charges-v3", { query: opts.query });
    }
    /**
     * getHistory.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/plans/history
     * @remarks Documented query: filters (extra keys allowed).
     */
    getHistory(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/plans/history", { query: opts.query });
    }
    /**
     * getPrices.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/plans/prices
     * @remarks Documented query: filters (extra keys allowed).
     */
    getPrices(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/plans/prices", { query: opts.query });
    }
    /**
     * getPlanResume.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/plans/resume
     * @remarks Documented query: filters (extra keys allowed).
     */
    getPlanResume(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/plans/resume", { query: opts.query });
    }
    /**
     * count.
     * @method POST /v1/workspaces/{workspace_id}/reports/alloy-reports/segmentations/count
     * @remarks Any query params may be sent (none documented).
     */
    count(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/reports/alloy-reports/segmentations/count", { body, query: opts.query });
    }
    /**
     * emailStats.
     * @method POST /v1/workspaces/{workspace_id}/reports/alloy-reports/segmentations/email-stats
     * @remarks Any query params may be sent (none documented).
     */
    emailStats(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/reports/alloy-reports/segmentations/email-stats", { body, query: opts.query });
    }
    /**
     * getDymamicSegmentations.
     * @method POST /v1/workspaces/{workspace_id}/reports/alloy-reports/segmentations/list
     * @remarks Any query params may be sent (none documented).
     */
    getDymamicSegmentations(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/reports/alloy-reports/segmentations/list", { body, query: opts.query });
    }
    /**
     * messageStats.
     * @method POST /v1/workspaces/{workspace_id}/reports/alloy-reports/segmentations/message-stats
     * @remarks Any query params may be sent (none documented).
     */
    messageStats(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/reports/alloy-reports/segmentations/message-stats", { body, query: opts.query });
    }
    /**
     * getQueryBuilder.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/segmentations/query-builder
     * @remarks Any query params may be sent (none documented).
     */
    getQueryBuilder(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/segmentations/query-builder", { query: opts.query });
    }
    /**
     * getAttendanceTotalTime.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/services/attendance-total-time
     * @remarks Documented query: filters (extra keys allowed).
     */
    getAttendanceTotalTime(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/services/attendance-total-time", { query: opts.query });
    }
    /**
     * getBotAverageTime.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/services/bot-average-time
     * @remarks Documented query: filters (extra keys allowed).
     */
    getBotAverageTime(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/services/bot-average-time", { query: opts.query });
    }
    /**
     * getBotAverageTimeMonth.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/services/bot-average-time-month
     * @remarks Documented query: filters (extra keys allowed).
     */
    getBotAverageTimeMonth(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/services/bot-average-time-month", { query: opts.query });
    }
    /**
     * getBotAverageTimeMonthSectors.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/services/bot-average-time-month-sectors
     * @remarks Documented query: filters (extra keys allowed).
     */
    getBotAverageTimeMonthSectors(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/services/bot-average-time-month-sectors", { query: opts.query });
    }
    /**
     * getFirstResponseTime.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/services/first-response-time
     * @remarks Documented query: filters (extra keys allowed).
     */
    getFirstResponseTime(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/services/first-response-time", { query: opts.query });
    }
    /**
     * getRepServicesMonthlyHistory.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/services/monthly-history
     * @remarks Documented query: filters (extra keys allowed).
     */
    getRepServicesMonthlyHistory(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/services/monthly-history", { query: opts.query });
    }
    /**
     * getQueueAverageTime.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/services/queue-average-time
     * @remarks Documented query: filters (extra keys allowed).
     */
    getQueueAverageTime(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/services/queue-average-time", { query: opts.query });
    }
    /**
     * getQueueAverageTimeMonth.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/services/queue-average-time-month
     * @remarks Documented query: filters (extra keys allowed).
     */
    getQueueAverageTimeMonth(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/services/queue-average-time-month", { query: opts.query });
    }
    /**
     * getQueueAverageTimeMonthSectors.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/services/queue-average-time-month-sectors
     * @remarks Documented query: filters (extra keys allowed).
     */
    getQueueAverageTimeMonthSectors(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/services/queue-average-time-month-sectors", { query: opts.query });
    }
    /**
     * getRepServicesReasons.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/services/reasons
     * @remarks Documented query: filters (extra keys allowed).
     */
    getRepServicesReasons(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/services/reasons", { query: opts.query });
    }
    /**
     * getServicesConsolidatedMetrics.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/services/services-consolidated-metrics
     * @remarks Documented query: query (extra keys allowed).
     */
    getServicesConsolidatedMetrics(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/services/services-consolidated-metrics", { query: opts.query });
    }
    /**
     * getServicesCount.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/services/services-count
     * @remarks Documented query: filters (extra keys allowed).
     */
    getServicesCount(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/services/services-count", { query: opts.query });
    }
    /**
     * getServicesUserMetrics.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/services/services-user-metrics
     * @remarks Documented query: filters (extra keys allowed).
     */
    getServicesUserMetrics(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/services/services-user-metrics", { query: opts.query });
    }
    /**
     * getServicesUserWeeklyCount.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/services/services-user-weekly-count
     * @remarks Documented query: filters (extra keys allowed).
     */
    getServicesUserWeeklyCount(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/services/services-user-weekly-count", { query: opts.query });
    }
    /**
     * getRepServicesTags.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/services/tags
     * @remarks Documented query: filters (extra keys allowed).
     */
    getRepServicesTags(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/services/tags", { query: opts.query });
    }
    /**
     * getRepSessionsMonthlyHistory.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/sessions/monthly-history
     * @remarks Documented query: filters (extra keys allowed).
     */
    getRepSessionsMonthlyHistory(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/sessions/monthly-history", { query: opts.query });
    }
    /**
     * getMonthlyTwoway.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/sessions/monthly-twoway
     * @remarks Documented query: filters (extra keys allowed).
     */
    getMonthlyTwoway(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/sessions/monthly-twoway", { query: opts.query });
    }
    /**
     * getRepSessionsResume.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/sessions/resume
     * @remarks Any query params may be sent (none documented).
     */
    getRepSessionsResume(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/sessions/resume", { query: opts.query });
    }
    /**
     * getUserIndicators.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/tasks/user-indicators
     * @remarks Documented query: filters (extra keys allowed).
     */
    getUserIndicators(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/tasks/user-indicators", { query: opts.query });
    }
    /**
     * getUserSectorIndicators.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/tasks/user-sector-indicators
     * @remarks Documented query: filters (extra keys allowed).
     */
    getUserSectorIndicators(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/tasks/user-sector-indicators", { query: opts.query });
    }
    /**
     * getUsersSummary.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/tasks/users-summary
     * @remarks Documented query: filters (extra keys allowed).
     */
    getUsersSummary(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/tasks/users-summary", { query: opts.query });
    }
    /**
     * Get all consolidated sectors users.
     * @method GET /v1/workspaces/{workspace_id}/reports/services/consolidated/sectors-users
     * @remarks Documented query: query, page, limit, connection, user, sector, start_date, end_date (extra keys allowed).
     */
    getSectorsUsers(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/services/consolidated/sectors-users", { query: opts.query });
    }
    /**
     * Get all dashboard counters.
     * @method GET /v1/workspaces/{workspace_id}/reports/services/dashboard/counters
     * @remarks Documented query: filters, user, sector, connection (extra keys allowed).
     */
    getCounters(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/services/dashboard/counters", { query: opts.query });
    }
    /**
     * Get all dashboard users counters.
     * @method GET /v1/workspaces/{workspace_id}/reports/services/dashboard/users-counters
     * @remarks Documented query: page, limit, user, sector, connection, is_available, is_available_to_call (extra keys allowed).
     */
    getUsersCounters(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/services/dashboard/users-counters", { query: opts.query });
    }
    /**
     * Get all reports from me.
     * @method GET /v1/workspaces/{workspace_id}/reports/services/summary/me
     * @remarks Documented query: page, limit, connection, user, sector, start_date, end_date, field_date (extra keys allowed).
     */
    ReportsController_getAllMe_v1(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/services/summary/me", { query: opts.query });
    }
    /**
     * getSectors.
     * @method GET /v1/workspaces/{workspace_id}/reports/alloy-reports/sectors
     * @remarks Documented query: filters (extra keys allowed).
     */
    getSectors(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/alloy-reports/sectors", { query: opts.query });
    }
    /**
     * Get csat.
     * @method GET /v1/workspaces/{workspace_id}/reports/services/csat
     * @remarks Documented query: filters, page, limit, connection, user, sector, start_date, end_date, field_date (extra keys allowed).
     */
    getCsat(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/services/csat", { query: opts.query });
    }
    /**
     * Get all reports.
     * @method GET /v1/workspaces/{workspace_id}/reports/services/summary
     * @remarks Documented query: filters, page, limit, connection, user, sector, start_date, end_date, field_date (extra keys allowed).
     */
    getReports(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/services/summary", { query: opts.query });
    }
    /**
     * Get services counters by tag.
     * @method GET /v1/workspaces/{workspace_id}/reports/services/tags
     * @remarks Documented query: page, limit, connection, user, sector, start_date, end_date, field_date (extra keys allowed).
     */
    ReportsController_getServicesCountersByTag_v1(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/services/tags", { query: opts.query });
    }
    /**
     * Get all messages templates report.
     * @method GET /v1/workspaces/{workspace_id}/reports/templates/{id}
     * @remarks Documented query: page, limit, order, direction_order, to, from, person, campaign, populate, created_at (extra keys allowed).
     */
    getTemplate(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/reports/templates/{id}", { path: { id }, query: opts.query });
    }
  };

  // src/sdk/resources/gen_resetPlans.ts
  var ResetPlans = class extends Resource {
    /**
     * updateResetPlan.
     * @method PUT /v1/workspaces/{workspace_id}/reset-plans
     * @remarks Any query params may be sent (none documented).
     */
    updateResetPlan(body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/reset-plans", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_root.ts
  var Root = class extends Resource {
    /**
     * Get all workspaces.
     * @method GET /v1/workspaces
     * @remarks Documented query: filters, page, limit, order, direction_order, name, search, type, plan_type, owner, partner, is_blocked, is_deleted, pending_plan, auto_invoice, created_at, updated_at (extra keys allowed).
     */
    getWorkspaces(opts = {}) {
      return this.http.get("/v1/workspaces", { query: opts.query });
    }
    /**
     * workspaces.
     * @method POST /v1/workspaces
     * @remarks Any query params may be sent (none documented).
     */
    workspaces(body, opts = {}) {
      return this.http.post("/v1/workspaces", { body, query: opts.query });
    }
    /**
     * deleteRoot.
     * @method DELETE /v1/workspaces/{workspace_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteRoot(opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}", { query: opts.query });
    }
    /**
     * Get workspace by id.
     * @method GET /v1/workspaces/{workspace_id}
     * @remarks Any query params may be sent (none documented).
     */
    getRoot(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}", { query: opts.query });
    }
    /**
     * putRoot.
     * @method PUT /v1/workspaces/{workspace_id}
     * @remarks Any query params may be sent (none documented).
     */
    putRoot(body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_scripts.ts
  var Scripts = class extends Resource {
    /**
     * scriptExecution.
     * @method POST /v1/workspaces/{workspace_id}/scripts/{script_id}/execution
     * @remarks Any query params may be sent (none documented).
     */
    scriptExecution(scriptId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/scripts/{script_id}/execution", { path: { script_id: scriptId }, body, query: opts.query });
    }
    /**
     * deleteScript.
     * @method DELETE /v1/workspaces/{workspace_id}/scripts/{script_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteScript(scriptId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/scripts/{script_id}", { path: { script_id: scriptId }, query: opts.query });
    }
    /**
     * getScript.
     * @method GET /v1/workspaces/{workspace_id}/scripts/{script_id}
     * @remarks Any query params may be sent (none documented).
     */
    getScript(scriptId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/scripts/{script_id}", { path: { script_id: scriptId }, query: opts.query });
    }
    /**
     * updateScript.
     * @method PUT /v1/workspaces/{workspace_id}/scripts/{script_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateScript(scriptId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/scripts/{script_id}", { path: { script_id: scriptId }, body, query: opts.query });
    }
    /**
     * runCode.
     * @method POST /v1/workspaces/{workspace_id}/scripts/execution
     * @remarks Any query params may be sent (none documented).
     */
    runCode(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/scripts/execution", { body, query: opts.query });
    }
    /**
     * listScripts.
     * @method GET /v1/workspaces/{workspace_id}/scripts
     * @remarks Documented query: filters (extra keys allowed).
     */
    listScripts(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/scripts", { query: opts.query });
    }
    /**
     * createScript.
     * @method POST /v1/workspaces/{workspace_id}/scripts
     * @remarks Any query params may be sent (none documented).
     */
    createScript(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/scripts", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_search.ts
  var Search = class extends Resource {
    /**
     * getPersonsWithOrganization.
     * @method GET /v1/workspaces/{workspace_id}/search/persons-with-organization
     * @remarks Documented query: filters (extra keys allowed).
     */
    getPersonsWithOrganization(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/search/persons-with-organization", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_sectors.ts
  var Sectors = class extends Resource {
    /**
     * Remove user from sector by id.
     * @method DELETE /v1/workspaces/{workspace_id}/sectors/{id}/sectors-users/{sector_user_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteSectorsUsers(id, sectorUserId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/sectors/{id}/sectors-users/{sector_user_id}", { path: { id, sector_user_id: sectorUserId }, query: opts.query });
    }
    /**
     * Update sector users.
     * @method PUT /v1/workspaces/{workspace_id}/sectors/{id}/sectors-users/{sector_user_id}
     * @remarks Any query params may be sent (none documented).
     */
    putSectorsUsers(id, sectorUserId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/sectors/{id}/sectors-users/{sector_user_id}", { path: { id, sector_user_id: sectorUserId }, body, query: opts.query });
    }
    /**
     * Get all users from sector by id.
     * @method GET /v1/workspaces/{workspace_id}/sectors/{id}/sectors-users
     * @remarks Documented query: filters, page, limit, order, direction_order, name, email, is_attendant, role_type (extra keys allowed).
     * @remarks Returns the users that belong to the sector, not sectors.
     */
    getSectorsUsers(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/sectors/{id}/sectors-users", { path: { id }, query: opts.query });
    }
    /**
     * Add user to sector by id.
     * @method POST /v1/workspaces/{workspace_id}/sectors/{id}/sectors-users
     * @remarks Documented query: users (extra keys allowed).
     */
    sectorsUsers(id, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/sectors/{id}/sectors-users", { path: { id }, body, query: opts.query });
    }
    /**
     * W.I.P.
     * @method DELETE /v1/workspaces/{workspace_id}/sectors/{id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteSector(id, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/sectors/{id}", { path: { id }, query: opts.query });
    }
    /**
     * Get sector by id.
     * @method GET /v1/workspaces/{workspace_id}/sectors/{id}
     * @remarks Any query params may be sent (none documented).
     */
    getSector(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/sectors/{id}", { path: { id }, query: opts.query });
    }
    /**
     * Update sector by id.
     * @method PUT /v1/workspaces/{workspace_id}/sectors/{id}
     * @remarks Any query params may be sent (none documented).
     */
    updateSector(id, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/sectors/{id}", { path: { id }, body, query: opts.query });
    }
    /**
     * Get default sector.
     * @method GET /v1/workspaces/{workspace_id}/sectors/default-sector
     * @remarks Any query params may be sent (none documented).
     */
    getDefaultSector(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/sectors/default-sector", { query: opts.query });
    }
    /**
     * Get all sectors by user permission target.
     * @method GET /v1/workspaces/{workspace_id}/sectors/user/permission-target
     * @remarks Documented query: filters, page, limit, order, direction_order, name, permission_target (extra keys allowed).
     */
    getPermissionTarget(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/sectors/user/permission-target", { query: opts.query });
    }
    /**
     * Get all sectors by user.
     * @method GET /v1/workspaces/{workspace_id}/sectors/user
     * @remarks Documented query: filters, sector (extra keys allowed).
     * @remarks Returns the sectors the current user belongs to (not users), hence {@link Sector}.
     */
    getUser(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/sectors/user", { query: opts.query });
    }
    /**
     * Get all sectors by user id.
     * @method GET /v1/workspaces/{workspace_id}/sectors/users/{user_id}
     * @remarks Documented query: filters, sector (extra keys allowed).
     * @remarks Returns the sectors the given user belongs to (not the user), hence {@link Sector}.
     *          The API has no get-user-by-id route — resolve a user via the workspace member list.
     */
    getUserById(userId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/sectors/users/{user_id}", { path: { user_id: userId }, query: opts.query });
    }
    /**
     * listUsers.
     * @method GET /v1/workspaces/{workspace_id}/sectors/users
     * @remarks Any query params may be sent (none documented).
     * @remarks Returns workspace users (the `/sectors/users` collection), not sectors.
     */
    listUsers(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/sectors/users", { query: opts.query });
    }
    /**
     * Get all sectors.
     * @method GET /v1/workspaces/{workspace_id}/sectors
     * @remarks Documented query: filters, page, limit, order, direction_order, name, reduced, users_count, populate (extra keys allowed).
     */
    listSectors(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/sectors", { query: opts.query });
    }
    /**
     * Create a new sector.
     * @method POST /v1/workspaces/{workspace_id}/sectors
     * @remarks Any query params may be sent (none documented).
     */
    createSector(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/sectors", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_segmentations.ts
  var Segmentations = class extends Resource {
    /**
     * Delete segmentation by id.
     * @method DELETE /v1/workspaces/{workspace_id}/segmentations/{segmentation_id}/segmentations-items/{segmentations_item_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteSegmentationsItems(segmentationId, segmentationsItemId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/segmentations/{segmentation_id}/segmentations-items/{segmentations_item_id}", { path: { segmentation_id: segmentationId, segmentations_item_id: segmentationsItemId }, query: opts.query });
    }
    /**
     * Get segmentation by id.
     * @method GET /v1/workspaces/{workspace_id}/segmentations/{segmentation_id}/segmentations-items/{segmentations_item_id}
     * @remarks Documented query: filters, populate (extra keys allowed).
     */
    getSegmentationsItem(segmentationId, segmentationsItemId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/segmentations/{segmentation_id}/segmentations-items/{segmentations_item_id}", { path: { segmentation_id: segmentationId, segmentations_item_id: segmentationsItemId }, query: opts.query });
    }
    /**
     * Get all segmentations items.
     * @method GET /v1/workspaces/{workspace_id}/segmentations/{segmentation_id}/segmentations-items
     * @remarks Documented query: filters, page, limit, order, direction_order, populate, search (extra keys allowed).
     */
    getSegmentationsItems(segmentationId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/segmentations/{segmentation_id}/segmentations-items", { path: { segmentation_id: segmentationId }, query: opts.query });
    }
    /**
     * Create a new segmentation item.
     * @method POST /v1/workspaces/{workspace_id}/segmentations/{segmentation_id}/segmentations-items
     * @remarks Documented query: populate (extra keys allowed).
     */
    segmentationsItems(segmentationId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/segmentations/{segmentation_id}/segmentations-items", { path: { segmentation_id: segmentationId }, body, query: opts.query });
    }
    /**
     * Delete segmentation by id.
     * @method DELETE /v1/workspaces/{workspace_id}/segmentations/{segmentation_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteSegmentation(segmentationId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/segmentations/{segmentation_id}", { path: { segmentation_id: segmentationId }, query: opts.query });
    }
    /**
     * Get segmentation by id.
     * @method GET /v1/workspaces/{workspace_id}/segmentations/{segmentation_id}
     * @remarks Any query params may be sent (none documented).
     */
    getSegmentation(segmentationId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/segmentations/{segmentation_id}", { path: { segmentation_id: segmentationId }, query: opts.query });
    }
    /**
     * Update segmentation by id.
     * @method PUT /v1/workspaces/{workspace_id}/segmentations/{segmentation_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateSegmentation(segmentationId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/segmentations/{segmentation_id}", { path: { segmentation_id: segmentationId }, body, query: opts.query });
    }
    /**
     * Get all segmentations.
     * @method GET /v1/workspaces/{workspace_id}/segmentations
     * @remarks Documented query: filters, page, limit, order, direction_order, name, is_enable, type, result_type (extra keys allowed).
     */
    listSegmentations(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/segmentations", { query: opts.query });
    }
    /**
     * Create a new segmentation.
     * @method POST /v1/workspaces/{workspace_id}/segmentations
     * @remarks Any query params may be sent (none documented).
     */
    createSegmentation(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/segmentations", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_seoProjects.ts
  var SeoProjects = class extends Resource {
    /**
     * deleteSeoSites.
     * @method DELETE /v1/workspaces/{workspace_id}/seo-projects/{seo_project_id}/seo-sites/{seo_site_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteSeoSites(seoProjectId, seoSiteId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/seo-projects/{seo_project_id}/seo-sites/{seo_site_id}", { path: { seo_project_id: seoProjectId, seo_site_id: seoSiteId }, query: opts.query });
    }
    /**
     * getSeoSite.
     * @method GET /v1/workspaces/{workspace_id}/seo-projects/{seo_project_id}/seo-sites/{seo_site_id}
     * @remarks Any query params may be sent (none documented).
     */
    getSeoSite(seoProjectId, seoSiteId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/seo-projects/{seo_project_id}/seo-sites/{seo_site_id}", { path: { seo_project_id: seoProjectId, seo_site_id: seoSiteId }, query: opts.query });
    }
    /**
     * putSeoSites.
     * @method PUT /v1/workspaces/{workspace_id}/seo-projects/{seo_project_id}/seo-sites/{seo_site_id}
     * @remarks Any query params may be sent (none documented).
     */
    putSeoSites(seoProjectId, seoSiteId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/seo-projects/{seo_project_id}/seo-sites/{seo_site_id}", { path: { seo_project_id: seoProjectId, seo_site_id: seoSiteId }, body, query: opts.query });
    }
    /**
     * getSeoSites.
     * @method GET /v1/workspaces/{workspace_id}/seo-projects/{seo_project_id}/seo-sites
     * @remarks Documented query: filters (extra keys allowed).
     */
    getSeoSites(seoProjectId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/seo-projects/{seo_project_id}/seo-sites", { path: { seo_project_id: seoProjectId }, query: opts.query });
    }
    /**
     * seoSites.
     * @method POST /v1/workspaces/{workspace_id}/seo-projects/{seo_project_id}/seo-sites
     * @remarks Any query params may be sent (none documented).
     */
    seoSites(seoProjectId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/seo-projects/{seo_project_id}/seo-sites", { path: { seo_project_id: seoProjectId }, body, query: opts.query });
    }
    /**
     * deleteSeoProject.
     * @method DELETE /v1/workspaces/{workspace_id}/seo-projects/{seo_project_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteSeoProject(seoProjectId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/seo-projects/{seo_project_id}", { path: { seo_project_id: seoProjectId }, query: opts.query });
    }
    /**
     * getSeoProject.
     * @method GET /v1/workspaces/{workspace_id}/seo-projects/{seo_project_id}
     * @remarks Any query params may be sent (none documented).
     */
    getSeoProject(seoProjectId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/seo-projects/{seo_project_id}", { path: { seo_project_id: seoProjectId }, query: opts.query });
    }
    /**
     * updateSeoProject.
     * @method PUT /v1/workspaces/{workspace_id}/seo-projects/{seo_project_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateSeoProject(seoProjectId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/seo-projects/{seo_project_id}", { path: { seo_project_id: seoProjectId }, body, query: opts.query });
    }
    /**
     * getCheckIfExists.
     * @method GET /v1/workspaces/{workspace_id}/seo-projects/check-if-exists
     * @remarks Any query params may be sent (none documented).
     */
    getCheckIfExists(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/seo-projects/check-if-exists", { query: opts.query });
    }
    /**
     * listSeoProjects.
     * @method GET /v1/workspaces/{workspace_id}/seo-projects
     * @remarks Documented query: filters (extra keys allowed).
     */
    listSeoProjects(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/seo-projects", { query: opts.query });
    }
    /**
     * createSeoProject.
     * @method POST /v1/workspaces/{workspace_id}/seo-projects
     * @remarks Any query params may be sent (none documented).
     */
    createSeoProject(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/seo-projects", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_services.ts
  var Services = class extends Resource {
    /**
     * Get all messages by service and connection.
     * @method GET /v1/workspaces/{workspace_id}/services/{service_id}/connection/{connection_id}/messages
     * @remarks Documented query: filters, page, limit, order, direction_order, user, body, type, key, populate, message, media_only (extra keys allowed).
     */
    getAllMediaMessagesByConnection(serviceId, connectionId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/services/{service_id}/connection/{connection_id}/messages", { path: { service_id: serviceId, connection_id: connectionId }, query: opts.query });
    }
    /**
     * Delete a message (of type comment) by id.
     * @method DELETE /v1/workspaces/{workspace_id}/services/{service_id}/messages/{message_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteMessages(serviceId, messageId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/services/{service_id}/messages/{message_id}", { path: { service_id: serviceId, message_id: messageId }, query: opts.query });
    }
    /**
     * Update a message (of type comment) by id.
     * @method PUT /v1/workspaces/{workspace_id}/services/{service_id}/messages/{message_id}
     * @remarks Any query params may be sent (none documented).
     */
    putMessages(serviceId, messageId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/services/{service_id}/messages/{message_id}", { path: { service_id: serviceId, message_id: messageId }, body, query: opts.query });
    }
    /**
     * Do action on service by id.
     * @method PATCH /v1/workspaces/{workspace_id}/services/{service_id}/action
     * @remarks Any query params may be sent (none documented).
     */
    patchAction(serviceId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/services/{service_id}/action", { path: { service_id: serviceId }, body, query: opts.query });
    }
    /**
     * Add cards on service by id.
     * @method PUT /v1/workspaces/{workspace_id}/services/{service_id}/add-cards
     * @remarks Any query params may be sent (none documented).
     */
    addCards(serviceId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/services/{service_id}/add-cards", { path: { service_id: serviceId }, body, query: opts.query });
    }
    /**
     * Add followers to service by id.
     * @method PUT /v1/workspaces/{workspace_id}/services/{service_id}/add-followers
     * @remarks Any query params may be sent (none documented).
     */
    addFollowers(serviceId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/services/{service_id}/add-followers", { path: { service_id: serviceId }, body, query: opts.query });
    }
    /**
     * Add tags on service by id.
     * @method PUT /v1/workspaces/{workspace_id}/services/{service_id}/add-tags
     * @remarks Any query params may be sent (none documented).
     */
    addTags(serviceId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/services/{service_id}/add-tags", { path: { service_id: serviceId }, body, query: opts.query });
    }
    /**
     * Create or associate a person with a service.
     * @method POST /v1/workspaces/{workspace_id}/services/{id}/associate
     * @remarks Any query params may be sent (none documented).
     */
    associate(id, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/services/{id}/associate", { path: { id }, body, query: opts.query });
    }
    /**
     * Update custom-fields on service by id.
     * @method PATCH /v1/workspaces/{workspace_id}/services/{service_id}/custom-fields
     * @remarks Any query params may be sent (none documented).
     */
    patchCustomFields(serviceId, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/services/{service_id}/custom-fields", { path: { service_id: serviceId }, body, query: opts.query });
    }
    /**
     * Get emails from services by id.
     * @method GET /v1/workspaces/{workspace_id}/services/{id}/emails
     * @remarks Documented query: page, limit, order, direction_order, to, subject, text, user, person, service, populate (extra keys allowed).
     */
    getEmails(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/services/{id}/emails", { path: { id }, query: opts.query });
    }
    /**
     * Get history (countless).
     * @method GET /v1/workspaces/{workspace_id}/services/{id}/history
     * @remarks Documented query: page, limit, order, retrieve_mode, direction_order, user, finished_by_user, person, connection, sector, reason, card, name, search, type, status, statuses, csat, populate, start_date, end_date, field_date, tags, sectors, fcr, win, key (extra keys allowed).
     */
    getHistory(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/services/{id}/history", { path: { id }, query: opts.query });
    }
    /**
     * Get all services by permission.
     * @method GET /v1/workspaces/{workspace_id}/services/{id}/history-by-permission
     * @remarks Documented query: page, limit, order, direction_order, user, finished_by_user, person, connection, sector, reason, card, name, search, type, status, statuses, csat, populate, start_date, end_date, field_date, tags, sectors, fcr, win, key, custom_fields (extra keys allowed).
     */
    getHistoryByPermission(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/services/{id}/history-by-permission", { path: { id }, query: opts.query });
    }
    /**
     * Get services messages by id.
     * @method GET /v1/workspaces/{workspace_id}/services/{id}/messages
     * @remarks Documented query: page, limit, order, direction_order, user, body, populate, message (extra keys allowed).
     */
    ServicesController_getServicesMessages_v1(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/services/{id}/messages", { path: { id }, query: opts.query });
    }
    /**
     * Create a message.
     * @method POST /v1/workspaces/{workspace_id}/services/{service_id}/messages
     * @remarks Any query params may be sent (none documented).
     */
    messagesV1(serviceId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/services/{service_id}/messages", { path: { service_id: serviceId }, body, query: opts.query });
    }
    /**
     * Create a message template.
     * @method POST /v1/workspaces/{workspace_id}/services/{service_id}/messages-templates
     * @remarks Any query params may be sent (none documented).
     */
    messagesTemplates(serviceId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/services/{service_id}/messages-templates", { path: { service_id: serviceId }, body, query: opts.query });
    }
    /**
     * Remove cards by id.
     * @method PUT /v1/workspaces/{workspace_id}/services/{service_id}/remove-cards
     * @remarks Any query params may be sent (none documented).
     */
    removeCards(serviceId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/services/{service_id}/remove-cards", { path: { service_id: serviceId }, body, query: opts.query });
    }
    /**
     * Remove followers by id.
     * @method PUT /v1/workspaces/{workspace_id}/services/{service_id}/remove-followers
     * @remarks Any query params may be sent (none documented).
     */
    removeFollowers(serviceId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/services/{service_id}/remove-followers", { path: { service_id: serviceId }, body, query: opts.query });
    }
    /**
     * Remove tags by id.
     * @method PUT /v1/workspaces/{workspace_id}/services/{service_id}/remove-tags
     * @remarks Any query params may be sent (none documented).
     */
    removeTags(serviceId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/services/{service_id}/remove-tags", { path: { service_id: serviceId }, body, query: opts.query });
    }
    /**
     * Get service-times by id.
     * @method GET /v1/workspaces/{workspace_id}/services/{id}/service-times
     * @remarks Documented query: page, limit, order, direction_order, user, person, connection, sector, service, card, type, active, populate, start_date, end_date, field_date (extra keys allowed).
     */
    getServiceTimes(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/services/{id}/service-times", { path: { id }, query: opts.query });
    }
    /**
     * Take service by id.
     * @method PATCH /v1/workspaces/{workspace_id}/services/{id}/take
     * @remarks Any query params may be sent (none documented).
     */
    patchTake(id, body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/services/{id}/take", { path: { id }, body, query: opts.query });
    }
    /**
     * Transfer service by id.
     * @method PUT /v1/workspaces/{workspace_id}/services/{service_id}/transfer
     * @remarks Documented query: populate (extra keys allowed).
     */
    putTransfer(serviceId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/services/{service_id}/transfer", { path: { service_id: serviceId }, body, query: opts.query });
    }
    /**
     * Get service by id.
     * @method GET /v1/workspaces/{workspace_id}/services/{service_id}
     * @remarks Any query params may be sent (none documented).
     */
    getServiceV1(serviceId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/services/{service_id}", { path: { service_id: serviceId }, query: opts.query });
    }
    /**
     * Update service by id.
     * @method PUT /v1/workspaces/{workspace_id}/services/{service_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateService(serviceId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/services/{service_id}", { path: { service_id: serviceId }, body, query: opts.query });
    }
    /**
     * Batch service actions.
     * @method POST /v1/workspaces/{workspace_id}/services/batch
     * @remarks Any query params may be sent (none documented).
     */
    batch(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/services/batch", { body, query: opts.query });
    }
    /**
     * Get all services.
     * @method GET /v1/workspaces/{workspace_id}/services
     * @remarks Documented query: page, limit, order, direction_order, user, finished_by_user, person, connection, sector, reason, card, name, search, type, status, statuses, csat, populate, start_date, end_date, field_date, tags, sectors, fcr, win, key, custom_fields (extra keys allowed).
     */
    listServicesV1(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/services", { query: opts.query });
    }
    /**
     * Create a new service.
     * @method POST /v1/workspaces/{workspace_id}/services
     * @remarks Any query params may be sent (none documented).
     */
    createService(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/services", { body, query: opts.query });
    }
    /**
     * messages. Create a message v2.
     * @method POST /v2/workspaces/{workspace_id}/services/{service_id}/messages
     * @remarks Any query params may be sent (none documented).
     * @param file The spreadsheet file part (sent under the `file` field).
     * @param fields Extra form-data text fields to send alongside the file.
     */
    messages(serviceId, file, fields, opts = {}) {
      const body = { kind: "multipart", fields, files: { file } };
      return this.http.post("/v2/workspaces/{workspace_id}/services/{service_id}/messages", { path: { service_id: serviceId }, body, query: opts.query });
    }
    /**
     * Get service by id (V2).
     * @method GET /v2/workspaces/{workspace_id}/services/{service_id}
     * @remarks Any query params may be sent (none documented).
     */
    getService(serviceId, opts = {}) {
      return this.http.get("/v2/workspaces/{workspace_id}/services/{service_id}", { path: { service_id: serviceId }, query: opts.query });
    }
    /**
     * Get all services (V2).
     * @method GET /v2/workspaces/{workspace_id}/services
     * @remarks Documented query: filters, page, limit, order, direction_order, user, finished_by_user, person, connection, sector, reason, card, name, search, type, status, statuses, csat, populate, start_date, end_date, field_date, tags, sectors, fcr, win, key, custom_fields (extra keys allowed).
     */
    listServices(opts = {}) {
      return this.http.get("/v2/workspaces/{workspace_id}/services", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_sessions.ts
  var Sessions = class extends Resource {
    /**
     * Get session by id.
     * @method GET /v1/workspaces/{workspace_id}/sessions/{session_id}
     * @remarks Documented query: connection, key (extra keys allowed).
     */
    getSession(sessionId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/sessions/{session_id}", { path: { session_id: sessionId }, query: opts.query });
    }
    /**
     * Get opened session by key.
     * @method GET /v1/workspaces/{workspace_id}/sessions/opened-session
     * @remarks Documented query: filters, connection, key (extra keys allowed).
     */
    getOpenedSession(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/sessions/opened-session", { query: opts.query });
    }
    /**
     * Get all sessions.
     * @method GET /v1/workspaces/{workspace_id}/sessions
     * @remarks Documented query: filters, page, limit, order, direction_order, key, connection, category, user_initiated, two_way_enable, has_error, is_valid, expire_at, type, populate, start_date, end_date, field_date (extra keys allowed).
     */
    listSessions(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/sessions", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_shortUrls.ts
  var ShortUrls = class extends Resource {
    /**
     * deleteShortUrl.
     * @method DELETE /v1/workspaces/{workspace_id}/short-urls/{short_url_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteShortUrl(shortUrlId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/short-urls/{short_url_id}", { path: { short_url_id: shortUrlId }, query: opts.query });
    }
    /**
     * getShortUrl.
     * @method GET /v1/workspaces/{workspace_id}/short-urls/{short_url_id}
     * @remarks Any query params may be sent (none documented).
     */
    getShortUrl(shortUrlId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/short-urls/{short_url_id}", { path: { short_url_id: shortUrlId }, query: opts.query });
    }
    /**
     * updateShortUrl.
     * @method PUT /v1/workspaces/{workspace_id}/short-urls/{short_url_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateShortUrl(shortUrlId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/short-urls/{short_url_id}", { path: { short_url_id: shortUrlId }, body, query: opts.query });
    }
    /**
     * getAvailableSlug.
     * @method GET /v1/workspaces/{workspace_id}/short-urls/available-slug
     * @remarks Documented query: filters (extra keys allowed).
     */
    getAvailableSlug(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/short-urls/available-slug", { query: opts.query });
    }
    /**
     * listShortUrls.
     * @method GET /v1/workspaces/{workspace_id}/short-urls
     * @remarks Documented query: filters (extra keys allowed).
     */
    listShortUrls(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/short-urls", { query: opts.query });
    }
    /**
     * createShortUrl.
     * @method POST /v1/workspaces/{workspace_id}/short-urls
     * @remarks Any query params may be sent (none documented).
     */
    createShortUrl(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/short-urls", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_slaconfigs.ts
  var Slaconfigs = class extends Resource {
    /**
     * deleteSlaconfig.
     * @method DELETE /v1/workspaces/{workspace_id}/slaconfigs/{slaconfig_id}
     * @remarks Documented query: data (extra keys allowed).
     */
    deleteSlaconfig(slaconfigId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/slaconfigs/{slaconfig_id}", { path: { slaconfig_id: slaconfigId }, query: opts.query });
    }
    /**
     * getSlaconfig.
     * @method GET /v1/workspaces/{workspace_id}/slaconfigs/{slaconfig_id}
     * @remarks Any query params may be sent (none documented).
     */
    getSlaconfig(slaconfigId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/slaconfigs/{slaconfig_id}", { path: { slaconfig_id: slaconfigId }, query: opts.query });
    }
    /**
     * updateSlaconfig.
     * @method PUT /v1/workspaces/{workspace_id}/slaconfigs/{slaconfig_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateSlaconfig(slaconfigId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/slaconfigs/{slaconfig_id}", { path: { slaconfig_id: slaconfigId }, body, query: opts.query });
    }
    /**
     * listSlaconfigs.
     * @method GET /v1/workspaces/{workspace_id}/slaconfigs
     * @remarks Documented query: filters (extra keys allowed).
     */
    listSlaconfigs(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/slaconfigs", { query: opts.query });
    }
    /**
     * createSlaconfig.
     * @method POST /v1/workspaces/{workspace_id}/slaconfigs
     * @remarks Any query params may be sent (none documented).
     */
    createSlaconfig(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/slaconfigs", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_superuser.ts
  var Superuser = class extends Resource {
    /**
     * updateSuperuser.
     * @method PUT /v1/workspaces/{workspace_id}/superuser
     * @remarks Any query params may be sent (none documented).
     */
    updateSuperuser(body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/superuser", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_syncDelete.ts
  var SyncDelete = class extends Resource {
    /**
     * listSyncDelete.
     * @method GET /v1/workspaces/{workspace_id}/sync-delete
     * @remarks Documented query: filters (extra keys allowed).
     */
    listSyncDelete(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/sync-delete", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_tags.ts
  var Tags = class extends Resource {
    /**
     * Delete tag by id.
     * @method DELETE /v1/workspaces/{workspace_id}/tags/{tag_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteTag(tagId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/tags/{tag_id}", { path: { tag_id: tagId }, query: opts.query });
    }
    /**
     * Get tag by id.
     * @method GET /v1/workspaces/{workspace_id}/tags/{tag_id}
     * @remarks Any query params may be sent (none documented).
     */
    getTag(tagId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/tags/{tag_id}", { path: { tag_id: tagId }, query: opts.query });
    }
    /**
     * Update tag by id.
     * @method PUT /v1/workspaces/{workspace_id}/tags/{tag_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateTag(tagId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/tags/{tag_id}", { path: { tag_id: tagId }, body, query: opts.query });
    }
    /**
     * Get all tags.
     * @method GET /v1/workspaces/{workspace_id}/tags
     * @remarks Documented query: filters, page, limit, order, direction_order, name, sector, populate (extra keys allowed).
     */
    listTags(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/tags", { query: opts.query });
    }
    /**
     * Create a tag.
     * @method POST /v1/workspaces/{workspace_id}/tags
     * @remarks Any query params may be sent (none documented).
     */
    createTag(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/tags", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_tasks.ts
  var Tasks = class extends Resource {
    /**
     * Delete checklist item by id.
     * @method DELETE /v1/workspaces/{workspace_id}/tasks/{task_id}/checklist/{checklist_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteChecklist(taskId, checklistId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/tasks/{task_id}/checklist/{checklist_id}", { path: { task_id: taskId, checklist_id: checklistId }, query: opts.query });
    }
    /**
     * Update checklist item on tag by id.
     * @method PUT /v1/workspaces/{workspace_id}/tasks/{task_id}/checklist/{checklist_id}
     * @remarks Any query params may be sent (none documented).
     */
    putChecklist(taskId, checklistId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/tasks/{task_id}/checklist/{checklist_id}", { path: { task_id: taskId, checklist_id: checklistId }, body, query: opts.query });
    }
    /**
     * Approve (give acceptance to) a service-team task.
     * @method PUT /v1/workspaces/{workspace_id}/tasks/{task_id}/approval
     * @remarks Any query params may be sent (none documented).
     */
    putApproval(taskId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/tasks/{task_id}/approval", { path: { task_id: taskId }, body, query: opts.query });
    }
    /**
     * Add checklist item to task by id.
     * @method POST /v1/workspaces/{workspace_id}/tasks/{task_id}/checklist
     * @remarks Any query params may be sent (none documented).
     */
    checklist(taskId, body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/tasks/{task_id}/checklist", { path: { task_id: taskId }, body, query: opts.query });
    }
    /**
     * Delete task by id.
     * @method DELETE /v1/workspaces/{workspace_id}/tasks/{task_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteTask(taskId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/tasks/{task_id}", { path: { task_id: taskId }, query: opts.query });
    }
    /**
     * Get task by id.
     * @method GET /v1/workspaces/{workspace_id}/tasks/{task_id}
     * @remarks Documented query: populate (extra keys allowed).
     */
    getTask(taskId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/tasks/{task_id}", { path: { task_id: taskId }, query: opts.query });
    }
    /**
     * Update task by id.
     * @method PUT /v1/workspaces/{workspace_id}/tasks/{task_id}
     * @remarks Documented query: populate (extra keys allowed).
     */
    updateTask(taskId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/tasks/{task_id}", { path: { task_id: taskId }, body, query: opts.query });
    }
    /**
     * Get all tasks created by the service team for this client's organization.
     * @method GET /v1/workspaces/{workspace_id}/tasks/approvals
     * @remarks Documented query: filters, page, limit, order, direction_order, person, card, service, organization, sector, user, created_by_user, name, description, status, type, start_date, finish_date, no_date, has_start_date, populate, statuses, billable, approval (extra keys allowed).
     */
    getApprovals(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/tasks/approvals", { query: opts.query });
    }
    /**
     * Get all tasks.
     * @method GET /v1/workspaces/{workspace_id}/tasks
     * @remarks Documented query: filters, page, limit, order, direction_order, person, card, service, organization, sector, user, created_by_user, name, description, status, type, start_date, finish_date, no_date, has_start_date, populate, statuses, billable, approval (extra keys allowed).
     */
    listTasksV1(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/tasks", { query: opts.query });
    }
    /**
     * Create a task.
     * @method POST /v1/workspaces/{workspace_id}/tasks
     * @remarks Documented query: populate (extra keys allowed).
     */
    createTask(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/tasks", { body, query: opts.query });
    }
    /**
     * Get all tasks v2.
     * @method GET /v2/workspaces/{workspace_id}/tasks
     * @remarks Documented query: filters, page, limit, order, direction_order, person, card, service, organization, sector, user, created_by_user, name, description, status, type, start_date, finish_date, no_date, has_start_date, populate, statuses, billable, approval (extra keys allowed).
     */
    listTasks(opts = {}) {
      return this.http.get("/v2/workspaces/{workspace_id}/tasks", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_temp.ts
  var Temp = class extends Resource {
    /**
     * listTemp.
     * @method GET /v1/workspaces/temp
     * @remarks Documented query: filters (extra keys allowed).
     */
    listTemp(opts = {}) {
      return this.http.get("/v1/workspaces/temp", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_tempTokens.ts
  var TempTokens = class extends Resource {
    /**
     * deleteTempToken.
     * @method DELETE /v1/workspaces/{workspace_id}/temp-tokens/{temp_token_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteTempToken(tempTokenId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/temp-tokens/{temp_token_id}", { path: { temp_token_id: tempTokenId }, query: opts.query });
    }
    /**
     * listTempTokens.
     * @method GET /v1/workspaces/{workspace_id}/temp-tokens
     * @remarks Documented query: filters (extra keys allowed).
     */
    listTempTokens(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/temp-tokens", { query: opts.query });
    }
    /**
     * createTempToken.
     * @method POST /v1/workspaces/{workspace_id}/temp-tokens
     * @remarks Any query params may be sent (none documented).
     */
    createTempToken(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/temp-tokens", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_trainingBases.ts
  var TrainingBases = class extends Resource {
    /**
     * deleteTrainingBase.
     * @method DELETE /v1/workspaces/{workspace_id}/training-bases/{training_base_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteTrainingBase(trainingBaseId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/training-bases/{training_base_id}", { path: { training_base_id: trainingBaseId }, query: opts.query });
    }
    /**
     * getTrainingBase.
     * @method GET /v1/workspaces/{workspace_id}/training-bases/{training_base_id}
     * @remarks Any query params may be sent (none documented).
     */
    getTrainingBase(trainingBaseId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/training-bases/{training_base_id}", { path: { training_base_id: trainingBaseId }, query: opts.query });
    }
    /**
     * listTrainingBases.
     * @method GET /v1/workspaces/{workspace_id}/training-bases
     * @remarks Documented query: filters (extra keys allowed).
     */
    listTrainingBases(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/training-bases", { query: opts.query });
    }
    /**
     * createTrainingBase.
     * @method POST /v1/workspaces/{workspace_id}/training-bases
     * @remarks Any query params may be sent (none documented).
     */
    createTrainingBase(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/training-bases", { body, query: opts.query });
    }
    /**
     * updateTrainingBase.
     * @method PUT /v1/workspaces/{workspace_id}/training-bases
     * @remarks Any query params may be sent (none documented).
     */
    updateTrainingBase(body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/training-bases", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_transferLogs.ts
  var TransferLogs = class extends Resource {
    /**
     * Get on card by id.
     * @method GET /v1/workspaces/{workspace_id}/transfer-logs/cards/{id}
     * @remarks Any query params may be sent (none documented).
     */
    getCard(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/transfer-logs/cards/{id}", { path: { id }, query: opts.query });
    }
    /**
     * Get on service by id.
     * @method GET /v1/workspaces/{workspace_id}/transfer-logs/services/{id}
     * @remarks Any query params may be sent (none documented).
     */
    getService(id, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/transfer-logs/services/{id}", { path: { id }, query: opts.query });
    }
    /**
     * Get all cards from transfer-logs.
     * @method GET /v1/workspaces/{workspace_id}/transfer-logs/cards
     * @remarks Documented query: page, limit, order, direction_order, flow, reason, user, card, service, description, type, populate (extra keys allowed).
     */
    getCards(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/transfer-logs/cards", { query: opts.query });
    }
    /**
     * Get all transfer-logs.
     * @method GET /v1/workspaces/{workspace_id}/transfer-logs/services
     * @remarks Documented query: page, limit, order, direction_order, flow, reason, user, card, service, description, type, populate (extra keys allowed).
     */
    getServices(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/transfer-logs/services", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_triggers.ts
  var Triggers = class extends Resource {
    /**
     * getTrigger.
     * @method GET /v1/workspaces/{workspace_id}/triggers/{trigger_id}
     * @remarks Any query params may be sent (none documented).
     */
    getTrigger(triggerId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/triggers/{trigger_id}", { path: { trigger_id: triggerId }, query: opts.query });
    }
    /**
     * listTriggers.
     * @method GET /v1/workspaces/{workspace_id}/triggers
     * @remarks Documented query: filters (extra keys allowed).
     */
    listTriggers(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/triggers", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_user.ts
  var User = class extends Resource {
    /**
     * listUser.
     * @method GET /v1/workspaces/{workspace_id}/user
     * @remarks Any query params may be sent (none documented).
     */
    listUser(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/user", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_userToInvite.ts
  var UserToInvite = class extends Resource {
    /**
     * listUserToInvite.
     * @method GET /v1/workspaces/{workspace_id}/user-to-invite
     * @remarks Any query params may be sent (none documented).
     */
    listUserToInvite(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/user-to-invite", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_users.ts
  var Users = class extends Resource {
    /**
     * getCalendar.
     * @method GET /v1/users/{id}/calendar
     * @remarks Documented query: sync (extra keys allowed).
     */
    getCalendar(id, opts = {}) {
      return this.http.get("/v1/users/{id}/calendar", { path: { id }, query: opts.query });
    }
    /**
     * updateUser.
     * @method PUT /v1/users/{user_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateUser(userId, body, opts = {}) {
      return this.http.put("/v1/users/{user_id}", { path: { user_id: userId }, body, query: opts.query });
    }
    /**
     * getHabllaDomain.
     * @method GET /v1/users/hablla-domain
     * @remarks Any query params may be sent (none documented).
     */
    getHabllaDomain(opts = {}) {
      return this.http.get("/v1/users/hablla-domain", { query: opts.query });
    }
    /**
     * getMe.
     * @method GET /v1/users/me
     * @remarks Any query params may be sent (none documented).
     */
    getMe(opts = {}) {
      return this.http.get("/v1/users/me", { query: opts.query });
    }
    /**
     * sendCode.
     * @method POST /v1/users/send-code
     * @remarks Any query params may be sent (none documented).
     */
    sendCode(body, opts = {}) {
      return this.http.post("/v1/users/send-code", { body, query: opts.query });
    }
    /**
     * verifyCode.
     * @method POST /v1/users/verify-code
     * @remarks Any query params may be sent (none documented).
     */
    verifyCode(body, opts = {}) {
      return this.http.post("/v1/users/verify-code", { body, query: opts.query });
    }
    /**
     * Get all users from a workspace by id.
     * @method GET /v1/workspaces/{workspace_id}/users
     * @remarks Documented query: filters, page, limit, order, direction_order, name, email, role_type, no_sector, no_person, no_service, permission, search, populate, start_date, end_date, field_date (extra keys allowed).
     */
    listUsers(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/users", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_webhooks.ts
  var Webhooks = class extends Resource {
    /**
     * updateWebhook.
     * @method PUT /v1/workspaces/{workspace_id}/webhooks/{webhook_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateWebhook(webhookId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/webhooks/{webhook_id}", { path: { webhook_id: webhookId }, body, query: opts.query });
    }
    /**
     * createWebhook.
     * @method POST /v1/workspaces/{workspace_id}/webhooks
     * @remarks Any query params may be sent (none documented).
     */
    createWebhook(body, opts = {}) {
      return this.http.post("/v1/workspaces/{workspace_id}/webhooks", { body, query: opts.query });
    }
  };

  // src/sdk/resources/gen_workspaces.ts
  var Workspaces = class extends Resource {
    /**
     * Get all users from a workspace by id.
     * @method GET /v1/workspaces/{workspace_id}/users
     * @remarks Documented query: filters, page, limit, order, direction_order, name, email, role_type, no_sector, no_person, no_service, permission, search, populate, start_date, end_date, field_date (extra keys allowed).
     */
    getUsers(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/users", { query: opts.query });
    }
    /**
     * Get workspace by id.
     * @method GET /v1/workspaces/{workspace_id}
     * @remarks Any query params may be sent (none documented).
     */
    getWorkspace(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}", { query: opts.query });
    }
    /**
     * Get all workspaces.
     * @method GET /v1/workspaces
     * @remarks Documented query: filters, page, limit, order, direction_order, name, search, type, plan_type, owner, partner, is_blocked, is_deleted, pending_plan, auto_invoice, created_at, updated_at (extra keys allowed).
     */
    listWorkspaces(opts = {}) {
      return this.http.get("/v1/workspaces", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_workspacesPlans.ts
  var WorkspacesPlans = class extends Resource {
    /**
     * deleteItems.
     * @method DELETE /v1/workspaces/{workspace_id}/workspaces-plans/items/{item_id}
     * @remarks Any query params may be sent (none documented).
     */
    deleteItems(itemId, opts = {}) {
      return this.http.delete("/v1/workspaces/{workspace_id}/workspaces-plans/items/{item_id}", { path: { item_id: itemId }, query: opts.query });
    }
    /**
     * getWorkspacesPlan.
     * @method GET /v1/workspaces/{workspace_id}/workspaces-plans/{workspaces_plan_id}
     * @remarks Any query params may be sent (none documented).
     */
    getWorkspacesPlan(workspacesPlanId, opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/workspaces-plans/{workspaces_plan_id}", { path: { workspaces_plan_id: workspacesPlanId }, query: opts.query });
    }
    /**
     * updateWorkspacesPlan.
     * @method PUT /v1/workspaces/{workspace_id}/workspaces-plans/{workspaces_plan_id}
     * @remarks Any query params may be sent (none documented).
     */
    updateWorkspacesPlan(workspacesPlanId, body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/workspaces-plans/{workspaces_plan_id}", { path: { workspaces_plan_id: workspacesPlanId }, body, query: opts.query });
    }
    /**
     * putAdditionalItems.
     * @method PUT /v1/workspaces/{workspace_id}/workspaces-plans/additional-items
     * @remarks Any query params may be sent (none documented).
     */
    putAdditionalItems(body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/workspaces-plans/additional-items", { body, query: opts.query });
    }
    /**
     * patchItems.
     * @method PATCH /v1/workspaces/{workspace_id}/workspaces-plans/items
     * @remarks Any query params may be sent (none documented).
     */
    patchItems(body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/workspaces-plans/items", { body, query: opts.query });
    }
    /**
     * listWorkspacesPlans.
     * @method GET /v1/workspaces/{workspace_id}/workspaces-plans
     * @remarks Documented query: filters (extra keys allowed).
     */
    listWorkspacesPlans(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/workspaces-plans", { query: opts.query });
    }
  };

  // src/sdk/resources/gen_wsusers.ts
  var Wsusers = class extends Resource {
    /**
     * getCounters.
     * @method GET /v1/workspaces/{workspace_id}/wsusers/logs/counters
     * @remarks Documented query: query (extra keys allowed).
     */
    getCounters(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/wsusers/logs/counters", { query: opts.query });
    }
    /**
     * putAdmin.
     * @method PUT /v1/workspaces/{workspace_id}/wsusers/admin
     * @remarks Any query params may be sent (none documented).
     */
    putAdmin(body, opts = {}) {
      return this.http.put("/v1/workspaces/{workspace_id}/wsusers/admin", { body, query: opts.query });
    }
    /**
     * getLogs.
     * @method GET /v1/workspaces/{workspace_id}/wsusers/logs
     * @remarks Documented query: filters (extra keys allowed).
     */
    getLogs(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/wsusers/logs", { query: opts.query });
    }
    /**
     * getMe.
     * @method GET /v1/workspaces/{workspace_id}/wsusers/me
     * @remarks Any query params may be sent (none documented).
     */
    getMe(opts = {}) {
      return this.http.get("/v1/workspaces/{workspace_id}/wsusers/me", { query: opts.query });
    }
    /**
     * patchWsuser.
     * @method PATCH /v1/workspaces/{workspace_id}/wsusers
     * @remarks Any query params may be sent (none documented).
     */
    patchWsuser(body, opts = {}) {
      return this.http.patch("/v1/workspaces/{workspace_id}/wsusers", { body, query: opts.query });
    }
  };

  // src/sdk/utils/primitives/string.ts
  var toDigits = (value) => String(value != null ? value : "").replace(/\D/g, "");

  // src/sdk/utils/primitives/record.ts
  var collectIndexed = (record, prefix) => {
    const pattern = new RegExp("^" + prefix + "\\d+$");
    return Object.keys(record).filter((key) => pattern.test(key)).sort((a, b) => Number(a.slice(prefix.length)) - Number(b.slice(prefix.length))).map((key) => record[key]);
  };

  // src/sdk/utils/formats/phone.ts
  var phoneVariants = (value) => {
    const digits = toDigits(value);
    const alternate = digits.length === 13 ? digits.slice(0, 4) + digits.slice(5) : digits.slice(0, 4) + "9" + digits.slice(4);
    return { digits, alternate };
  };
  var matchesPhone = (candidate, variants) => {
    const value = toDigits(candidate);
    return value === variants.digits || value === variants.alternate;
  };

  // src/sdk/utils/formats/email.ts
  var isEmail = (value) => /@/.test(String(value != null ? value : ""));
  var deriveEmail = (rawValue, rule) => {
    const accounts = String(rawValue != null ? rawValue : "").split(/[^\dA-Za-z]+/).filter(Boolean);
    if (!accounts.length) return null;
    return accounts.join(rule.separator) + "@" + rule.domain;
  };

  // src/sdk/utils/formats/hablla.ts
  var customFieldKeys = (record) => Object.keys(record).filter((key) => /^cf_[a-f0-9]{24}$/.test(key));

  // src/sdk/domain/dispatch/dispatch.ts
  var Dispatch = class {
    constructor(client) {
      __publicField(this, "client", client);
    }
    run(input, spec) {
      return __async(this, null, function* () {
        var _a, _b, _c;
        const phone = String((_a = input[spec.person.phoneField]) != null ? _a : "");
        if (!spec.connectionId) return { status: "conexao_invalida", phone };
        if (!spec.templateId) return { status: "template_invalido", phone };
        if (!spec.sectorId) return { status: "sem_setor", phone };
        const variants = phoneVariants(phone);
        if (variants.digits.length < 12) return { status: "telefone_invalido", phone };
        let person = yield this.findPerson(variants);
        if (person == null ? void 0 : person.is_blocked) return { status: "bloqueado", phone };
        const advisor = this.resolveAdvisor(person, input, spec);
        let created = false;
        if (!person) {
          if (spec.missingContact === "skip") return { status: "sem_cadastro", phone };
          if (!advisor) return { status: "sem_assessor", phone };
          person = yield this.createPerson(input, spec, advisor.id);
          created = true;
        } else if (advisor && !((_b = person.users) != null ? _b : []).length) {
          try {
            yield this.client.persons.addUsers(person.id, { users: [advisor.id] });
          } catch (error) {
          }
        }
        let closedExisting = false;
        if (!created) {
          const { results: open } = yield this.client.services.listServices({ query: { limit: 50, connection: spec.connectionId, person: person.id, status: "in_attendance" } });
          if (open == null ? void 0 : open.length) {
            if (spec.openAttendance === "finishAndSend") {
              for (const service of open) {
                try {
                  yield this.client.services.patchAction(service.id, { status: "finished", reason: spec.finishReasonId });
                } catch (error) {
                }
              }
              closedExisting = true;
            } else {
              return { status: "em_atendimento", person: person.id, service: (_c = open[0]) == null ? void 0 : _c.id, assessor: advisor == null ? void 0 : advisor.name };
            }
          }
        }
        const variables = collectIndexed(input, "var");
        if (variables.length < spec.templateVarCount) return { status: "variaveis_invalidas", phone, assessor: advisor == null ? void 0 : advisor.name };
        const destinationKey = this.destinationKey(person, variants, phone);
        try {
          yield this.client.connections.messagesTemplates(spec.connectionId, {
            examples: { body: spec.templateVarCount ? variables.slice(0, spec.templateVarCount) : [], header: [] },
            template: spec.templateId,
            sector: spec.sectorId,
            to: { key: destinationKey, type: "whatsapp", person: person.id }
          });
          if (spec.person.tagField && input[spec.person.tagField]) {
            try {
              yield this.client.persons.addTags(person.id, { tags: [String(input[spec.person.tagField])] });
            } catch (error) {
            }
          }
          const service = yield this.settleAttendance(person.id, spec, advisor == null ? void 0 : advisor.id);
          return { status: created ? "criado_enviado" : closedExisting ? "atendimento_encerrado_enviado" : "enviado", person: person.id, service, assessor: advisor == null ? void 0 : advisor.name, created };
        } catch (error) {
          if (error && (error.status === 409 || /\b(?:136|409)\b/.test(String(error.message)))) {
            return { status: "em_atendimento", person: person.id, assessor: advisor == null ? void 0 : advisor.name };
          }
          return { status: "erro_envio", phone, detail: String(error && error.message).slice(0, 120), person: person.id };
        }
      });
    }
    /** Finds a person by the dedicated `phone` query, trying both phone shapes. */
    findPerson(variants) {
      return __async(this, null, function* () {
        var _a, _b, _c;
        const owns = (candidate) => {
          var _a2;
          return ((_a2 = candidate.phones) != null ? _a2 : []).some((entry) => matchesPhone(entry.phone, variants));
        };
        let matches = ((_a = (yield this.client.persons.listPersons({ query: { phone: variants.digits } })).results) != null ? _a : []).filter(owns);
        if (!matches.length && variants.alternate !== variants.digits) {
          matches = ((_b = (yield this.client.persons.listPersons({ query: { phone: variants.alternate } })).results) != null ? _b : []).filter(owns);
        }
        const seen = /* @__PURE__ */ new Set();
        const unique = matches.filter((candidate) => candidate && candidate.id && !seen.has(candidate.id) && seen.add(candidate.id));
        return (_c = unique.find((candidate) => {
          var _a2;
          return ((_a2 = candidate.users) != null ? _a2 : []).length;
        })) != null ? _c : unique[0];
      });
    }
    /** Existing owner (if it is one of the advisors) wins, then the sheet code, then the fallback. */
    resolveAdvisor(person, input, spec) {
      var _a, _b, _c;
      const byId = new Map(Object.values(spec.advisors).map((advisor) => [advisor.id, advisor]));
      const owners = person ? ((_a = person.users) != null ? _a : []).map((user) => {
        var _a2;
        return (_a2 = user == null ? void 0 : user.id) != null ? _a2 : user;
      }) : [];
      const existing = owners.map((id) => byId.get(id)).find(Boolean);
      if (existing) return existing;
      const code = String((_b = input[spec.advisorField]) != null ? _b : "");
      if (spec.advisors[code]) return spec.advisors[code];
      if (spec.ownerFallbackUserId) return (_c = byId.get(spec.ownerFallbackUserId)) != null ? _c : { id: spec.ownerFallbackUserId, name: "" };
      return void 0;
    }
    /** Builds a Hablla Person from the record, per the spec. */
    createPerson(input, spec, advisorId) {
      return __async(this, null, function* () {
        var _a, _b, _c, _d;
        const p = spec.person;
        const name = String((_a = input[p.nameField]) != null ? _a : "") || String((_b = input[p.phoneField]) != null ? _b : "");
        const emails = [];
        for (const field of p.emailFields) {
          const value = String((_c = input[field]) != null ? _c : "").trim();
          if (value && isEmail(value)) emails.push({ email: value });
        }
        for (const rule of p.derivedEmails) {
          const email = deriveEmail(input[rule.sourceField], { separator: rule.separator, domain: rule.domain });
          if (email) emails.push({ email });
        }
        const cfKeys = p.customFields === "auto" ? customFieldKeys(input) : p.customFields;
        const customFields = cfKeys.filter((key) => {
          var _a2;
          return String((_a2 = input[key]) != null ? _a2 : "").trim();
        }).map((key) => ({ custom_field: key.slice(3), value: String(input[key]).trim() }));
        return this.client.persons.createPerson(__spreadValues(__spreadValues({
          name,
          customer_status: "customer",
          phones: [{ phone: String((_d = input[p.phoneField]) != null ? _d : ""), is_whatsapp: true, type: "personal" }],
          sectors: [spec.sectorId],
          users: [advisorId]
        }, emails.length ? { emails } : {}), customFields.length ? { custom_fields: customFields } : {}));
      });
    }
    /**
     * Assigns the just-sent attendance to the advisor and finishes it (silent, with reason).
     * Returns the settled service id — the durable dispatch→attendance link — so the caller
     * can record it (exact traceability). Undefined when no attendance was found.
     */
    settleAttendance(personId, spec, advisorId) {
      return __async(this, null, function* () {
        try {
          const { results } = yield this.client.services.listServices({ query: { limit: 5, connection: spec.connectionId, person: personId, status: "in_attendance", order: "created_at", direction_order: "desc" } });
          const service = (results != null ? results : [])[0];
          if (!service) return void 0;
          if (advisorId) {
            try {
              yield this.client.services.putTransfer(service.id, { user: advisorId, sector: spec.sectorId });
            } catch (error) {
            }
          }
          yield this.client.services.patchAction(service.id, { status: "finished", reason: spec.finishReasonId });
          return service.id;
        } catch (error) {
          return void 0;
        }
      });
    }
    destinationKey(person, variants, phone) {
      var _a, _b, _c;
      const matched = ((_a = person.phones) != null ? _a : []).find((entry) => matchesPhone(entry.phone, variants));
      return matched && matched.phone || ((_c = ((_b = person.phones) != null ? _b : []).find((entry) => entry.is_whatsapp)) == null ? void 0 : _c.phone) || phone;
    }
  };

  // src/sdk/client.ts
  var HabllaClient = class {
    constructor(config) {
      __publicField(this, "auth");
      __publicField(this, "http");
      __publicField(this, "acceptInvite");
      __publicField(this, "annotations");
      __publicField(this, "asaas");
      __publicField(this, "authentication");
      __publicField(this, "automationRules");
      __publicField(this, "backup");
      __publicField(this, "block");
      __publicField(this, "blockedWords");
      __publicField(this, "boards");
      __publicField(this, "calendars");
      __publicField(this, "calls");
      __publicField(this, "campaigns");
      __publicField(this, "cards");
      __publicField(this, "cardsGroups");
      __publicField(this, "cdn");
      __publicField(this, "changeOwner");
      __publicField(this, "classes");
      __publicField(this, "comments");
      __publicField(this, "connections");
      __publicField(this, "copilot");
      __publicField(this, "costCenters");
      __publicField(this, "count");
      __publicField(this, "createUsers");
      __publicField(this, "credentials");
      __publicField(this, "customFields");
      __publicField(this, "customScreens");
      __publicField(this, "data");
      __publicField(this, "dictionaries");
      __publicField(this, "dictionariesFields");
      __publicField(this, "distributionLogs");
      __publicField(this, "domains");
      __publicField(this, "emailEvents");
      __publicField(this, "enrichment");
      __publicField(this, "events");
      __publicField(this, "export");
      __publicField(this, "extensions");
      __publicField(this, "fastMessages");
      __publicField(this, "feedPosts");
      __publicField(this, "flows");
      __publicField(this, "flowsExecutions");
      __publicField(this, "goals");
      __publicField(this, "goalsScopes");
      __publicField(this, "habllaAgent");
      __publicField(this, "habllaDocs");
      __publicField(this, "habllaReports");
      __publicField(this, "holidays");
      __publicField(this, "htm");
      __publicField(this, "import");
      __publicField(this, "invoices");
      __publicField(this, "knowledgeBases");
      __publicField(this, "legalEntities");
      __publicField(this, "linkInBio");
      __publicField(this, "messages");
      __publicField(this, "newPrefix");
      __publicField(this, "nps");
      __publicField(this, "officeHours");
      __publicField(this, "organizations");
      __publicField(this, "pages");
      __publicField(this, "pagesProjects");
      __publicField(this, "partner");
      __publicField(this, "partnered");
      __publicField(this, "partners");
      __publicField(this, "paymentIntervals");
      __publicField(this, "permissions");
      __publicField(this, "persons");
      __publicField(this, "plans");
      __publicField(this, "products");
      __publicField(this, "productsGroups");
      __publicField(this, "productsPrices");
      __publicField(this, "queueItems");
      __publicField(this, "queues");
      __publicField(this, "quotation");
      __publicField(this, "reasons");
      __publicField(this, "removeUser");
      __publicField(this, "reports");
      __publicField(this, "resetPlans");
      __publicField(this, "root");
      __publicField(this, "scripts");
      __publicField(this, "search");
      __publicField(this, "sectors");
      __publicField(this, "segmentations");
      __publicField(this, "seoProjects");
      __publicField(this, "services");
      __publicField(this, "sessions");
      __publicField(this, "shortUrls");
      __publicField(this, "slaconfigs");
      __publicField(this, "superuser");
      __publicField(this, "syncDelete");
      __publicField(this, "tags");
      __publicField(this, "tasks");
      __publicField(this, "temp");
      __publicField(this, "tempTokens");
      __publicField(this, "trainingBases");
      __publicField(this, "transferLogs");
      __publicField(this, "triggers");
      __publicField(this, "user");
      __publicField(this, "userToInvite");
      __publicField(this, "users");
      __publicField(this, "webhooks");
      __publicField(this, "workspaces");
      __publicField(this, "workspacesPlans");
      __publicField(this, "wsusers");
      /** Agnostic, spec-driven mass-dispatch executor (our logic over Hablla objects). */
      __publicField(this, "dispatch");
      var _a, _b;
      const baseUrl = (_a = config.baseUrl) != null ? _a : "https://api.hablla.com";
      this.auth = new HabllaAuth(
        config.transport,
        { workspaceToken: (_b = config.workspaceToken) != null ? _b : "", refreshToken: config.refreshToken, firebaseApiKey: config.firebaseApiKey, baseUrl, retry: config.retry },
        config.strategyCache
      );
      this.http = new HabllaHttpClient(config.transport, this.auth, { workspaceId: config.workspaceId, baseUrl, debug: config.debug, retry: config.retry });
      this.acceptInvite = new AcceptInvite(this.http);
      this.annotations = new Annotations(this.http);
      this.asaas = new Asaas(this.http);
      this.authentication = new Authentication(this.http);
      this.automationRules = new AutomationRules(this.http);
      this.backup = new Backup(this.http);
      this.block = new Block(this.http);
      this.blockedWords = new BlockedWords(this.http);
      this.boards = new Boards(this.http);
      this.calendars = new Calendars(this.http);
      this.calls = new Calls(this.http);
      this.campaigns = new Campaigns(this.http);
      this.cards = new Cards(this.http);
      this.cardsGroups = new CardsGroups(this.http);
      this.cdn = new Cdn(this.http);
      this.changeOwner = new ChangeOwner(this.http);
      this.classes = new Classes(this.http);
      this.comments = new Comments(this.http);
      this.connections = new Connections(this.http);
      this.copilot = new Copilot(this.http);
      this.costCenters = new CostCenters(this.http);
      this.count = new Count(this.http);
      this.createUsers = new CreateUsers(this.http);
      this.credentials = new Credentials(this.http);
      this.customFields = new CustomFields(this.http);
      this.customScreens = new CustomScreens(this.http);
      this.data = new Data(this.http);
      this.dictionaries = new Dictionaries(this.http);
      this.dictionariesFields = new DictionariesFields(this.http);
      this.distributionLogs = new DistributionLogs(this.http);
      this.domains = new Domains(this.http);
      this.emailEvents = new EmailEvents(this.http);
      this.enrichment = new Enrichment(this.http);
      this.events = new Events(this.http);
      this.export = new Export(this.http);
      this.extensions = new Extensions(this.http);
      this.fastMessages = new FastMessages(this.http);
      this.feedPosts = new FeedPosts(this.http);
      this.flows = new Flows(this.http);
      this.flowsExecutions = new FlowsExecutions(this.http);
      this.goals = new Goals(this.http);
      this.goalsScopes = new GoalsScopes(this.http);
      this.habllaAgent = new HabllaAgent(this.http);
      this.habllaDocs = new HabllaDocs(this.http);
      this.habllaReports = new HabllaReports(this.http);
      this.holidays = new Holidays(this.http);
      this.htm = new Htm(this.http);
      this.import = new Import(this.http);
      this.invoices = new Invoices(this.http);
      this.knowledgeBases = new KnowledgeBases(this.http);
      this.legalEntities = new LegalEntities(this.http);
      this.linkInBio = new LinkInBio(this.http);
      this.messages = new Messages(this.http);
      this.newPrefix = new NewPrefix(this.http);
      this.nps = new Nps(this.http);
      this.officeHours = new OfficeHours(this.http);
      this.organizations = new Organizations(this.http);
      this.pages = new Pages(this.http);
      this.pagesProjects = new PagesProjects(this.http);
      this.partner = new Partner(this.http);
      this.partnered = new Partnered(this.http);
      this.partners = new Partners(this.http);
      this.paymentIntervals = new PaymentIntervals(this.http);
      this.permissions = new Permissions(this.http);
      this.persons = new Persons(this.http);
      this.plans = new Plans(this.http);
      this.products = new Products(this.http);
      this.productsGroups = new ProductsGroups(this.http);
      this.productsPrices = new ProductsPrices(this.http);
      this.queueItems = new QueueItems(this.http);
      this.queues = new Queues(this.http);
      this.quotation = new Quotation(this.http);
      this.reasons = new Reasons(this.http);
      this.removeUser = new RemoveUser(this.http);
      this.reports = new Reports(this.http);
      this.resetPlans = new ResetPlans(this.http);
      this.root = new Root(this.http);
      this.scripts = new Scripts(this.http);
      this.search = new Search(this.http);
      this.sectors = new Sectors(this.http);
      this.segmentations = new Segmentations(this.http);
      this.seoProjects = new SeoProjects(this.http);
      this.services = new Services(this.http);
      this.sessions = new Sessions(this.http);
      this.shortUrls = new ShortUrls(this.http);
      this.slaconfigs = new Slaconfigs(this.http);
      this.superuser = new Superuser(this.http);
      this.syncDelete = new SyncDelete(this.http);
      this.tags = new Tags(this.http);
      this.tasks = new Tasks(this.http);
      this.temp = new Temp(this.http);
      this.tempTokens = new TempTokens(this.http);
      this.trainingBases = new TrainingBases(this.http);
      this.transferLogs = new TransferLogs(this.http);
      this.triggers = new Triggers(this.http);
      this.user = new User(this.http);
      this.userToInvite = new UserToInvite(this.http);
      this.users = new Users(this.http);
      this.webhooks = new Webhooks(this.http);
      this.workspaces = new Workspaces(this.http);
      this.workspacesPlans = new WorkspacesPlans(this.http);
      this.wsusers = new Wsusers(this.http);
      this.dispatch = new Dispatch(this);
    }
  };

  // src/sdk/store/schema.ts
  var RAW_COLUMN = "_raw";
  function toSpec(input) {
    return typeof input === "string" ? { name: input } : input;
  }
  function keepsRaw(schema) {
    return schema.raw !== false;
  }
  function columnValue(spec, entity) {
    if (spec.get) return spec.get(entity);
    return entity[spec.name];
  }
  function toCell(value) {
    if (value == null) return null;
    if (value instanceof Date) return value.toISOString();
    const t = typeof value;
    if (t === "string" || t === "number" || t === "boolean") return value;
    return JSON.stringify(value);
  }

  // src/sdk/store/projection.ts
  function headerFor(schema) {
    const names = schema.columns.map((c) => toSpec(c).name);
    return keepsRaw(schema) ? [...names, RAW_COLUMN] : names;
  }
  function projectRow(schema, entity) {
    const cells = schema.columns.map((c) => toCell(columnValue(toSpec(c), entity)));
    return keepsRaw(schema) ? [...cells, JSON.stringify(entity)] : cells;
  }
  function projectMatrix(schema, entities) {
    return [headerFor(schema), ...entities.map((e) => projectRow(schema, e))];
  }
  function parseRow(schema, header, row) {
    const rawIdx = header.indexOf(RAW_COLUMN);
    if (rawIdx >= 0) {
      const raw = row[rawIdx];
      if (typeof raw === "string" && raw) {
        try {
          return JSON.parse(raw);
        } catch (e) {
        }
      }
    }
    const record = {};
    let empty = true;
    header.forEach((name, i) => {
      var _a;
      if (name === RAW_COLUMN) return;
      const value = (_a = row[i]) != null ? _a : null;
      record[name] = value;
      if (value !== null && value !== "") empty = false;
    });
    return empty ? null : record;
  }
  function parseMatrix(schema, matrix) {
    var _a;
    if (!matrix.length) return [];
    const header = ((_a = matrix[0]) != null ? _a : []).map(String);
    const rows = matrix.slice(1);
    const out = [];
    for (const row of rows) {
      const entity = parseRow(schema, header, row);
      if (entity !== null) out.push(entity);
    }
    return out;
  }

  // src/sdk/store/merge.ts
  function updatedAt(entity, field) {
    const value = entity[field];
    if (value == null) return -Infinity;
    if (typeof value === "number") return value;
    const ms = Date.parse(String(value));
    return Number.isNaN(ms) ? -Infinity : ms;
  }
  function updatedAtResolver(updatedAtField) {
    if (!updatedAtField) return () => true;
    return (incoming, existing) => updatedAt(incoming, updatedAtField) >= updatedAt(existing, updatedAtField);
  }
  function idOf(schema, entity) {
    const value = entity[schema.idField];
    return value == null ? "" : String(value);
  }
  function upsertAll(schema, existing, incoming, resolver = updatedAtResolver(schema.updatedAtField)) {
    const by = /* @__PURE__ */ new Map();
    for (const entity of existing) {
      const id = idOf(schema, entity);
      if (id) by.set(id, entity);
    }
    for (const entity of incoming) {
      const id = idOf(schema, entity);
      if (!id) continue;
      const prev = by.get(id);
      if (!prev || resolver(entity, prev)) by.set(id, entity);
    }
    return [...by.values()];
  }

  // src/sdk/store/dynamic.ts
  function flatKeys(obj, prefix = "", keys = []) {
    if (obj == null || typeof obj !== "object") return keys;
    for (const key of Object.keys(obj)) {
      const val = obj[key];
      const full = prefix ? `${prefix}.${key}` : key;
      if (val && typeof val === "object" && !Array.isArray(val) && !(val instanceof Date)) {
        flatKeys(val, full, keys);
      } else {
        keys.push(full);
      }
    }
    return keys;
  }
  function autoColumns(records) {
    const seen = /* @__PURE__ */ new Set();
    const cols = [];
    for (const rec of records) {
      for (const key of flatKeys(rec)) {
        if (!seen.has(key)) {
          seen.add(key);
          cols.push(key);
        }
      }
    }
    return cols;
  }
  function getPath(obj, path) {
    let cur = obj;
    for (const part of path.split(".")) {
      if (cur == null || typeof cur !== "object") return null;
      cur = cur[part];
    }
    return cur != null ? cur : null;
  }
  function dynamicSchema(name, records, opts = {}) {
    var _a, _b;
    const columns = autoColumns(records).map((col) => ({
      name: col,
      get: (entity) => getPath(entity, col)
    }));
    return {
      name,
      idField: (_a = opts.idField) != null ? _a : "id",
      updatedAtField: opts.updatedAtField,
      columns,
      raw: (_b = opts.raw) != null ? _b : true
    };
  }

  // src/sdk/store/sync.ts
  var DEFAULT_TTL_SECONDS = 3600;
  function makeSyncRecord(partial) {
    return __spreadValues({
      strategy: "full",
      lastFullSyncAt: null,
      lastDeltaSyncAt: null,
      cursor: null,
      rowCount: 0,
      sourceCount: null,
      contentHash: "",
      ttlSeconds: DEFAULT_TTL_SECONDS,
      status: "ok",
      lastError: null,
      sdkVersion: null,
      blobId: null,
      blobHash: null,
      sig: null,
      keyId: null,
      lastArchiveAt: null
    }, partial);
  }
  var NUMERIC_FIELDS = [
    "lastFullSyncAt",
    "lastDeltaSyncAt",
    "rowCount",
    "sourceCount",
    "ttlSeconds",
    "lastArchiveAt"
  ];
  function toNumberOrNull(value) {
    if (value == null || value === "") return null;
    const n = Number(value);
    return Number.isNaN(n) ? null : n;
  }
  function coerceSyncRecord(raw) {
    const out = __spreadValues({}, raw);
    for (const field of NUMERIC_FIELDS) out[field] = toNumberOrNull(raw[field]);
    return makeSyncRecord(out);
  }
  function lastSyncedAt(sync) {
    var _a;
    return (_a = sync.lastDeltaSyncAt) != null ? _a : sync.lastFullSyncAt;
  }
  function isStale(sync, nowMs) {
    if (sync.status === "error") return true;
    const at = lastSyncedAt(sync);
    if (at == null) return true;
    return nowMs - at > sync.ttlSeconds * 1e3;
  }
  function nextCursor(records, updatedAtField, prev) {
    if (!updatedAtField) return prev;
    let max = prev;
    for (const rec of records) {
      const value = rec[updatedAtField];
      if (value == null) continue;
      const s = value instanceof Date ? value.toISOString() : String(value);
      if (max == null || s > max) max = s;
    }
    return max;
  }
  function contentHash(matrix) {
    const text = matrix.map((row) => row.join("")).join("");
    let h1 = 3735928559;
    let h2 = 1103547991;
    for (let i = 0; i < text.length; i++) {
      const ch = text.charCodeAt(i);
      h1 = Math.imul(h1 ^ ch, 2654435761);
      h2 = Math.imul(h2 ^ ch, 1597334677);
    }
    h1 = Math.imul(h1 ^ h1 >>> 16, 2246822507) ^ Math.imul(h2 ^ h2 >>> 13, 3266489909);
    h2 = Math.imul(h2 ^ h2 >>> 16, 2246822507) ^ Math.imul(h1 ^ h1 >>> 13, 3266489909);
    const n = 4294967296 * (2097151 & h2) + (h1 >>> 0);
    return n.toString(16).padStart(14, "0");
  }
  var SYNC_SCHEMA = {
    name: "_sync",
    idField: "table",
    columns: [
      "table",
      "strategy",
      "lastFullSyncAt",
      "lastDeltaSyncAt",
      "cursor",
      "rowCount",
      "sourceCount",
      "contentHash",
      "ttlSeconds",
      "status",
      "lastError",
      "sdkVersion",
      "blobId",
      "blobHash",
      "sig",
      "keyId",
      "lastArchiveAt"
    ],
    raw: false
  };

  // src/sdk/store/store.ts
  var HabllaStore = class {
    constructor(backend, schemas = [], options = {}) {
      __publicField(this, "backend", backend);
      __publicField(this, "options", options);
      __publicField(this, "schemas", /* @__PURE__ */ new Map());
      for (const schema of schemas) this.register(schema);
    }
    /** Registers (or replaces) a table schema. Returns this for chaining. */
    register(schema) {
      this.schemas.set(schema.name, schema);
      return this;
    }
    schemaOf(table) {
      const schema = this.schemas.get(table);
      if (!schema) throw new Error(`HabllaStore: tabela "${table}" não registrada.`);
      return schema;
    }
    /** Every registered table name (the `_sync` control table is implicit, not listed here). */
    tableNames() {
      return [...this.schemas.keys()];
    }
    /**
     * Creates the tab for every registered table (plus `_sync`) when it's missing, seeding it
     * with just the header row — the "criação/migração das abas". Existing tabs are left untouched
     * (no data loss). Runtime-agnostic: the backend's `write` is what materializes a tab. Returns
     * the names of the tables it created.
     */
    migrate() {
      return __async(this, null, function* () {
        const created = [];
        const schemas = [...this.schemas.values(), SYNC_SCHEMA];
        for (const schema of schemas) {
          const existing = yield this.backend.read(schema.name);
          if (existing.length) continue;
          yield this.backend.write(schema.name, [headerFor(schema)]);
          created.push(schema.name);
        }
        return created;
      });
    }
    /** All entities currently stored for a table (parsed from its rows; `[]` when empty). */
    all(table) {
      return __async(this, null, function* () {
        const schema = this.schemaOf(table);
        return parseMatrix(schema, yield this.backend.read(table));
      });
    }
    /**
     * Folds `incoming` into a table: reads what's there, upserts by id (resolver decides
     * collisions), rewrites only when the projected content actually changed, and refreshes
     * the `_sync` control row (cursor watermark, counts, hash, timestamps). Idempotent — the
     * same batch twice writes once.
     */
    upsert(table, incoming, opts) {
      return __async(this, null, function* () {
        var _a, _b, _c, _d, _e, _f, _g, _h, _i;
        const schema = this.schemaOf(table);
        const existing = yield this.all(table);
        const merged = upsertAll(schema, existing, incoming, opts.resolver);
        const matrix = projectMatrix(schema, merged);
        const hash = contentHash(matrix);
        const prev = yield this.syncOf(table);
        const changed = !prev || prev.contentHash !== hash;
        if (changed) yield this.backend.write(table, matrix);
        const strategy = (_b = (_a = opts.strategy) != null ? _a : prev == null ? void 0 : prev.strategy) != null ? _b : "full";
        const base = __spreadProps(__spreadValues({}, prev != null ? prev : {}), {
          table,
          strategy,
          rowCount: merged.length,
          sourceCount: (_d = (_c = opts.sourceCount) != null ? _c : prev == null ? void 0 : prev.sourceCount) != null ? _d : null,
          contentHash: hash,
          cursor: nextCursor(
            incoming,
            schema.updatedAtField,
            (_e = prev == null ? void 0 : prev.cursor) != null ? _e : null
          ),
          lastFullSyncAt: strategy === "full" ? opts.now : (_f = prev == null ? void 0 : prev.lastFullSyncAt) != null ? _f : null,
          lastDeltaSyncAt: strategy === "full" ? (_g = prev == null ? void 0 : prev.lastDeltaSyncAt) != null ? _g : null : opts.now,
          status: "ok",
          lastError: null,
          sdkVersion: (_i = (_h = this.options.sdkVersion) != null ? _h : prev == null ? void 0 : prev.sdkVersion) != null ? _i : null
        });
        if (opts.ttlSeconds != null) base.ttlSeconds = opts.ttlSeconds;
        const sync = makeSyncRecord(base);
        yield this.writeSync(sync);
        return { entities: merged, changed, sync };
      });
    }
    /**
     * Como {@link upsert}, mas descobre as colunas DOS DADOS ("todos os campos"): monta um schema
     * dinâmico ({@link dynamicSchema}) com uma coluna por campo achatado, registra-o e faz o upsert.
     * É o caminho para tabelas-espelho onde se quer a tabela inteira, não um recorte. O `_raw` fica
     * ligado, então mudanças de coluna entre syncs não perdem nada.
     */
    upsertAuto(table, records, opts) {
      return __async(this, null, function* () {
        const schema = dynamicSchema(table, records, {
          idField: opts.idField,
          updatedAtField: opts.updatedAtField
        });
        this.register(schema);
        return this.upsert(table, records, opts);
      });
    }
    /** The control row for a table (null when never synced). */
    syncOf(table) {
      return __async(this, null, function* () {
        const rows = parseMatrix(SYNC_SCHEMA, yield this.backend.read(SYNC_SCHEMA.name));
        const row = rows.find((r) => String(r.table) === table);
        return row ? coerceSyncRecord(row) : null;
      });
    }
    /** Every control row, keyed by table — the "última atualização de cada entidade" view. */
    syncAll() {
      return __async(this, null, function* () {
        const rows = parseMatrix(SYNC_SCHEMA, yield this.backend.read(SYNC_SCHEMA.name));
        const out = {};
        for (const row of rows) out[String(row.table)] = coerceSyncRecord(row);
        return out;
      });
    }
    /** Fresh enough to serve from cache? (Unknown/stale tables ⇒ false — go to the API.) */
    isFresh(table, nowMs) {
      return __async(this, null, function* () {
        const sync = yield this.syncOf(table);
        return sync != null && !isStale(sync, nowMs);
      });
    }
    /**
     * Read-through: a aba primeiro, a API só em miss/stale. Se a tabela está fresca (pelo `_sync`),
     * devolve as linhas da planilha — **zero token** (o ponto do storage: matar a saturação no
     * disparo). Senão chama `fetch` (a API), grava write-through e devolve o resultado fresco. Se
     * `fetch` falha, marca erro e **serve o stale** ("nunca ficar sem dado, mesmo que mais lento").
     * Runtime-agnóstico: `fetch` é injetado, então o mesmo método serve GAS/Node/RPO. O `fetch` deve
     * devolver ENTIDADES no shape do schema (id/updatedAt/colunas) — o mapeamento da API é do caller
     * (sem adivinhação; ver [[sem-fallbacks-arbitrarios]]).
     */
    readThrough(table, fetch, opts) {
      return __async(this, null, function* () {
        var _a, _b;
        if (yield this.isFresh(table, opts.now)) {
          return { entities: yield this.all(table), source: "cache" };
        }
        try {
          const fresh = yield fetch();
          const { entities } = yield this.upsert(table, fresh, {
            now: opts.now,
            strategy: (_a = opts.strategy) != null ? _a : "full",
            sourceCount: fresh.length,
            ttlSeconds: opts.ttlSeconds,
            resolver: opts.resolver
          });
          return { entities, source: "api" };
        } catch (err) {
          (_b = opts.onError) == null ? void 0 : _b.call(opts, err);
          yield this.markError(table, err instanceof Error ? err.message : String(err));
          return { entities: yield this.all(table), source: "stale" };
        }
      });
    }
    /** Records that a table's sync failed, without touching its data — read-through can still serve stale. */
    markError(table, message) {
      return __async(this, null, function* () {
        const prev = yield this.syncOf(table);
        const sync = makeSyncRecord(__spreadProps(__spreadValues({}, prev != null ? prev : {}), { table, status: "error", lastError: message }));
        yield this.writeSync(sync);
        return sync;
      });
    }
    /** Upserts one control row into the `_sync` table (idempotent, keyed by table name). */
    writeSync(record) {
      return __async(this, null, function* () {
        const existing = parseMatrix(SYNC_SCHEMA, yield this.backend.read(SYNC_SCHEMA.name));
        const merged = upsertAll(SYNC_SCHEMA, existing, [record], () => true);
        const matrix = projectMatrix(SYNC_SCHEMA, merged);
        yield this.backend.write(SYNC_SCHEMA.name, matrix);
      });
    }
  };

  // src/sdk/store/schemas.ts
  var SECTORS_SCHEMA = {
    name: "sectors",
    idField: "id",
    updatedAtField: "updated_at",
    columns: ["id", "name", "color", "default", "created_at", "updated_at"]
  };
  var USERS_SCHEMA = {
    name: "users",
    idField: "id",
    updatedAtField: "updated_at",
    columns: [
      "id",
      { name: "user_id", get: (e) => {
        var _a;
        return (_a = e.user) == null ? void 0 : _a.id;
      } },
      { name: "name", get: (e) => {
        var _a;
        return (_a = e.user) == null ? void 0 : _a.name;
      } },
      { name: "email", get: (e) => {
        var _a;
        return (_a = e.user) == null ? void 0 : _a.email;
      } },
      "role_type",
      "is_available",
      "created_at",
      "updated_at"
    ]
  };
  var CONNECTIONS_SCHEMA = {
    name: "connections",
    idField: "id",
    updatedAtField: "updated_at",
    columns: [
      "id",
      "name",
      "type",
      "status",
      "sector_id",
      "key",
      { name: "number", get: (e) => {
        var _a;
        return (_a = e.data) == null ? void 0 : _a.phone_number;
      } },
      "created_at",
      "updated_at"
    ]
  };
  var FLOWS_SCHEMA = {
    name: "flows",
    idField: "id",
    updatedAtField: "updated_at",
    columns: ["id", "name", "type", "is_enable", "user_id", "created_at", "updated_at"]
  };
  var TAGS_SCHEMA = {
    name: "tags",
    idField: "id",
    updatedAtField: "updated_at",
    columns: ["id", "name", "color", "sector_id", "created_at", "updated_at"]
  };
  var REASONS_SCHEMA = {
    name: "reasons",
    idField: "id",
    updatedAtField: "updated_at",
    columns: ["id", "name", "type", "sector_id", "created_at", "updated_at"]
  };
  var CUSTOM_FIELDS_SCHEMA = {
    name: "custom_fields",
    idField: "id",
    updatedAtField: "updated_at",
    columns: ["id", "name", "std_name", "target", "type", "is_required", "is_sensitive", "created_at", "updated_at"]
  };
  var CAMPAIGNS_SCHEMA = {
    name: "campaigns",
    idField: "id",
    updatedAtField: "updated_at",
    columns: [
      "id",
      "name",
      "type",
      "status",
      "quantity",
      { name: "done", get: (e) => {
        var _a;
        return (_a = e.processed_status) == null ? void 0 : _a.done;
      } },
      { name: "failed", get: (e) => {
        var _a;
        return (_a = e.processed_status) == null ? void 0 : _a.failed;
      } },
      "user_id",
      "created_at",
      "updated_at"
    ]
  };
  var PERSONS_SCHEMA = {
    name: "persons",
    idField: "personToken",
    updatedAtField: "updatedAt",
    columns: ["personToken", "ownerId", "sectorId", "tags", "lastDispatchAt", "updatedAt", "cold_ref"],
    raw: false
  };
  var FLOWS_EXECUTIONS_SCHEMA = {
    name: "flows_executions",
    idField: "id",
    updatedAtField: "finishedAt",
    columns: ["id", "flowId", "status", "startedAt", "finishedAt", "personToken", "cold_ref"],
    raw: false
  };
  var DISPATCH_RUNS_SCHEMA = {
    name: "dispatch_runs",
    idField: "runId",
    updatedAtField: "finishedAt",
    columns: [
      "runId",
      "campaignRef",
      "label",
      "templateId",
      "connectionId",
      "sectorId",
      "startedAt",
      "finishedAt",
      "engineVersion",
      "ownerUserId",
      "countTotal",
      "countSent",
      "countDelivered",
      "countRead",
      "countFailed"
    ]
  };
  var DISPATCH_LEDGER_SCHEMA = {
    name: "dispatch_ledger",
    idField: "correlationId",
    updatedAtField: "sentAt",
    columns: [
      "correlationId",
      "runId",
      "sentAt",
      "status",
      "delivered",
      "read",
      "serviceId",
      "personToken",
      "confidence",
      "matchBasis"
    ]
  };
  var DIMENSION_SCHEMAS = [
    SECTORS_SCHEMA,
    USERS_SCHEMA,
    CONNECTIONS_SCHEMA,
    FLOWS_SCHEMA,
    TAGS_SCHEMA,
    REASONS_SCHEMA,
    CUSTOM_FIELDS_SCHEMA,
    CAMPAIGNS_SCHEMA
  ];
  var FACT_SCHEMAS = [
    PERSONS_SCHEMA,
    FLOWS_EXECUTIONS_SCHEMA,
    DISPATCH_RUNS_SCHEMA,
    DISPATCH_LEDGER_SCHEMA
  ];
  var STORE_SCHEMAS = [...DIMENSION_SCHEMAS, ...FACT_SCHEMAS];
  var STORE_SCHEMAS_BY_NAME = Object.fromEntries(
    STORE_SCHEMAS.map((schema) => [schema.name, schema])
  );

  // src/runtime/gas/transport.ts
  function stripContentType(headers) {
    const out = {};
    for (const key of Object.keys(headers)) {
      const value = headers[key];
      if (value !== void 0 && key.toLowerCase() !== "content-type") out[key] = value;
    }
    return out;
  }
  function contentTypeOf(headers) {
    for (const key of Object.keys(headers)) {
      if (key.toLowerCase() === "content-type") return headers[key];
    }
    return void 0;
  }
  var UrlFetchTransport = class {
    send(req) {
      var _a, _b, _c;
      const hasBody = req.body != null && req.method.toUpperCase() !== "GET";
      const headers = (_a = req.headers) != null ? _a : {};
      const params = {
        method: req.method.toLowerCase(),
        headers,
        muteHttpExceptions: true,
        followRedirects: true
      };
      if (hasBody && isMultipart(req.body)) {
        const payload = __spreadValues({}, (_b = req.body.fields) != null ? _b : {});
        const files = req.body.files;
        for (const field of Object.keys(files)) {
          const file = files[field];
          if (!file) continue;
          payload[field] = Utilities.newBlob(file.data, file.contentType, file.filename);
        }
        params.payload = payload;
        params.headers = stripContentType(headers);
      } else if (hasBody && typeof req.body === "string") {
        params.contentType = (_c = contentTypeOf(headers)) != null ? _c : "application/json";
        params.payload = req.body;
        params.headers = stripContentType(headers);
      } else if (hasBody) {
        params.contentType = "application/json";
        params.payload = JSON.stringify(req.body);
        params.headers = stripContentType(headers);
      }
      const response = UrlFetchApp.fetch(req.url, params);
      const status = response.getResponseCode();
      const text = response.getContentText();
      let data = text;
      try {
        data = text ? JSON.parse(text) : null;
      } catch (e) {
        data = text;
      }
      return Promise.resolve({
        status,
        headers: response.getAllHeaders(),
        data
      });
    }
  };

  // src/runtime/gas/properties-strategy-cache.ts
  var KEY = "HABLLA_STRATEGY_CACHE";
  var DEFAULT_STRATEGY = "workspace";
  var LOCK_TIMEOUT_MS = 1e3;
  var PropertiesStrategyCache = class {
    load() {
      const raw = PropertiesService.getScriptProperties().getProperty(KEY);
      let map = {};
      try {
        if (raw) map = JSON.parse(raw);
      } catch (e) {
        map = {};
      }
      return Promise.resolve(map);
    }
    save(map) {
      const pruned = {};
      for (const key of Object.keys(map)) {
        const value = map[key];
        if (value !== void 0 && value !== DEFAULT_STRATEGY) pruned[key] = value;
      }
      let lock;
      try {
        lock = LockService.getScriptLock();
        lock.tryLock(LOCK_TIMEOUT_MS);
      } catch (e) {
        lock = void 0;
      }
      try {
        PropertiesService.getScriptProperties().setProperty(KEY, JSON.stringify(pruned));
      } catch (e) {
      } finally {
        try {
          lock == null ? void 0 : lock.releaseLock();
        } catch (e) {
        }
      }
      return Promise.resolve();
    }
  };

  // src/runtime/gas/spreadsheet-table-store.ts
  var SpreadsheetTableStore = class {
    constructor(options = {}) {
      __publicField(this, "options", options);
    }
    spreadsheet() {
      const { spreadsheetId } = this.options;
      if (spreadsheetId) return SpreadsheetApp.openById(spreadsheetId);
      const active = SpreadsheetApp.getActiveSpreadsheet();
      if (!active) throw new Error("SpreadsheetTableStore: sem spreadsheetId e sem planilha ativa.");
      return active;
    }
    read(table) {
      return __async(this, null, function* () {
        const sheet = this.spreadsheet().getSheetByName(table);
        if (!sheet) return [];
        const rows = sheet.getLastRow();
        const cols = sheet.getLastColumn();
        if (rows < 1 || cols < 1) return [];
        const values = sheet.getRange(1, 1, rows, cols).getValues();
        return values.map((row) => row.map(normalizeCell));
      });
    }
    write(table, matrix) {
      return __async(this, null, function* () {
        var _a, _b;
        const ss = this.spreadsheet();
        let sheet = ss.getSheetByName(table);
        if (!sheet) {
          sheet = ss.insertSheet(table);
          sheet.hideSheet();
        }
        sheet.clearContents();
        if (!matrix.length) return;
        const cols = (_b = (_a = matrix[0]) == null ? void 0 : _a.length) != null ? _b : 0;
        if (!cols) return;
        this.ensureGrid(sheet, matrix.length, cols);
        sheet.getRange(1, 1, matrix.length, cols).setValues(padMatrix(matrix, cols));
      });
    }
    tables() {
      return __async(this, null, function* () {
        return this.spreadsheet().getSheets().map((s) => s.getName());
      });
    }
    /** Cresce o grid da aba para caber `rows × cols` (o grid nasce 1000×26; tabelas grandes passam disso). */
    ensureGrid(sheet, rows, cols) {
      const maxRows = sheet.getMaxRows();
      if (rows > maxRows) sheet.insertRowsAfter(maxRows, rows - maxRows);
      const maxCols = sheet.getMaxColumns();
      if (cols > maxCols) sheet.insertColumnsAfter(maxCols, cols - maxCols);
    }
  };
  function normalizeCell(value) {
    if (value instanceof Date) return value.toISOString();
    if (value == null || value === "") return null;
    const t = typeof value;
    if (t === "string" || t === "number" || t === "boolean") return value;
    return String(value);
  }
  function padMatrix(matrix, cols) {
    return matrix.map((row) => {
      if (row.length === cols) return row;
      const out = row.slice(0, cols);
      while (out.length < cols) out.push(null);
      return out;
    });
  }

  // src/runtime/gas/sync-promise.ts
  var unhandledRejections = [];
  function trackUnhandled(promise, reason) {
    unhandledRejections.push({ promise, reason });
  }
  function untrackUnhandled(promise) {
    var _a;
    for (let i = unhandledRejections.length - 1; i >= 0; i--) {
      if (((_a = unhandledRejections[i]) == null ? void 0 : _a.promise) === promise) unhandledRejections.splice(i, 1);
    }
  }
  function drainUnhandledRejections() {
    const reasons = unhandledRejections.map((entry) => entry.reason);
    unhandledRejections.length = 0;
    return reasons;
  }
  var SyncPromise = class _SyncPromise {
    constructor(executor) {
      __publicField(this, "state", "pending");
      __publicField(this, "value");
      __publicField(this, "handlers", []);
      /** True assim que QUALQUER continuação é anexada — a rejeição flui pra jusante e é reportada só na cauda. */
      __publicField(this, "handled", false);
      if (!executor) return;
      try {
        executor((v) => this.settle("fulfilled", v), (e) => this.settle("rejected", e));
      } catch (error) {
        this.settle("rejected", error);
      }
    }
    settle(state, value) {
      if (this.state !== "pending") return;
      if (state === "fulfilled" && value && typeof value.then === "function") {
        value.then((v) => this.settle("fulfilled", v), (e) => this.settle("rejected", e));
        return;
      }
      this.state = state;
      this.value = value;
      if (state === "rejected" && !this.handled) trackUnhandled(this, value);
      const handlers = this.handlers;
      this.handlers = [];
      handlers.forEach((h) => this.run(h));
    }
    run(handler) {
      const cb = this.state === "fulfilled" ? handler.onFulfilled : handler.onRejected;
      if (typeof cb !== "function") {
        handler.next.settle(this.state, this.value);
        return;
      }
      try {
        handler.next.settle("fulfilled", cb(this.value));
      } catch (error) {
        handler.next.settle("rejected", error);
      }
    }
    then(onFulfilled, onRejected) {
      this.handled = true;
      untrackUnhandled(this);
      const next = new _SyncPromise();
      const handler = {
        onFulfilled,
        onRejected,
        next
      };
      if (this.state === "pending") this.handlers.push(handler);
      else this.run(handler);
      return next;
    }
    catch(onRejected) {
      return this.then(void 0, onRejected);
    }
    finally(onFinally) {
      return this.then(
        (v) => {
          onFinally == null ? void 0 : onFinally();
          return v;
        },
        (e) => {
          onFinally == null ? void 0 : onFinally();
          throw e;
        }
      );
    }
    static resolve(value) {
      return new _SyncPromise((resolve) => resolve(value));
    }
    static reject(reason) {
      return new _SyncPromise((_, reject) => reject(reason));
    }
    static all(items) {
      return new _SyncPromise((resolve, reject) => {
        const list = Array.from(items);
        const out = new Array(list.length);
        let remaining = list.length;
        if (remaining === 0) {
          resolve(out);
          return;
        }
        list.forEach((item, index) => {
          _SyncPromise.resolve(item).then((v) => {
            out[index] = v;
            if (--remaining === 0) resolve(out);
          }, reject);
        });
      });
    }
    static allSettled(items) {
      return _SyncPromise.all(
        Array.from(items).map(
          (item) => _SyncPromise.resolve(item).then(
            (value) => ({ status: "fulfilled", value }),
            (reason) => ({ status: "rejected", reason })
          )
        )
      );
    }
    static race(items) {
      return new _SyncPromise((resolve, reject) => {
        for (const item of items) _SyncPromise.resolve(item).then(resolve, reject);
      });
    }
  };
  function unwrap(promise) {
    let resolved = false;
    let rejected = false;
    let value;
    let reason;
    promise.then((v) => {
      resolved = true;
      value = v;
    }, (e) => {
      rejected = true;
      reason = e;
    });
    if (rejected) throw reason;
    if (!resolved) throw new Error("Chamada do SDK não resolveu de forma síncrona (I/O assíncrono num runtime síncrono?).");
    return value;
  }

  // src/runtime/gas/entry.ts
  var FETCH_ALL_BATCH = 100;
  var SET_TIMEOUT_CAP_MS = 15e3;
  function readVariables() {
    var _a, _b, _c, _d, _e, _f;
    const g = globalThis;
    const env = (_a = g.HABLLA_ENV) != null ? _a : {};
    const prop = (key) => {
      var _a2;
      try {
        return (_a2 = PropertiesService.getScriptProperties().getProperty(key)) != null ? _a2 : "";
      } catch (e) {
        return "";
      }
    };
    return {
      workspaceId: (_b = env.workspaceId) != null ? _b : prop("HABLLA_WORKSPACE_ID"),
      refreshToken: (_c = env.refreshToken) != null ? _c : prop("HABLLA_REFRESH_TOKEN"),
      firebaseApiKey: (_d = env.firebaseApiKey) != null ? _d : prop("HABLLA_FIREBASE_API_KEY"),
      workspaceToken: (_e = env.workspaceToken) != null ? _e : prop("HABLLA_WORKSPACE_TOKEN") || void 0,
      baseUrl: (_f = env.baseUrl) != null ? _f : prop("HABLLA_BASE_URL") || void 0,
      debug: env.debug
    };
  }
  function gasSetTimeout(callback, ms) {
    const delay = Math.min(Math.max(0, ms != null ? ms : 0), SET_TIMEOUT_CAP_MS);
    if (delay > 0) {
      try {
        Utilities.sleep(delay);
      } catch (e) {
      }
    }
    callback();
    return 0;
  }
  function runSync(call) {
    const g = globalThis;
    const nativePromise = g.Promise;
    const nativeSetTimeout = g.setTimeout;
    g.Promise = SyncPromise;
    g.setTimeout = gasSetTimeout;
    try {
      return unwrap(call());
    } finally {
      g.Promise = nativePromise;
      g.setTimeout = nativeSetTimeout;
      const leaked = drainUnhandledRejections();
      if (leaked.length > 0 && typeof console !== "undefined") {
        for (const reason of leaked) console.warn("SyncPromise: rejeição não-tratada", reason);
      }
    }
  }
  function makeGetAll(client, base, workspaceId) {
    const auth = client.auth;
    const keyOf = (rawPath) => "GET:" + rawPath.split("?")[0].replace("{ws}", "{workspace_id}").replace(/\/[0-9a-fA-F]{24}(?=\/|$)/g, "/{id}");
    return function getAll(paths) {
      var _a;
      const out = new Array(paths.length).fill(null);
      if (paths.length === 0) return out;
      const g = globalThis;
      const nativePromise = g.Promise;
      const nativeSetTimeout = g.setTimeout;
      g.Promise = SyncPromise;
      g.setTimeout = gasSetTimeout;
      let wsHeader = "";
      let bearerHeader = "";
      const strategyFor = [];
      try {
        wsHeader = String((_a = unwrap(auth.authorization("workspace"))) != null ? _a : "");
        bearerHeader = String(unwrap(auth.authorization("bearer")));
        for (let i = 0; i < paths.length; i++) {
          strategyFor[i] = wsHeader ? unwrap(auth.resolveStrategy(keyOf(paths[i]))) : "bearer";
        }
      } finally {
        g.Promise = nativePromise;
        g.setTimeout = nativeSetTimeout;
      }
      const headerFor2 = (strategy) => strategy === "workspace" && wsHeader ? wsHeader : bearerHeader;
      const winner = new Array(paths.length).fill(null);
      for (let start = 0; start < paths.length; start += FETCH_ALL_BATCH) {
        const end = Math.min(start + FETCH_ALL_BATCH, paths.length);
        let pending = [];
        for (let i = start; i < end; i++) pending.push({ i, strategy: strategyFor[i], flipped: false });
        for (let attempt = 0; attempt < 4 && pending.length; attempt++) {
          if (attempt > 0) {
            try {
              Utilities.sleep(500 * attempt);
            } catch (e) {
            }
          }
          const requests = pending.map((p) => ({
            url: base + paths[p.i].replace("{ws}", workspaceId),
            method: "get",
            headers: { Authorization: headerFor2(p.strategy) },
            muteHttpExceptions: true
          }));
          let responses;
          try {
            responses = UrlFetchApp.fetchAll(requests);
          } catch (e) {
            responses = [];
          }
          const next = [];
          for (let k = 0; k < pending.length; k++) {
            const p = pending[k];
            const response = responses[k];
            if (!response) {
              next.push(p);
              continue;
            }
            const status = response.getResponseCode();
            if (status >= 200 && status < 300) {
              try {
                out[p.i] = JSON.parse(response.getContentText());
              } catch (e) {
              }
              winner[p.i] = p.strategy;
              continue;
            }
            if (status === 429 || status === 503) {
              next.push(p);
              continue;
            }
            if (!p.flipped && wsHeader) {
              p.strategy = p.strategy === "workspace" ? "bearer" : "workspace";
              p.flipped = true;
              next.push(p);
              continue;
            }
          }
          pending = next;
        }
      }
      g.Promise = SyncPromise;
      g.setTimeout = gasSetTimeout;
      try {
        for (let i = 0; i < paths.length; i++) {
          const strategy = winner[i];
          if (strategy) unwrap(auth.recordStrategy(keyOf(paths[i]), strategy));
        }
      } finally {
        g.Promise = nativePromise;
        g.setTimeout = nativeSetTimeout;
      }
      return out;
    };
  }
  function makeStore() {
    let sheetId;
    try {
      sheetId = PropertiesService.getScriptProperties().getProperty("HABLLA_SHEET_ID") || void 0;
    } catch (e) {
      sheetId = void 0;
    }
    const backend = new SpreadsheetTableStore({ spreadsheetId: sheetId });
    return new HabllaStore(backend, STORE_SCHEMAS);
  }
  function installHabllaClient() {
    var _a;
    const vars = readVariables();
    const client = new HabllaClient(__spreadProps(__spreadValues({}, vars), {
      transport: new UrlFetchTransport(),
      strategyCache: new PropertiesStrategyCache()
    }));
    const g = globalThis;
    g.hablla = client;
    g.Hablla = {
      client,
      runSync,
      unwrap,
      getAll: makeGetAll(client, (_a = vars.baseUrl) != null ? _a : "https://api.hablla.com", vars.workspaceId),
      store: makeStore()
    };
    return client;
  }
  installHabllaClient();
})();
